---
phase: 00-hardware-proof
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified:
  - packages/sportident/src/bin/fartola-readout.ts
  - packages/sportident/src/bin/record.ts
  - packages/sportident/src/bin/replay.ts
  - packages/sportident/src/bin/record.test.ts
  - packages/sportident/src/bin/replay.test.ts
  - scripts/hardware-smoke.sh
  - packages/sportident/tests/fixtures/jonas/.gitkeep
  - packages/sportident/README.md
autonomous: false
requirements: [REQ-HW-001, REQ-HW-002, REQ-HW-004]
must_haves:
  truths:
    - 'fartola-readout --record <basename> writes a DIRECTIONAL transcript <basename>.bytes.hex (one event per line: `out <hex>` for transport sends, `in <hex>` for transport receives, in chronological order) AND <basename>.expected.json (NDJSON lines actually emitted). Codex review #6.'
    - 'fartola-readout --replay <basename> reads the directional transcript through a deterministic playback fake-transport that emits each `in` chunk only AFTER the corresponding `out` chunk has been written by SiMainStation — so the replay reproduces the exact send/receive ordering. Output is byte-equal to <basename>.expected.json (ts_ms normalized to 0).'
    - 'fartola-readout --record path validation accepts basenames resolving under EITHER process.cwd() OR an explicitly-permitted /tmp root (codex review #7). Path-traversal outside both roots is rejected before any file is opened. The allowed roots are documented in the action and listed in acceptance.'
    - 'scripts/hardware-smoke.sh invokes fartola-readout `--record --once` SEPARATELY for each card type (SI5, SI9, SI10, SIAC), producing 4 distinct fixture pairs natively — no post-hoc splitting (codex review #8).'
    - 'scripts/hardware-smoke.sh enumerates /dev/ttyUSB0 + dialout-group preflight, then prompts the operator to insert SI5, SI9, SI10, SIAC in turn; uses a JSON parser (node -e or jq) to assert the expected NDJSON event types per card (NOT grep on key-order); exits 0 only if all 4 round-trip cleanly'
    - "Hardware smoke run against Jonas's bench inventory (SI5, SI9, SI10, SIAC Air+) produces 4 captured fixtures in packages/sportident/tests/fixtures/jonas/ committed alongside this plan's SUMMARY"
    - 'Once smoke passes, repo is tagged v0.0.1-handshake (signed annotated tag), satisfying Phase 0 success criterion #6'
    - 'README documents the hardware smoke runbook + the --record/--replay workflow + Linux dialout/brltty pre-flight notes (RESEARCH §Landmines #1, #2)'
  artifacts:
    - path: 'packages/sportident/src/bin/record.ts'
      provides: 'RecordSink: extends NdjsonEmitter; tees stdout NDJSON to <basename>.expected.json AND captures a DIRECTIONAL transcript to <basename>.bytes.hex (`out <hex>` for sends, `in <hex>` for receives). Codex review #6.'
      exports: ['RecordSink']
    - path: 'packages/sportident/src/bin/replay.ts'
      provides: 'Reads the directional transcript, drives the same SiMainStation+NdjsonEmitter pipeline with a deterministic playback transport that gates `in` chunks on `out` chunk delivery, asserts output matches <basename>.expected.json (ts_ms normalized). Codex review #6.'
      exports: ['replayFixture']
    - path: 'scripts/hardware-smoke.sh'
      provides: 'Operator-driven assertion script: preflight + 4 separate per-card `--record --once` invocations (codex review #8) + JSON-parsed NDJSON assertions (codex review LOW) + summary report'
    - path: 'packages/sportident/tests/fixtures/jonas/{si5,si9,si10,siac}-jonas-001.{bytes.hex,expected.json}'
      provides: 'Real-hardware-captured fixtures, 4 card types × 2 files each = 8 files committed by this plan; bytes.hex is the directional transcript format'
  key_links:
    - from: 'packages/sportident/src/bin/fartola-readout.ts (--record flag)'
      to: 'packages/sportident/src/bin/record.ts (RecordSink)'
      via: 'conditional instantiation when --record is present; subscribes to both transport raw sends and raw receives'
      pattern: 'RecordSink'
    - from: 'scripts/hardware-smoke.sh'
      to: 'packages/sportident/dist/bin/fartola-readout.cjs'
      via: '4 separate spawns (one per card type), each `--record --once <basename>`'
      pattern: 'fartola-readout'
    - from: 'packages/sportident/src/bin/replay.test.ts'
      to: 'packages/sportident/tests/fixtures/jonas/*.bytes.hex + *.expected.json'
      via: 'fixture-replay round-trip via the directional-transcript replay engine'
      pattern: 'fixtures/jonas'
---

<objective>
Close Phase 0: implement `--record`/`--replay` modes on the bin with a DIRECTIONAL transcript format (codex review #6) so the replay is deterministic; flesh out `scripts/hardware-smoke.sh` to invoke `--record --once` separately per card type (codex review #8) and assert per-type NDJSON events via JSON parsing (codex review LOW); capture the four fixtures (SI5, SI9, SI10, SIAC) and commit them; then tag `v0.0.1-handshake`. The phase exits when CI is green AND the smoke script exits 0 AND the tag is pushed.

**KEY DESIGN CHOICES FROM CODEX REVIEW:**

- **Review #6 (HIGH):** The transcript format is `out <hex>\n` per transport send and `in <hex>\n` per transport receive, in chronological order. Replay walks the file line-by-line; on `out` it sets a "next-out-expected" buffer, and on `in` it queues the chunk to be emitted by the playback transport AFTER the next matching `out` is performed by SiMainStation. This gives byte-for-byte deterministic replay.
- **Review #7 (HIGH):** `RecordSink` accepts basenames resolving under EITHER `process.cwd()` (default fixtures-in-repo case) OR under `/tmp` (explicit, so test files can use `/tmp/fartola-record-test-<nonce>`). Both roots are spelled out in the validation function; nothing outside both is permitted.
- **Review #8 (HIGH):** Hardware smoke runs `fartola-readout --record --once <basename>` four times in sequence, each capturing a SINGLE card. The 4 per-card fixtures are produced natively; no post-hoc splitting.
- **Review LOW (threat-model theatre):** Threat model is trimmed to the four real risks: parser DoS, pending-send aborts, PII, and record-path traversal. Commit-message spoofing / CRC timing / registry tampering are removed.

Purpose: This is the proof point. D-17 + D-18 + D-19 — fixture-based unit tests are great, but Phase 0 isn't done until a real card has been read end-to-end. The `--record` mode satisfies D-18's "ours" side of the two-source fixture strategy. The smoke script satisfies D-19. The tag satisfies Phase 0 success criterion #6. Critically, this plan has TWO human-verify checkpoints: (a) running the smoke script and confirming the captured card numbers match the printed labels on Jonas's physical cards, and (b) signing off on the tag.

Output: 4 real-hardware fixtures committed (directional transcript + expected.json per card); smoke script that any future contributor can run and that natively produces per-card fixtures; v0.0.1-handshake tag pushed; README updated with the runbook. After this, Phase 1 begins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-hardware-proof/00-CONTEXT.md
@.planning/phases/00-hardware-proof/00-RESEARCH.md
@.planning/phases/00-hardware-proof/00-VALIDATION.md
@.planning/phases/00-hardware-proof/00-01-SUMMARY.md
@.planning/phases/00-hardware-proof/00-02-SUMMARY.md
@.planning/phases/00-hardware-proof/00-03-SUMMARY.md
@.planning/phases/00-hardware-proof/00-04-SUMMARY.md
@.planning/phases/00-hardware-proof/00-05-SUMMARY.md
@.planning/ROADMAP.md

<interfaces>
<!-- From Plan 05 -->
import { NdjsonEmitter } from '../output/ndjson.ts';
import { SerialTransport } from '../transport/SerialTransport.ts';
import { SiMainStation } from '../SiStation/SiMainStation.ts';

<!-- This plan adds: -->

export class RecordSink extends NdjsonEmitter {
constructor(opts: { device_path: string; recordBasename: string; allowedRoots?: string[] });
onRawSend(bytes: number[]): void; // hook the SerialTransport send pathway (every chunk written to wire)
onRawReceive(bytes: number[]): void; // hook the SerialTransport 'data' event (every chunk read from wire)
close(): Promise<void>; // flushes bytes.hex and expected.json to disk
}

export async function replayFixture(basename: string): Promise<{ matches: boolean; diff?: string }>;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: --record mode (RecordSink with directional transcript) + --replay mode (deterministic playback) + their unit tests</name>
  <files>packages/sportident/src/bin/record.ts, packages/sportident/src/bin/replay.ts, packages/sportident/src/bin/record.test.ts, packages/sportident/src/bin/replay.test.ts, packages/sportident/src/bin/fartola-readout.ts</files>
  <read_first>
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-RESEARCH.md §"Fixture Strategy" + §"Test Fixture Format — Locally captured fixtures (D-18 'ours')" + §"Fixture file format spec" (file naming and shape)
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-RESEARCH.md §"Open Questions for Planner" #2 (recommendation: BOTH bytes.hex AND expected.json files)
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-CONTEXT.md (D-18 fixture sources both)
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-REVIEWS.md (codex concerns #6 directional transcript, #7 allowed roots include /tmp)
    - packages/sportident/src/output/ndjson.ts (NdjsonEmitter — RecordSink extends/wraps)
    - packages/sportident/src/bin/fartola-readout.ts (Plan 05 — has `--record <path>` stubbed; this task implements it)
    - packages/sportident/src/transport/SerialTransport.ts (Plan 04 — listens to 'data' event and exposes send())
  </read_first>
  <behavior>
    record.test.ts (codex review #7 — the test writes to /tmp, so the allowed-roots list MUST include /tmp):
    - Construct a RecordSink with `{device_path: '/dev/null', recordBasename: '/tmp/fartola-record-test-<nonce>', allowedRoots: [process.cwd(), '/tmp']}`. Inject the `out` of the inner NdjsonEmitter so we can also assert in-test what was emitted.
    - Emit a connection_changed/open + card_inserted + card_read sequence.
    - Call sink.onRawSend([0x02, 0xF0, 0x01, 0x4D]) followed by sink.onRawReceive([0xFF, 0xFF, 0x02, 0xF0, 0x01, 0x4D, 0xBA, 0xBB, 0x03]).
    - Call sink.close().
    - Assert `/tmp/fartola-record-test-<nonce>.bytes.hex` exists and contains EXACTLY two non-comment lines: `out 02 F0 01 4D` followed by `in FF FF 02 F0 01 4D BA BB 03` (codex review #6 — directional transcript). A `# Captured ...` comment header is allowed at the top.
    - Assert `/tmp/fartola-record-test-<nonce>.expected.json` exists, each line is JSON.parse-able, and the parsed objects (with ts_ms set to 0) match what was emitted (also with ts_ms 0).

    Path-traversal test (codex review #7):
    - Constructing a RecordSink with `{recordBasename: '/etc/passwd', allowedRoots: [process.cwd(), '/tmp']}` throws synchronously with an error message containing the substring `outside allowed roots` BEFORE any stream is opened.
    - Constructing with `recordBasename: '../escape', allowedRoots: [process.cwd()]` also throws (resolves outside cwd).
    - Constructing with `recordBasename: '/tmp/ok-here', allowedRoots: [process.cwd(), '/tmp']` succeeds (/tmp explicitly permitted).
    - Constructing with `recordBasename: 'fixtures/jonas/si5-test', allowedRoots: [process.cwd()]` succeeds (resolves under cwd).

    replay.test.ts (codex review #6 — directional transcript drives deterministic playback):
    - Create a synthetic basename pair in /tmp by manually writing a directional bytes.hex (multiple `out <hex>` / `in <hex>` lines reproducing a handshake + SI5 card read) + expected.json. The transcript MUST reflect actual SiMainStation send/receive ordering (the test author derives this from a recorded run or from a Plan 04 FakeSerialTransport rule list).
    - Call replayFixture(basename) — returns `{matches: true}`.
    - Now corrupt expected.json (change a punch code by 1); replayFixture returns `{matches: false, diff: <non-empty>}` with a unified-diff summary.
    - Edge case: a bytes.hex where the recorded `out` order differs from what SiMainStation would actually send (simulate a corrupted transcript) — replayFixture detects the mismatch and returns `{matches: false, diff: <message containing 'out mismatch' or similar>}`.

    fartola-readout.ts (--record flag): when `--record <basename>` is present, use RecordSink instead of NdjsonEmitter. Subscribe BOTH transport pathways: `transport.on('data', sink.onRawReceive.bind(sink))` AND intercept transport.send (either via a wrapper or by adding a `'rawSend'` event in SerialTransport — see action). The bin must guarantee that when --record exits (SIGINT or --once), sink.close() is awaited before process.exit.

  </behavior>
  <action>
    Three things:

    - `record.ts` (RecordSink): extends NdjsonEmitter. Constructor takes `{device_path, recordBasename, device_serial?, includeRawPages?, allowedRoots?}` where `allowedRoots` defaults to `[process.cwd()]` but the bin will pass `[process.cwd(), '/tmp']` so tests can write to /tmp (codex review #7). On construction:
      1. Resolve `recordBasename` via `path.resolve(recordBasename)` to absolute.
      2. Validate: the resolved path must START with one of `allowedRoots` (each itself resolved). If not, throw `new Error('recordBasename resolves outside allowed roots: ' + resolved + ' (allowed: ' + allowedRoots.join(', ') + ')')` BEFORE opening any stream.
      3. Open two write streams: `<basename>.bytes.hex` and `<basename>.expected.json`. Override the base class's internal `out` to write to `expected.json` (line-buffered, append) AND ALSO call the parent's `out` (stdout default) so stdout NDJSON is preserved during recording — operators still see live events.

      Methods:
      - `onRawSend(bytes: number[])`: append `out ${hexEncode(bytes)}\n` to bytes.hex (codex review #6 — directional transcript). hexEncode = `bytes.map(b => b.toString(16).padStart(2,'0').toUpperCase()).join(' ')`.
      - `onRawReceive(bytes: number[])`: append `in ${hexEncode(bytes)}\n` to bytes.hex.
      - `close()`: ensure both write streams are flushed and ended before resolving.

      Header of bytes.hex on file-open: `# Captured ${new Date().toISOString()} from ${device_path}\n# device_serial: ${device_serial ?? 'unknown'}\n# Format: directional transcript ('out <hex>' for sends, 'in <hex>' for receives), chronological.\n`. Header of expected.json: empty (it's pure NDJSON).

      **Path-traversal mitigation (codex review #7, T-00-19):** validate `recordBasename` against the `allowedRoots` list. Resolve to absolute path; reject if resolved path is NOT under any permitted root. Throw a clear error before opening any stream. NEVER permit basenames outside cwd OR /tmp (when /tmp is in the list).

    - `replay.ts` (replayFixture): reads `<basename>.bytes.hex` line by line; comment lines (`# ...`) and blank lines skipped; non-comment lines must match `/^(out|in) ([0-9A-Fa-f ]+)$/` — parse direction + hex-encoded chunk. Read `<basename>.expected.json` (NDJSON).

      Build a `PlaybackTransport` implementing ISerialTransport that:
      - Has a queue of inbound chunks (from `in` lines) and a pointer into the transcript.
      - On `transport.send(bytes)`: read the next `out <hex>` line from the transcript; assert `hexEncode(bytes) === expectedOutHex` (case-insensitive, whitespace-normalized). If mismatch, store the mismatch reason and reject the send promise (replayFixture will surface as `diff`).
      - After the matching `out`, scan forward in the transcript for any consecutive `in <hex>` lines and emit them as 'data' events on the next tick. Stop scanning at the next `out` line.

      Run through SiMainStation + NdjsonEmitter (with an `out` capturing into an array). Normalize ts_ms to 0 on every captured + expected line (parse JSON, set ts_ms=0, stringify). Compare line-by-line; if mismatch, return `{matches: false, diff: <unified diff string built via a tiny in-house diff helper or just `node:assert.deepStrictEqual` exception .message>}`.

    - `record.test.ts` + `replay.test.ts`: tests as per the behavior block. Use `/tmp/fartola-record-test-${process.pid}-${Date.now()}` for temp basenames AND pass `allowedRoots: [process.cwd(), '/tmp']` to the RecordSink constructor (codex review #7). Clean up in an after-hook.

    - `fartola-readout.ts`: replace the `// implemented in plan-06` stub for `--record` parsing with the real wiring. Add CLI parsing: `--record <basename>` (string), `--once` (boolean). When the bin parses `--record`, it constructs `new RecordSink({device_path, recordBasename, allowedRoots: [process.cwd(), '/tmp']})` (CWD + /tmp; the smoke script and tests both need these roots).

      Wire the transport sends to the sink: `SerialTransport.ts` should emit a `'rawSend'` event on every send (add this in this task if not already present — small, additive change). Alternatively, wrap the transport at the bin layer (`const tappedTransport = new Proxy(transport, { get(t, k) { if (k === 'send') return (b) => { sink.onRawSend(b); return t.send(b); }; return Reflect.get(t, k); } });`). Pick one; the proxy is simpler and keeps Plan 04's SerialTransport surface minimal. Also subscribe `transport.on('data', sink.onRawReceive.bind(sink))`.

      When both `--record` and `--once` are set, the bin reads exactly one card, awaits sink.close(), exits 0. When `--record` is set without `--once`, the bin runs until SIGINT; on SIGINT it flushes the sink and exits 0. Update package.json scripts in `packages/sportident/package.json`: add `"dev:record": "node --enable-source-maps src/bin/fartola-readout.ts --record"` so Jonas can run `pnpm dev:record fixtures/jonas/si5-jonas-001` directly.

    NEVER spawn a subprocess for record output — record IS the bin running, just with an extra sink. NEVER allow the `--record <basename>` value to escape both allowed roots (cwd OR /tmp) via `..`; Reject before opening any file. NEVER use the old undirectional `bytes.hex` format (single chunk dumped) — the file format is the directional transcript (codex review #6).

  </action>
  <verify>
    <automated>cd /home/jonas/src/fartOLa && pnpm --filter @fartola/sportident exec tsc --noEmit && pnpm --filter @fartola/sportident exec node --test --test-reporter=spec packages/sportident/src/bin/record.test.ts packages/sportident/src/bin/replay.test.ts 2>&1 | tee /tmp/p06-t1.log && ! grep -E '^# fail [1-9]' /tmp/p06-t1.log && pnpm --filter @fartola/sportident exec node --test 'packages/sportident/src/**/*.test.ts' 2>&1 | tee /tmp/p06-cumulative.log && ! grep -E '^# fail [1-9]' /tmp/p06-cumulative.log && pnpm --filter @fartola/sportident exec tsup --silent && test -f packages/sportident/dist/bin/fartola-readout.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `packages/sportident/src/bin/record.ts` exports `RecordSink`; class extends `NdjsonEmitter` (greppable: `grep -c 'extends NdjsonEmitter' packages/sportident/src/bin/record.ts` >= 1)
    - `packages/sportident/src/bin/record.ts` defines `onRawSend(bytes)` AND `onRawReceive(bytes)` methods writing `out` and `in` prefixed lines respectively (greppable: `grep -c "'out '" packages/sportident/src/bin/record.ts` >= 1 AND `grep -c "'in '" packages/sportident/src/bin/record.ts` >= 1; codex review #6)
    - `packages/sportident/src/bin/record.ts` contains an `allowedRoots` parameter and a validation guard rejecting basenames outside the allowed roots (greppable: `grep -c 'allowedRoots' packages/sportident/src/bin/record.ts` >= 2; codex review #7)
    - `packages/sportident/src/bin/replay.ts` exports `replayFixture(basename: string)` returning `{matches: boolean; diff?: string}` AND parses the directional transcript (`out` / `in` prefixes; greppable: `grep -cE "(^|[^a-zA-Z])(out|in) " packages/sportident/src/bin/replay.ts` >= 2)
    - `pnpm --filter @fartola/sportident exec node --test packages/sportident/src/bin/record.test.ts` exits 0 with >= 3 passing tests: (a) directional transcript round-trip — both `out` and `in` lines written in chronological order; (b) `/tmp` basename accepted when `/tmp` in allowedRoots (codex review #7); (c) `/etc/passwd` or `../escape` basename rejected with `outside allowed roots` error
    - `pnpm --filter @fartola/sportident exec node --test packages/sportident/src/bin/replay.test.ts` exits 0 with >= 3 passing tests (happy-path matches=true + corrupted-expected matches=false + corrupted-transcript-out-order matches=false)
    - `packages/sportident/src/bin/fartola-readout.ts` contains `--record` flag handling AND `--once` flag handling (greppable: `grep -c "'--record'" packages/sportident/src/bin/fartola-readout.ts` >= 1 AND `grep -c "'--once'" packages/sportident/src/bin/fartola-readout.ts` >= 1)
    - `packages/sportident/src/bin/fartola-readout.ts` instantiates RecordSink with `allowedRoots: [process.cwd(), '/tmp']` (or equivalent both-roots construction; greppable: `grep -c "'/tmp'" packages/sportident/src/bin/fartola-readout.ts` >= 1)
    - `packages/sportident/package.json` `scripts.dev:record` is set
    - Cumulative test suite passes (`pnpm --filter @fartola/sportident exec node --test 'packages/sportident/src/**/*.test.ts'` exits 0)
    - `tsup` build succeeds and produces `dist/bin/fartola-readout.cjs`
  </acceptance_criteria>
  <done>--record writes directional transcript + expected.json atomically; --replay round-trips byte-equal via the deterministic playback engine; path-traversal blocked but /tmp is an explicit allowed root; bin's CLI parsing updated; all tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Hardware smoke script (4 per-card --record --once invocations + JSON-parsed assertions) + README runbook + diagnostic preflight</name>
  <files>scripts/hardware-smoke.sh, packages/sportident/README.md</files>
  <read_first>
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-RESEARCH.md §"Manual Hardware Smoke" (script outline) + §"Validation Architecture — Manual Hardware Smoke" + §"Landmines & Pitfalls" §1 (brltty) + §2 (dialout group) + §6 (multi-page read timeout)
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-VALIDATION.md §"Manual-Only Verifications" (TBD-HW-01, TBD-HW-02 — task IDs become 00-06-02-HW-CARDS, 00-06-02-HW-PORT)
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-CONTEXT.md (D-19 scripted smoke; D-10 card-type coverage: SI5, SI9, SI10, SIAC)
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-REVIEWS.md (codex concern #8 — per-card --record --once; codex LOW — use JSON parser not grep)
    - scripts/hardware-smoke.sh (Plan 01 stub — replace)
    - packages/sportident/README.md (Plan 01 scaffold — append the runbook)
    - packages/sportident/dist/bin/fartola-readout.cjs (Plan 06 Task 1 build — the smoke runs this)
  </read_first>
  <action>
    Replace the Plan 01 stub `scripts/hardware-smoke.sh` with a real assertion script that runs ONE `--record --once` invocation per card type (codex review #8). Concrete structure (`bash`, `set -euo pipefail`):

    1. **Preflight** (covers RESEARCH §Landmines #1 + #2):
       - Assert `/dev/ttyUSB0` exists; if not, suggest `dmesg | grep cp210x` + `sudo apt-get remove brltty`.
       - Assert `[[ -r /dev/ttyUSB0 && -w /dev/ttyUSB0 ]]`; if not, suggest `sudo usermod -aG dialout $USER && log out/in`.
       - Assert `node --version` >= v22.18 (TS-stripping support).
       - Assert `pnpm --version` works.
       - Assert `packages/sportident/dist/bin/fartola-readout.cjs` exists; if not, run `pnpm --filter @fartola/sportident exec tsup` first.

    2. **Per-card capture loop (codex review #8):**

       ```bash
       FIXTURE_DIR="packages/sportident/tests/fixtures/jonas"
       mkdir -p "$FIXTURE_DIR"
       FAIL=0
       for card_type in si5 si9 si10 siac; do
         BASENAME="${FIXTURE_DIR}/${card_type}-jonas-001"
         CARD_LABEL="$(echo "$card_type" | tr 'a-z' 'A-Z')"
         echo
         echo "=== ${CARD_LABEL} capture ==="
         echo "Insert the ${CARD_LABEL} card. Press Enter to start the --record --once capture (Ctrl-C to abort)."
         read -r
         # --once exits after a single cardRead. --record captures directional transcript + NDJSON.
         pnpm exec fartola-readout --record "$BASENAME" --once --include-raw-pages 2> "${BASENAME}.stderr.log"
         # JSON-parsed assertions (codex review LOW — no grep on key-order):
         node -e "
           const fs = require('fs');
           const lines = fs.readFileSync('${BASENAME}.expected.json', 'utf8').trim().split('\n');
           const events = lines.map(l => JSON.parse(l));
           const ci = events.find(e => e.event === 'card_inserted');
           const cr = events.find(e => e.event === 'card_read');
           if (!ci || ci.card_type !== '${CARD_LABEL}') {
             console.error('FAIL: expected card_inserted with card_type=${CARD_LABEL}, got', ci);
             process.exit(1);
           }
           if (!cr || cr.card_type !== '${CARD_LABEL}') {
             console.error('FAIL: expected card_read with card_type=${CARD_LABEL}, got', cr);
             process.exit(1);
           }
           console.log('  -> Detected ${CARD_LABEL} card_number=' + cr.card_number + '. Verify against printed label.');
         " || { FAIL=1; tail -20 "${BASENAME}.stderr.log"; }
         echo "Remove the ${CARD_LABEL} card. Press Enter when ready for next card."
         read -r
       done
       if [[ "$FAIL" -ne 0 ]]; then
         echo "smoke FAILED" >&2
         exit 1
       fi
       echo
       echo "Smoke passed: 4 cards round-tripped. Fixtures in ${FIXTURE_DIR}/."
       echo "Next step: 'git add ${FIXTURE_DIR} && git commit -m \"test(00): capture jonas hardware fixtures\"' then this plan's checkpoint 3 (tag)."
       exit 0
       ```

       NOTE: `--once` causes fartola-readout to exit cleanly after the first cardRead, so each loop iteration runs a fresh process. This makes the 4 fixtures natively per-card (codex review #8) — no post-hoc splitting required.

    3. **Trap on error**: `set -E; trap 'echo "smoke FAILED at line $LINENO" >&2; exit 1' ERR` so any preflight or assertion failure aborts cleanly.

    Update `packages/sportident/README.md` with a "Running the hardware smoke" runbook section: ~30-50 lines, terse (Jonas reads on mobile — RESEARCH §"Project Constraints"). Sections:
    - **Preflight** (4 bullets: ttyUSB0, dialout, Node 22, build dist).
    - **Linux gotchas** (brltty + dialout — link to RESEARCH §Landmines #1/#2).
    - **Run** (`./scripts/hardware-smoke.sh`).
    - **--record / --replay** workflow (4 bullets, including: bytes.hex is a directional transcript with `out` / `in` lines).
    - **Tag** (`git tag -s v0.0.1-handshake -m "Phase 0 hardware-proof complete"`).

    NEVER hard-code `/home/jonas/...` paths anywhere — use `$(pwd)` or relative. NEVER skip a card-type assertion to make smoke pass faster; D-10 lists 4 card types Jonas owns and ALL 4 must round-trip. NEVER use `grep -c '"event":"card_inserted".*"card_type":"SI5"'` (key-order dependent — codex review LOW); use `node -e` JSON parsing.

  </action>
  <verify>
    <automated>cd /home/jonas/src/fartOLa && test -x scripts/hardware-smoke.sh && bash -n scripts/hardware-smoke.sh && grep -c 'si5\|si9\|si10\|siac\|SI5\|SI9\|SI10\|SIAC' scripts/hardware-smoke.sh | { read n; [ "$n" -ge 4 ] || { echo "FAIL: not all 4 card types referenced"; exit 1; }; } && grep -c -- '--record' scripts/hardware-smoke.sh | { read n; [ "$n" -ge 1 ] || { echo "FAIL: smoke must use --record (codex #8)"; exit 1; }; } && grep -c -- '--once' scripts/hardware-smoke.sh | { read n; [ "$n" -ge 1 ] || { echo "FAIL: smoke must use --once for per-card fixtures (codex #8)"; exit 1; }; } && grep -c 'node -e' scripts/hardware-smoke.sh | { read n; [ "$n" -ge 1 ] || { echo "FAIL: smoke must use JSON parser, not grep (codex LOW)"; exit 1; }; } && grep -c 'brltty\|dialout' packages/sportident/README.md | { read n; [ "$n" -ge 2 ] || { echo "FAIL: README missing brltty/dialout notes"; exit 1; }; } && grep -q 'hardware-smoke.sh\|hardware smoke' packages/sportident/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/hardware-smoke.sh` is executable (`test -x scripts/hardware-smoke.sh` succeeds)
    - `bash -n scripts/hardware-smoke.sh` exits 0 (syntactic validity)
    - Script contains `set -euo pipefail` (greppable: `grep -c 'set -euo pipefail' scripts/hardware-smoke.sh` >= 1)
    - Script references all 4 card types (si5/SI5, si9/SI9, si10/SI10, siac/SIAC) inside its capture loop
    - Script invokes fartola-readout with BOTH `--record` AND `--once` flags (codex review #8 — per-card fixtures natively, no post-hoc splitting)
    - Script uses `node -e` (or equivalent JSON parser) for NDJSON assertions, NOT `grep -c '"event":"card_inserted"...'` (codex review LOW)
    - Script contains the preflight checks for `/dev/ttyUSB0`, dialout group, Node version, and dist build
    - Script has a SIGINT/ERR trap that exits on assertion failure
    - `packages/sportident/README.md` contains sections "Preflight", "Linux gotchas", "Run", "--record / --replay" (or equivalent prose mentioning the directional transcript format), and "Tag"
    - README mentions both `brltty` AND `dialout` (Landmines #1 + #2)
    - README contains the literal command `./scripts/hardware-smoke.sh`
    - README explicitly documents the directional transcript format (`out <hex>` / `in <hex>` lines)
  </acceptance_criteria>
  <done>Smoke script ready to run; README runbook complete. Smoke has NOT yet been executed against real hardware — that is the next checkpoint (Task 3). Per-card fixtures will be produced natively via the 4 `--record --once` invocations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator runs hardware-smoke.sh against the bench and confirms all 4 card types round-trip</name>
  <what-built>
    `scripts/hardware-smoke.sh` is implemented and tested in dry-run. The bin builds. CI is green on Plans 01-05. The script is poised to run against Jonas's bench hardware (SPORTident CP2102 on /dev/ttyUSB0, cards SI5/SI9/SI10/SIAC Air+) and produce 4 captured fixture pairs via 4 separate `--record --once` invocations (codex review #8).
  </what-built>
  <how-to-verify>
    1. **Bench prep** (Jonas):
       - Plug in the SPORTident BSM7/8-USB reader (serial 593656). `ls -l /dev/ttyUSB0` should show `crw-rw---- 1 root dialout`.
       - Have SI5, SI9, SI10, SIAC cards within reach.
       - `pnpm install --frozen-lockfile && pnpm --filter @fartola/sportident exec tsup` to build the dist.
    2. **Run the smoke** in the repo root:
       - `./scripts/hardware-smoke.sh`
    3. **Per prompt** (the script will guide you through SI5, then SI9, then SI10, then SIAC):
       - Insert the named card type into the reader slot.
       - Press Enter — the script invokes `pnpm exec fartola-readout --record fixtures/jonas/<card>-jonas-001 --once`. The bin exits after one cardRead.
       - **Check the printed line**: `"  -> Detected SI5 card_number=<N>. Verify against printed label."` — confirm `<N>` matches the printed label on your physical card. If it doesn't match, abort (Ctrl-C) and capture the stderr.log for debugging.
       - Remove the card when prompted; press Enter to continue.
    4. **Expected final line**: `"Smoke passed: 4 cards round-tripped..."` and exit code 0.
    5. **Verify fixtures**:
       - `ls packages/sportident/tests/fixtures/jonas/{si5,si9,si10,siac}-jonas-001.{bytes.hex,expected.json}` — all 8 files exist.
       - Inspect one bytes.hex (e.g. `head -10 packages/sportident/tests/fixtures/jonas/si5-jonas-001.bytes.hex`) — confirm directional transcript format (alternating `out <hex>` / `in <hex>` lines after the comment header).
       - `pnpm --filter @fartola/sportident exec node -e "import {replayFixture} from './src/bin/replay.ts'; for (const t of ['si5','si9','si10','siac']) { const r = await replayFixture(\`packages/sportident/tests/fixtures/jonas/\${t}-jonas-001\`); if (!r.matches) { console.error(t, r.diff); process.exit(1); } } console.log('all 4 fixtures replay match');"` — exits 0 with the success line.
    6. **Commit the fixtures**: stage `packages/sportident/tests/fixtures/jonas/` and create a `test(00): capture jonas hardware fixtures` commit.
  </how-to-verify>
  <files>scripts/hardware-smoke.sh, packages/sportident/tests/fixtures/jonas/{si5,si9,si10,siac}-jonas-001.{bytes.hex,expected.json}</files>
  <action>Operator-only step. Execute the verification steps in <how-to-verify> verbatim on Jonas's bench hardware (SPORTident CP2102 on /dev/ttyUSB0, cards SI5/SI9/SI10/SIAC). No code changes by Claude — Claude waits for the resume-signal. If a card fails, Claude resumes only after Jonas supplies the failure detail and routes gap-closure via `/gsd-plan-phase --gaps`.</action>
  <verify><automated>ls packages/sportident/tests/fixtures/jonas/si5-jonas-001.bytes.hex packages/sportident/tests/fixtures/jonas/si5-jonas-001.expected.json packages/sportident/tests/fixtures/jonas/si9-jonas-001.bytes.hex packages/sportident/tests/fixtures/jonas/si9-jonas-001.expected.json packages/sportident/tests/fixtures/jonas/si10-jonas-001.bytes.hex packages/sportident/tests/fixtures/jonas/si10-jonas-001.expected.json packages/sportident/tests/fixtures/jonas/siac-jonas-001.bytes.hex packages/sportident/tests/fixtures/jonas/siac-jonas-001.expected.json && head -3 packages/sportident/tests/fixtures/jonas/si5-jonas-001.bytes.hex | grep -E '^(out|in|#) '</automated></verify>
  <done>All 4 card types round-tripped under hardware-smoke.sh (4 separate --record --once invocations); 8 fixture files exist in directional transcript format and are committed; replayFixture matches=true for each of the 4 fixture basenames.</done>
  <resume-signal>Type "approved — fixtures committed" once all 4 cards round-tripped and the fixtures are committed. If a card failed, type "failed — <which card>" and provide the relevant stderr/stdout snippet so the planner can route a gap-closure plan via `/gsd-plan-phase --gaps`.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Tag v0.0.1-handshake (signed annotated tag) and push</name>
  <what-built>
    All Plan 01-06 PRs landed; hardware smoke green; fixtures committed; CI green on the latest commit (`main`).
  </what-built>
  <how-to-verify>
    1. **Confirm CI is green** on the latest `main`:
       - `gh run list --branch main --limit 1` shows the latest run as `completed success`.
    2. **Sanity test against fixtures one more time** (zero hardware):
       - `pnpm --filter @fartola/sportident exec node --test 'packages/sportident/src/**/*.test.ts'` exits 0.
       - `pnpm --filter @fartola/sportident exec tsup` builds clean.
       - `bash scripts/check-mit-attribution.sh` exits 0 (codex review #13).
    3. **Tag**:
       - `git tag -s v0.0.1-handshake -m "Phase 0 hardware-proof: BSM7/8-USB + SI5/SI9/SI10/SIAC NDJSON readout on Linux"` (signed; uses Jonas's GPG key).
       - `git push origin v0.0.1-handshake`.
    4. **Verify the tag**:
       - `git show v0.0.1-handshake` shows the annotation + signature.
       - GitHub release page shows the tag.
    5. **Update STATE.md and ROADMAP.md** (will be done by the post-plan flow):
       - Mark Phase 0 complete in ROADMAP.md (`- [x] **Phase 0: Hardware proof** ...`).
       - Update STATE.md `## Current position` to Phase 1 ready.
  </how-to-verify>
  <files>(git tag — no repo file modifications)</files>
  <action>Operator-only step. Execute the verification steps in <how-to-verify> verbatim. Claude does not run `git tag` itself; the tag is created by Jonas with a GPG-signed annotation. After the tag is pushed, Claude updates ROADMAP.md and STATE.md to reflect Phase 0 complete (separate commit).</action>
  <verify><automated>git rev-parse v0.0.1-handshake && git show v0.0.1-handshake --no-patch --format='%(contents:signature)'</automated></verify>
  <done>v0.0.1-handshake tag exists, signed, pushed to origin; CI green at the tagged commit; ROADMAP.md and STATE.md updated to Phase 1 next.</done>
  <resume-signal>Type "tagged" once `v0.0.1-handshake` is pushed. Phase 0 is then DONE; Jonas can proceed to `/gsd-plan-phase 1`.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                            | Description                                                                                      |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `--record <basename>` CLI arg -> filesystem         | A malicious or fat-fingered basename could write outside cwd or /tmp and overwrite system files. |
| Hardware-smoke.sh -> spawned bin -> physical reader | The smoke trusts physical hardware presence; assertion failures terminate cleanly.               |

## STRIDE Threat Register (trimmed per codex review LOW — only real risks retained)

| Threat ID              | Category                   | Component                                        | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                            |
| ---------------------- | -------------------------- | ------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-00-19 (re-evaluated) | E (Elevation of privilege) | bin --record path traversal                      | mitigate    | record.ts resolves the basename via `path.resolve` and rejects if the result is not under ANY of the configured `allowedRoots` (default cwd; bin and tests add /tmp explicitly). Test case in record.test.ts covers a basename containing `../escape` AND `/etc/passwd` and asserts rejection before any file is opened (codex review #7). |
| T-00-21                | D (DoS)                    | hardware-smoke.sh hangs if a card isn't detected | mitigate    | Smoke is operator-driven (humans press Enter between captures); each `--record --once` exits after one cardRead OR after operator Ctrl-C. SIGINT trap kills the bin on any failure. No infinite-loop risk.                                                                                                                                 |

</threat_model>

<verification>
- After Task 1: cumulative tests green; tsup builds; `--record` (directional transcript) and `--replay` (deterministic playback) modes covered by unit tests including path-traversal AND /tmp-allowed cases.
- After Task 2: smoke script is syntactically valid bash; uses `--record --once` per card; uses `node -e` for NDJSON assertions; README has the runbook.
- After Task 3 (checkpoint): 4 real-hardware fixtures committed in directional transcript format; replayFixture matches for all 4.
- After Task 4 (checkpoint): v0.0.1-handshake tag exists and is pushed; CI green at that commit.
</verification>

<success_criteria>

- D-17/D-18/D-19 fully satisfied: fixture-based unit tests in CI + manual hardware smoke + per-card-type captured fixtures (4 separate --record --once invocations).
- Phase 0 Success Criterion #4 (real SI8/9/10 logs to stdout) verified by the hardware smoke run.
- Phase 0 Success Criterion #5 (SI5 test passes) verified by the hardware smoke run.
- Phase 0 Success Criterion #6 (tagged v0.0.1-handshake) verified by Task 4 checkpoint.
- Plan 06 SUMMARY documents the smoke output + tag commit.
- ROADMAP.md and STATE.md updated to reflect Phase 0 complete.
- Directional transcript format committed to disk for all 4 cards; replay produces byte-equal NDJSON output (deterministic).
  </success_criteria>

<output>
After completion, create `.planning/phases/00-hardware-proof/00-06-SUMMARY.md`:
- Paste the smoke-script output (4 card types round-tripping via 4 --record --once invocations).
- Paste the `git show v0.0.1-handshake` annotation.
- Paste the cumulative test count from `pnpm --filter @fartola/sportident exec node --test 'packages/sportident/src/**/*.test.ts'`.
- Note any deviations from RESEARCH (e.g. a CP2102 wakeup-byte fix-up if Landmine #8 actually bit).
- List the 4 captured fixture pairs with their card_numbers, bytes.hex line counts (transcript length), and sizes.
</output>
</content>
</invoke>
