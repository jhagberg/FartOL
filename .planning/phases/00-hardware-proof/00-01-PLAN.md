---
phase: 00-hardware-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - .nvmrc
  - tsconfig.json
  - .gitignore
  - .npmrc
  - packages/sportident/package.json
  - packages/sportident/tsconfig.json
  - packages/sportident/tsup.config.ts
  - packages/sportident/README.md
  - packages/sportident/LICENSE
  - packages/sportident/NOTICE.md
  - packages/sportident/src/index.ts
  - packages/sportident/src/siProtocol.test.ts
  - packages/sportident/src/output/ndjson.test.ts
  - packages/sportident/src/integration/frameError.test.ts
  - packages/sportident/src/integration/e2e.test.ts
  - packages/sportident/src/SiCard/types/SiCard5.test.ts
  - packages/sportident/src/SiCard/types/SiCard9.test.ts
  - packages/sportident/src/SiCard/types/SiCard10.test.ts
  - packages/sportident/src/SiCard/types/SIAC.test.ts
  - packages/sportident/tests/fixtures/upstream/.gitkeep
  - packages/sportident/tests/fixtures/jonas/.gitkeep
  - packages/sportident/tests/fixtures/synthetic/.gitkeep
  - scripts/hardware-smoke.sh
  - lefthook.yml
  - commitlint.config.cjs
  - eslint.config.js
  - .prettierrc.json
  - .github/workflows/ci.yml
autonomous: true
requirements: []
user_setup:
  - service: pnpm
    why: 'Package manager (D-03). Required to run install/lint/typecheck/test.'
    env_vars: []
    dashboard_config:
      - task: 'Enable corepack so pnpm is auto-provisioned'
        location: 'Local shell: `corepack enable && corepack prepare pnpm@latest --activate`'
must_haves:
  truths:
    - 'pnpm install --frozen-lockfile succeeds on a clean clone'
    - 'pnpm typecheck and pnpm lint pass with the empty/placeholder source tree'
    - 'pnpm test runs node:test against placeholder test files (zero failing assertions; framework wires up)'
    - 'Every required Wave 0 test file from 00-VALIDATION.md §Wave 0 Requirements exists at the documented path'
    - 'Pre-commit hook (lefthook) runs lint+format on staged files; commitlint enforces Conventional Commits on commit-msg'
    - '.github/workflows/ci.yml runs pnpm install --frozen-lockfile && pnpm lint && pnpm typecheck && pnpm test on Ubuntu Linux runner with a pinned pnpm version (NOT a `${{ packageManager.split }}` expression — codex review #9)'
    - 'pnpm-workspace.yaml is an INTENTIONAL deviation from D-01 (which deferred workspaces to Phase 1); deviation rationale is captured in the workspace file and 00-01-SUMMARY.md (codex review #10)'
    - 'tsup.config.ts sets outExtension explicitly so bin/exports paths in package.json resolve to real .mjs/.cjs files on disk (codex review #12; full implementation lands in Plan 05, this plan creates a config skeleton with the outExtension stub in place)'
  artifacts:
    - path: 'packages/sportident/package.json'
      provides: 'Standalone-shaped package metadata: name @fartola/sportident, version 0.0.0, license MIT, bin fartola-readout, ESM+CJS exports, engines.node >= 22'
      contains: '"name": "@fartola/sportident"'
    - path: 'packages/sportident/tsconfig.json'
      provides: 'strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes + erasableSyntaxOnly + verbatimModuleSyntax (per D-02 and RESEARCH §Validation Architecture)'
      contains: '"erasableSyntaxOnly": true'
    - path: 'packages/sportident/tsup.config.ts'
      provides: 'Dual ESM+CJS output with .d.ts, target node22 (D-05), explicit outExtension producing .mjs/.cjs (codex review #12)'
    - path: 'packages/sportident/NOTICE.md'
      provides: 'Per-package upstream attribution doc listing allestuetsmerweh/sportident.js with MIT license URL (D-11)'
      contains: 'allestuetsmerweh/sportident.js'
    - path: 'packages/sportident/LICENSE'
      provides: 'MIT license for the @fartola/sportident sub-package (D-04)'
      contains: 'MIT License'
    - path: '.github/workflows/ci.yml'
      provides: 'CI pipeline: install + lint + typecheck + test, no hardware (D-07, D-20); uses Corepack with an explicitly-pinned pnpm version (codex review #9)'
      contains: 'pnpm install --frozen-lockfile'
    - path: 'lefthook.yml'
      provides: 'Pre-commit hook config running lint + format on staged files (D-08)'
    - path: 'commitlint.config.cjs'
      provides: 'Extends @commitlint/config-conventional (D-08)'
    - path: 'scripts/hardware-smoke.sh'
      provides: 'Operator-driven hardware smoke shell skeleton (filled in Plan 06; created empty/scaffolded here so Wave 0 path exists per 00-VALIDATION.md)'
    - path: 'pnpm-workspace.yaml'
      provides: "Workspace anchor (`packages: ['packages/*']`). INTENTIONAL deviation from D-01 — see action and SUMMARY (codex review #10)."
  key_links:
    - from: 'package.json (root scripts)'
      to: 'pnpm lint, pnpm typecheck, pnpm test'
      via: 'scripts entries that delegate to packages/sportident'
      pattern: '"test":.*node --test'
    - from: '.github/workflows/ci.yml'
      to: 'pnpm install/lint/typecheck/test'
      via: 'GitHub Actions ubuntu-latest job with pinned pnpm via Corepack'
      pattern: 'pnpm test'
    - from: 'lefthook.yml'
      to: 'pnpm lint, pnpm format'
      via: 'pre-commit hook'
      pattern: 'pre-commit'
---

<objective>
Wave 0 scaffold: stand up `packages/sportident/` as a standalone-shaped, MIT-licensed sub-package inside the fartOLa repo, with the full toolchain wired up (TypeScript strict, tsup build with explicit `outExtension` per codex review #12, node:test, lefthook + commitlint, GitHub Actions CI with explicitly-pinned pnpm per codex review #9) AND every Wave 0 test/fixture file from `00-VALIDATION.md` already present as empty placeholders. Document the intentional deviation from D-01 (workspace file anchored now, not deferred to Phase 1) per codex review #10. This plan creates the runway; every later plan in Phase 0 lands code into files this plan created.

Purpose: Without scaffolding first we cannot run `pnpm test` after each task, which 00-VALIDATION.md mandates. Without empty placeholder test files in their final paths, Wave 1-5 tasks would create them ad-hoc and risk drifting from the validation strategy. Without the CI workflow, post-task verification has no source of truth. Without explicit pnpm pinning, the CI workflow would reference an undefined variable (codex review #9). Without `outExtension` set, Plan 05's dist artifacts would default to `.js` and Plan 01's `bin: ./dist/bin/fartola-readout.cjs` would not exist on disk (codex review #12).

Output: A repo that on `git clone && pnpm install && pnpm lint && pnpm typecheck && pnpm test` exits 0 with zero tests defined yet (or trivial placeholder assertions). Every locked decision D-01..D-08, D-11, D-13/D-14/D-15/D-16 has its scaffolding artifact in place — with D-01's deviation explicitly documented. Wave 0 sign-off section of 00-VALIDATION.md is satisfied except for `nyquist_compliant: true` flip (planner sets that after this plan lands).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-hardware-proof/00-CONTEXT.md
@.planning/phases/00-hardware-proof/00-RESEARCH.md
@.planning/phases/00-hardware-proof/00-VALIDATION.md
@.planning/adr/0005-sportident-code-isolated-mit.md
@.planning/adr/0006-tech-stack.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root scaffold + pnpm workspace anchor (intentional D-01 deviation documented) + tsconfig + lint/format toolchain</name>
  <files>package.json, pnpm-lock.yaml, .nvmrc, tsconfig.json, .gitignore, .npmrc, eslint.config.js, .prettierrc.json, lefthook.yml, commitlint.config.cjs, pnpm-workspace.yaml</files>
  <read_first>
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-CONTEXT.md (lock D-01..D-08; CODEX REVIEW #10: D-01 says "defer pnpm workspaces to Phase 1" — this plan DEVIATES by anchoring the workspace now)
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-RESEARCH.md §"Environment Availability" + §"Validation Architecture" + §"State of the Art"
    - /home/jonas/src/fartOLa/.planning/adr/0006-tech-stack.md
    - /home/jonas/src/fartOLa/.gitignore (does not exist yet — create from scratch)
  </read_first>
  <action>
    Create the repo-root scaffold. **Intentional D-01 deviation (codex review #10):** D-01 says "defer pnpm workspaces to Phase 1", but anchoring the workspace file NOW costs ~5 lines and avoids restructuring `package.json` exports + `paths` resolution when Phase 1 adds a sibling package (`@fartola/ingester` or `@fartola/db`). This deviation MUST be documented in the workspace file header AND in 00-01-SUMMARY.md.

    Per D-03: pnpm; allow corepack. Per D-06: node:test, no extra deps. Per D-08: lefthook + commitlint. Concrete values:

    - `.nvmrc`: literal `22` (pin to Node 22 LTS — discretion area, locked here).
    - `package.json` (root): private=true, packageManager=pnpm@latest pinned to a real published version (use the version returned by `pnpm --version` at scaffold time, currently 10.30.3 per RESEARCH §Environment Availability — e.g. `"packageManager": "pnpm@10.30.3"`), engines.node=">=22.18.0" (so TS-stripping is unflagged), scripts:
      - `lint`: `pnpm -r --if-present run lint && pnpm run lint:attribution` (lint:attribution chain added by Plan 05 when scripts/check-mit-attribution.sh lands; pre-create the script entry now to avoid a follow-up edit — point it at a placeholder shell script if needed)
      - `lint:attribution`: `bash scripts/check-mit-attribution.sh || echo 'check-mit-attribution.sh not yet present (Plan 05 lands it); skipping'` — keeps `pnpm lint` exit 0 in Wave 0 while signaling the audit hook
      - `format`: `prettier --write '**/*.{ts,js,cjs,mjs,md,yml,yaml,json}'`
      - `typecheck`: `pnpm -r --if-present run typecheck`
      - `test`: `pnpm -r --if-present run test`
      - `prepare`: `lefthook install`
      devDependencies: typescript, tsup, eslint, @eslint/js, typescript-eslint, prettier, lefthook, @commitlint/cli, @commitlint/config-conventional.
    - `pnpm-workspace.yaml` (root): `packages: ['packages/*']`. **MUST contain a YAML comment at the top documenting the D-01 deviation (codex review #10)**:
      ```yaml
      # INTENTIONAL DEVIATION FROM D-01 (codex review #10).
      # D-01 says: "single packages/sportident/ for now; defer pnpm workspaces to Phase 1".
      # We anchor the workspace file now (5 lines) so Phase 1 can add a sibling
      # package (@fartola/ingester) without restructuring package.json `exports`
      # / `paths` resolution. Cost: 5 lines today. Benefit: no Phase 1 restructure.
      # See .planning/phases/00-hardware-proof/00-01-SUMMARY.md for the full rationale.
      packages:
        - 'packages/*'
      ```
    - `tsconfig.json` (root): base config that packages extend. Per RESEARCH §Validation Architecture recommended block — target esnext, module nodenext, moduleResolution nodenext, strict, noUncheckedIndexedAccess, exactOptionalPropertyTypes, erasableSyntaxOnly, verbatimModuleSyntax, skipLibCheck, noEmit. No `paths` (Node TS stripper ignores them). No `include` at root (packages own includes).
    - `eslint.config.js` (flat config, ESLint 9 — per RESEARCH §State of the Art): import `@eslint/js` and `typescript-eslint`. Apply `tseslint.configs.recommended` to `**/*.ts`. Ignore `dist/`, `node_modules/`, `**/*.test.ts` snapshot regeneration outputs. No custom rules beyond recommended in this scaffold.
    - `.prettierrc.json`: single-quote, semi=true, trailingComma=es5, printWidth=100 (match upstream sportident.js style to ease per-file porting).
    - `lefthook.yml`: pre-commit running `pnpm exec prettier --check {staged_files}` and `pnpm exec eslint {staged_files}`; commit-msg running `pnpm exec commitlint --edit {1}`. Stage globs limited to `*.ts,*.js,*.cjs,*.mjs,*.json,*.md,*.yml,*.yaml`.
    - `commitlint.config.cjs`: `module.exports = {extends: ['@commitlint/config-conventional']};` (discretion area locked: extend conventional, no custom rules — keeps with existing repo commits per RESEARCH §"Project Constraints").
    - `.gitignore`: node_modules/, dist/, coverage/, .DS_Store, *.log, .env*, .vscode/settings.json (allow shared .vscode/extensions.json), tmp/.
    - `.npmrc`: `auto-install-peers=true`, `strict-peer-dependencies=false`.
    - Run `pnpm install` so `pnpm-lock.yaml` is generated and committed.

    NEVER fold the sub-package's `package.json` into the root; D-01 says single package now, but standalone-shaped (D-04). Root is the workspace anchor only. NEVER omit the deviation-rationale comment in pnpm-workspace.yaml (codex review #10).

  </action>
  <verify>
    <automated>cd /home/jonas/src/fartOLa && pnpm install --frozen-lockfile && pnpm exec tsc --version && pnpm exec eslint --version && pnpm exec prettier --version && pnpm exec lefthook version && pnpm exec commitlint --version && grep -c 'INTENTIONAL DEVIATION FROM D-01' pnpm-workspace.yaml | { read n; [ "$n" -ge 1 ] || { echo "FAIL: D-01 deviation not documented in pnpm-workspace.yaml (codex review #10)"; exit 1; }; }</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists with `"private": true`, `"packageManager"` set to a `pnpm@<version>` string (e.g. `pnpm@10.30.3`), `"engines": {"node": ">=22.18.0"}`, and root scripts `lint`/`lint:attribution`/`format`/`typecheck`/`test`/`prepare` defined exactly as listed in `<action>`
    - `pnpm-workspace.yaml` contains `packages:` with the single entry `'packages/*'` AND contains the literal substring `INTENTIONAL DEVIATION FROM D-01` (codex review #10)
    - `tsconfig.json` contains all 10 compilerOptions from RESEARCH.md §Validation Architecture, including literal `"erasableSyntaxOnly": true` and `"verbatimModuleSyntax": true`
    - `.nvmrc` contains literal `22` (no other text)
    - `lefthook.yml` declares both `pre-commit` (lint + format) and `commit-msg` (commitlint) groups
    - `commitlint.config.cjs` source contains the substring `@commitlint/config-conventional`
    - `eslint.config.js` is a flat config (default export is an array) and includes `typescript-eslint` recommended
    - `pnpm install --frozen-lockfile` exits 0 (proves devDependencies resolve and lockfile is consistent)
    - `pnpm exec tsc --version` prints a 5.x version
    - `pnpm exec lefthook version` prints a non-error version string
    - `.gitignore` contains `node_modules/`, `dist/`, and `.env*`
    - root `package.json` `packageManager` field uses a literal pinned version (NOT `pnpm@latest`) — codex review #9 prerequisite for CI step
  </acceptance_criteria>
  <done>Root toolchain installs, all CLIs respond, lockfile is committed, pnpm-workspace.yaml carries the deviation rationale comment, all 12 acceptance items above pass.</done>
</task>

<task type="auto">
  <name>Task 2: packages/sportident/ skeleton — package.json + tsconfig + tsup (with explicit outExtension stub) + LICENSE + NOTICE + README + index.ts</name>
  <files>packages/sportident/package.json, packages/sportident/tsconfig.json, packages/sportident/tsup.config.ts, packages/sportident/LICENSE, packages/sportident/NOTICE.md, packages/sportident/README.md, packages/sportident/src/index.ts</files>
  <read_first>
    - /home/jonas/src/fartOLa/LICENSE (root AGPL-3.0 — the SUB-package uses MIT, see CONTEXT D-04/D-11 and ADR-0005)
    - /home/jonas/src/fartOLa/.planning/adr/0005-sportident-code-isolated-mit.md
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-CONTEXT.md (D-04 standalone-shaped, D-05 tsup dual ESM+CJS + .d.ts, D-11 NOTICE.md and per-file MIT header, D-16 bin name)
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-RESEARCH.md §"sportident.js Port Surface" (table of files to port)
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-REVIEWS.md (codex review #12 — tsup outExtension must be explicit so bin field paths resolve)
    - external-ref: https://github.com/allestuetsmerweh/sportident.js/blob/master/LICENSE (MIT license text)
    - external-ref: tsup.dev/docs/options/output-extension (outExtension option signature; this plan creates the stub, Plan 05 finalizes)
    - external-ref: https://github.com/allestuetsmerweh/sportident.js/blob/master/packages/sportident/package.json (for shape reference only; copy-attribute don't fork verbatim)
  </read_first>
  <action>
    Create `packages/sportident/` as a standalone-shaped sub-package (D-04). Concrete values:

    - `packages/sportident/package.json`: name `@fartola/sportident`, version `0.0.0`, license `MIT`, type `module`, description "Node.js library for reading SPORTident orienteering cards via BSM7/8-USB", repository pointing to the fartOLa repo with `directory: packages/sportident`, engines.node `>=22.18.0`. Exports map: `"."` -> `{import: "./dist/index.mjs", require: "./dist/index.cjs", types: "./dist/index.d.ts"}`. Bin: `"fartola-readout": "./dist/bin/fartola-readout.cjs"` (filled in by Plan 05; declare path now so npm/pnpm wires PATH on dev clone — D-16; codex review #12 ensures the `.cjs` extension matches tsup output). Files: `dist/`, `LICENSE`, `NOTICE.md`, `README.md`. Scripts: `build`: `tsup`, `dev:readout`: `node src/bin/fartola-readout.ts` (Node 22.19+ strips TS without tsx — RESEARCH §State of the Art). `test`: `node --test --test-reporter=spec 'src/**/*.test.ts'`. `test:watch`: same with `--watch`. `lint`: `eslint src`. `typecheck`: `tsc --noEmit`. Dependencies: `serialport@^13.0.0` (per RESEARCH §serialport version). devDependencies: inherits tsup/typescript via root.
    - `packages/sportident/tsconfig.json`: extends `../../tsconfig.json`, `include: ['src/**/*.ts', 'tests/**/*.ts']`, `compilerOptions.rootDir: "."`.
    - `packages/sportident/tsup.config.ts`: defineConfig — entry `['src/index.ts', 'src/bin/fartola-readout.ts']`, format `['esm', 'cjs']`, dts true, sourcemap true, clean true, target `node22`, splitting false, outDir `dist`. **AND set `outExtension({format}) { return { js: format === 'esm' ? '.mjs' : '.cjs' }; }`** (codex review #12 — this is the stub; Plan 05 may extend with bin-specific shebang handling but the extension contract is locked here so package.json `bin: ./dist/bin/fartola-readout.cjs` is satisfied from day one). Add a comment: `// outExtension (codex review #12): explicit .mjs/.cjs so package.json bin and exports paths resolve to actual files on disk.`
    - `packages/sportident/LICENSE`: full MIT license text with copyright `Copyright (c) 2026 Jonas Hagberg and the fartOLa contributors`.
    - `packages/sportident/NOTICE.md`: per D-11, list upstream attributions. Concrete sections: header "Third-party software", subsection "allestuetsmerweh/sportident.js" with link `https://github.com/allestuetsmerweh/sportident.js`, license `MIT`, scope "Protocol primitives (siProtocol, constants, storage primitives), card decoders (BaseSiCard, ModernSiCard, SiCard5/9/10/SIAC + their examples), station handshake choreography (BaseSiStation, SiMainStation, SiSendTask). See per-file MIT NOTICE headers in `src/` for line-level attribution. See `scripts/check-mit-attribution.sh` (Plan 05) for the automated audit." Subsection "per-magnusson/sportident-python" — reference only (no code copied), GPL — included for CRC cross-verification credit.
    - `packages/sportident/README.md`: terse — what the package is (NDJSON-emitting reader for SPORTident BSM7/8-USB on Linux), Phase 0 scope (SI5/SI9/SI10/SIAC, Linux only), how to run (`pnpm dev:readout` and `pnpm exec fartola-readout`, D-16), env var `FARTOLA_DEVICE` (default `/dev/ttyUSB0`), one-line attribution pointer to NOTICE.md. ≤ 80 lines.
    - `packages/sportident/src/index.ts`: empty stub — `export {};` plus a `/* @public API surface defined in later waves */` comment. Plan 05 will fill this in with the exports listed in RESEARCH §"Open Questions for Planner" #6 (Recommendation): SerialTransport, SiMainStation, parseAll/render/CRC16, the four card classes, NDJSON event TypeScript types.

  </action>
  <verify>
    <automated>cd /home/jonas/src/fartOLa && pnpm install && pnpm --filter @fartola/sportident exec tsc --noEmit && test -f packages/sportident/LICENSE && test -f packages/sportident/NOTICE.md && grep -q '@commitlint/config-conventional' commitlint.config.cjs && node -e "const p=require('./packages/sportident/package.json'); if(p.name!=='@fartola/sportident')process.exit(1); if(p.license!=='MIT')process.exit(2); if(!p.bin?.['fartola-readout'])process.exit(3); if(!p.exports?.['.'])process.exit(4);" && grep -c 'outExtension' packages/sportident/tsup.config.ts | { read n; [ "$n" -ge 1 ] || { echo "FAIL: tsup.config.ts missing explicit outExtension (codex review #12)"; exit 1; }; }</automated>
  </verify>
  <acceptance_criteria>
    - `packages/sportident/package.json` parses as JSON, `name === '@fartola/sportident'`, `license === 'MIT'`, `type === 'module'`, `engines.node === '>=22.18.0'`, `bin['fartola-readout'] === './dist/bin/fartola-readout.cjs'`, `exports['.']` has `import`/`require`/`types` keys, `dependencies.serialport` matches `^13.`
    - `packages/sportident/tsconfig.json` extends `../../tsconfig.json` and includes both `src/**/*.ts` and `tests/**/*.ts`
    - `packages/sportident/tsup.config.ts` exports defineConfig with `format: ['esm', 'cjs']`, `dts: true`, `target: 'node22'`, AND contains the literal substring `outExtension` (codex review #12)
    - `packages/sportident/LICENSE` first non-blank line starts with `MIT License`
    - `packages/sportident/NOTICE.md` contains the literal substring `allestuetsmerweh/sportident.js` and the substring `MIT`
    - `packages/sportident/src/index.ts` exists and is syntactically valid TypeScript (typecheck passes)
    - `pnpm --filter @fartola/sportident exec tsc --noEmit` exits 0 with the empty index.ts in place
  </acceptance_criteria>
  <done>Sub-package skeleton in place; typecheck passes on empty source; LICENSE + NOTICE + README + tsup config (with outExtension stub per codex review #12) + package.json all match acceptance criteria above.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 test placeholders + fixture dirs + CI workflow (pinned pnpm via Corepack) + smoke-script stub</name>
  <files>packages/sportident/src/siProtocol.test.ts, packages/sportident/src/output/ndjson.test.ts, packages/sportident/src/integration/frameError.test.ts, packages/sportident/src/integration/e2e.test.ts, packages/sportident/src/SiCard/types/SiCard5.test.ts, packages/sportident/src/SiCard/types/SiCard9.test.ts, packages/sportident/src/SiCard/types/SiCard10.test.ts, packages/sportident/src/SiCard/types/SIAC.test.ts, packages/sportident/tests/fixtures/upstream/.gitkeep, packages/sportident/tests/fixtures/jonas/.gitkeep, packages/sportident/tests/fixtures/synthetic/.gitkeep, scripts/hardware-smoke.sh, .github/workflows/ci.yml</files>
  <read_first>
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-VALIDATION.md §"Wave 0 Requirements" (exact list of files to create)
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-RESEARCH.md §"Validation Architecture" + §"Per-Task Verification Map" (CI command, framework, reporter)
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-CONTEXT.md (D-07 GitHub Actions lean Linux; D-20 CI scope; D-19 smoke script)
    - /home/jonas/src/fartOLa/.planning/phases/00-hardware-proof/00-REVIEWS.md (codex review #9 — `${{ packageManager.split('@')[1] }}` is NOT a valid GitHub Actions expression; pin pnpm version explicitly or use Corepack)
    - external-ref: https://docs.github.com/en/actions/learn-github-actions/contexts (no `packageManager` context exists; only `github`, `env`, `vars`, `secrets`, `inputs`, `job`, `jobs`, `steps`, `runner`, `matrix`, `needs`)
    - external-ref: https://github.com/pnpm/action-setup (version input accepts a literal version OR `package.json` to auto-detect)
  </read_first>
  <action>
    Create every Wave 0 file that 00-VALIDATION.md §"Wave 0 Requirements" lists. Each test file is a node:test module with a single skipped placeholder test so `pnpm test` exits 0 and the file path exists for later waves to fill in. Concrete values:

    - For each placeholder `*.test.ts` (8 files in `src/`): `import {test} from 'node:test'; import assert from 'node:assert'; test.skip('Wave 0 placeholder — implemented in Wave N', () => { assert.ok(true); });` — replace `Wave N` with the actual wave (1 for siProtocol/frameError, 2 for SiCard5/9/10/SIAC, 4 for ndjson/e2e) so the placeholder rationalizes itself. Each file MUST include a top-of-file comment `// Wave 0 scaffold (00-VALIDATION.md). Implemented in PLAN 0X.` (X = plan number that lands real assertions; planner sets: siProtocol -> 02, frameError -> 02, SiCard5/9/10/SIAC -> 03, ndjson -> 05, e2e -> 05).
    - `tests/fixtures/upstream/.gitkeep`, `tests/fixtures/jonas/.gitkeep`, `tests/fixtures/synthetic/.gitkeep`: empty files so git tracks the directories. Plan 03/06 fills `upstream/` and `jonas/`; Plan 02/05 fills `synthetic/`.
    - `scripts/hardware-smoke.sh`: shebang `#!/usr/bin/env bash`, `set -euo pipefail`, an `echo "TODO: implemented in Plan 06 (hardware smoke). Exit 2 = not-yet-implemented."` then `exit 2`. `chmod +x` it (commit the executable bit). This satisfies the Wave 0 requirement that the path exists and is runnable; Plan 06 replaces the body.

    - `.github/workflows/ci.yml`: triggers on `pull_request` and `push: branches: [main]`. One job `ci` on `ubuntu-latest`. Concrete YAML (codex review #9 — use Corepack with the pnpm version that matches root `package.json` packageManager; NO `${{ packageManager.split('@')[1] }}` because GHA does not expose `packageManager` as a context variable):

      ```yaml
      name: ci
      on:
        pull_request:
        push:
          branches: [main]
      permissions:
        contents: read
      jobs:
        ci:
          runs-on: ubuntu-latest
          timeout-minutes: 10
          steps:
            - uses: actions/checkout@v4
            # Codex review #9: pnpm pinning. Corepack reads the root package.json `packageManager` field
            # (set to `pnpm@<pinned-version>` in Task 1). No GHA context variable substitution required.
            - name: Enable Corepack (pins pnpm from package.json `packageManager` field)
              run: corepack enable && corepack prepare --activate
            - uses: actions/setup-node@v4
              with:
                node-version-file: .nvmrc
                cache: pnpm
            - run: pnpm install --frozen-lockfile
            - run: pnpm lint
            - run: pnpm typecheck
            - run: pnpm test
      ```

      Alternative if Corepack is flaky in CI: use `pnpm/action-setup@v4` with an explicit literal version that matches root package.json packageManager:

      ```yaml
            - uses: pnpm/action-setup@v4
              with:
                version: 10.30.3   # MUST match package.json packageManager pnpm@<version>
      ```

      Pick ONE strategy and document the choice. Default to Corepack — it auto-derives the version from package.json so the two never drift; if Corepack fails on the runner, switch to the explicit-version approach and update both root package.json AND ci.yml in a single commit. NEVER use `${{ packageManager.split('@')[1] }}` — that expression evaluates to empty in GHA and silently breaks the setup step (codex review #9).

      NO hardware-related steps; NO `/dev/ttyUSB0` mention. NO matrix (Linux-only Phase 0 per CONTEXT scope).

    NEVER use `pnpm dlx` in CI — D-07 says lean, deterministic install via lockfile. NEVER add `--no-frozen-lockfile`. NEVER reference a non-existent GHA context like `packageManager` (codex review #9).

  </action>
  <verify>
    <automated>cd /home/jonas/src/fartOLa && pnpm install --frozen-lockfile && pnpm test 2>&1 | tee /tmp/wave0.log && grep -c '^# tests ' /tmp/wave0.log && test -x scripts/hardware-smoke.sh && test -f .github/workflows/ci.yml && for f in packages/sportident/src/siProtocol.test.ts packages/sportident/src/output/ndjson.test.ts packages/sportident/src/integration/frameError.test.ts packages/sportident/src/integration/e2e.test.ts packages/sportident/src/SiCard/types/SiCard5.test.ts packages/sportident/src/SiCard/types/SiCard9.test.ts packages/sportident/src/SiCard/types/SiCard10.test.ts packages/sportident/src/SiCard/types/SIAC.test.ts; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done && for d in packages/sportident/tests/fixtures/upstream packages/sportident/tests/fixtures/jonas packages/sportident/tests/fixtures/synthetic; do test -d "$d" || { echo "MISSING DIR: $d"; exit 1; }; done && ! grep -E '\\$\\{\\{ *packageManager' .github/workflows/ci.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 test files from 00-VALIDATION.md §"Wave 0 Requirements" exist at their exact paths
    - All 3 fixture directories exist and contain `.gitkeep`
    - `scripts/hardware-smoke.sh` is executable (`stat -c '%A' scripts/hardware-smoke.sh` reports an `x` bit)
    - `pnpm test` exits 0 with all 8 placeholder tests reported as skipped (count: `grep -c 'ok .* # SKIP' /tmp/wave0.log` returns 8 OR node:test reporter equivalent shows 8 skipped)
    - `.github/workflows/ci.yml` contains the four literal commands `pnpm install --frozen-lockfile`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (one per line, in that order)
    - `.github/workflows/ci.yml` `runs-on:` value is `ubuntu-latest` and there is no matrix block
    - `.github/workflows/ci.yml` uses EITHER `corepack enable` + `corepack prepare --activate` OR `pnpm/action-setup@v4` with a literal `version:` value matching root `package.json` packageManager — verified by either pattern via grep
    - `.github/workflows/ci.yml` does NOT contain the broken `${{ packageManager.split('@')[1] }}` expression (codex review #9; greppable: `grep -c 'packageManager.split' .github/workflows/ci.yml` returns 0)
    - `.github/workflows/ci.yml` parses as valid YAML (verified by `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"`)
    - Each placeholder test file starts with the comment `// Wave 0 scaffold (00-VALIDATION.md). Implemented in PLAN 0X.` where X matches the wave-owner plan number declared in `<action>` (greppable: `grep -l 'Implemented in PLAN 0' packages/sportident/src/**/*.test.ts | wc -l` returns 8)
  </acceptance_criteria>
  <done>All Wave 0 scaffolding files exist; `pnpm test` succeeds with placeholders; CI workflow runs the four-step pipeline on ubuntu-latest with EXPLICITLY-PINNED pnpm via Corepack or pnpm/action-setup (codex review #9 — no broken expression); hardware smoke script is executable and prints its TODO message.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries (trimmed per codex review LOW)

| Boundary                       | Description                                                                                                                                                                                                                                              |
| ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm install` -> npm registry | We pull tsup, eslint, lefthook, commitlint, serialport, typescript at install time; lockfile pins versions but a compromised registry could ship a malicious tarball. Mitigation: `pnpm install --frozen-lockfile` everywhere + commit `pnpm-lock.yaml`. |
| Pre-commit hook -> host shell  | lefthook runs on every commit; a malicious hook config could exfiltrate. Hook config is committed and reviewed — mitigation accepted by inspection.                                                                                                      |

## STRIDE Threat Register (trimmed per codex review LOW — only real risks retained)

| Threat ID | Category                   | Component                            | Disposition | Mitigation Plan                                                                                         |
| --------- | -------------------------- | ------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------- |
| T-00-01   | T (Tampering)              | npm dependency tarballs              | mitigate    | Use `pnpm install --frozen-lockfile` in CI and dev (root scripts and CI step); commit `pnpm-lock.yaml`. |
| T-00-03   | I (Information disclosure) | `.env*` files accidentally committed | mitigate    | `.gitignore` blocks `.env*`; no Phase 0 secrets exist yet.                                              |
| T-00-04   | E (Elevation of privilege) | CI workflow secret leak              | accept      | Phase 0 CI uses no secrets; `permissions: contents: read` set on the workflow job.                      |

</threat_model>

<verification>
- `pnpm install --frozen-lockfile && pnpm lint && pnpm typecheck && pnpm test` exits 0 from a clean clone.
- `git diff HEAD~1 --name-only` shows the full file list from `files_modified` was created.
- `.github/workflows/ci.yml` is valid YAML (parses with `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"`).
- `.github/workflows/ci.yml` does NOT reference the broken `${{ packageManager.split('@')[1] }}` expression — uses Corepack or pinned `pnpm/action-setup@v4 version:` (codex review #9).
- `pnpm-workspace.yaml` contains the `INTENTIONAL DEVIATION FROM D-01` comment (codex review #10).
- `packages/sportident/tsup.config.ts` contains explicit `outExtension` (codex review #12 — stub here, Plan 05 may extend).
- Every file listed in `files_modified` exists on disk after the plan completes.
</verification>

<success_criteria>

- Plan completes when: every Wave 0 file in 00-VALIDATION.md exists; `pnpm install --frozen-lockfile && pnpm lint && pnpm typecheck && pnpm test` exits 0; CI workflow YAML is valid AND pnpm version is pinned via Corepack or explicit setup (codex review #9); sub-package has LICENSE + NOTICE.md + README; lefthook + commitlint + ESLint + Prettier + tsup (with outExtension) + tsconfig are all wired and runnable; D-01 deviation documented in pnpm-workspace.yaml (codex review #10).
- Plan does NOT need to land any code from upstream sportident.js yet — that's Plan 02.
  </success_criteria>

<output>
After completion, create `.planning/phases/00-hardware-proof/00-01-SUMMARY.md` with: scaffolded files actually written, pnpm + Node + serialport versions captured at install time, **a dedicated "D-01 deviation" section explaining the workspace anchoring decision (codex review #10)**, any other deviations from the planned config (and why), the exact `pnpm install --frozen-lockfile && pnpm lint && pnpm typecheck && pnpm test` output (paste the final two lines), and the CI pnpm-pinning strategy chosen (Corepack vs pnpm/action-setup version literal).
</output>
</content>
</invoke>
