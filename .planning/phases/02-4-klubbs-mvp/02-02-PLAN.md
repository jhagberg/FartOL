---
phase: 02-4-klubbs-mvp
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - apps/edge/src/eventor/lookup.ts
  - apps/edge/src/eventor/lookup.test.ts
  - apps/edge/src/routes/eventor.ts
  - apps/edge/src/routes/eventor.test.ts
  - apps/edge/src/routes/competitors.ts
  - apps/edge/src/routes/competitors.test.ts
  - apps/edge/src/server.ts
  - packages/shared-types/src/dtos.ts
  - apps/web/src/lib/api/client.ts
  - apps/web/src/lib/components/EventorAutocomplete.svelte
  - apps/web/src/lib/screens/WalkupModal.svelte
  - apps/web/src/lib/i18n/sv.json
  - apps/web/src/lib/i18n/en.json
  - apps/web/src/lib/stores/eventorStatus.svelte.ts
  - apps/web/src/lib/components/TweaksPanel.svelte
  - apps/edge/src/routes/admin.ts
  - apps/web/e2e/walkup-eventor.spec.ts
autonomous: true
tags: [walkup, eventor, autocomplete, hyrbricka, ui, sveltekit, rest, i18n]
requirements:
  - REQ-STD-004
  - REQ-EVT-CMP-004
  - REQ-PRIV-002
  - REQ-EXT-MEOS-001
must_haves:
  truths:
    - 'GET /api/eventor/lookup?si_card=8535005 returns 200 { hit: true, name, club } for a cached competitor with that SI card'
    - 'GET /api/eventor/lookup?prefix=Hag returns 200 { suggestions: [...] } with up to 20 EventorCompetitor matches by family_name prefix'
    - "GET /api/eventor/lookup with no params returns 400 { error: 'missing_query' }"
    - "GET /api/eventor/status returns 200 { state: 'ready' | 'stale' | 'offline' | 'no_key', ageDays: number | null, competitorCount: number, fartol_dev: boolean }"
    - "WalkupModal renders the label 'Bana' (not 'Klass') for the course-picker field (D-locked-#1)"
    - 'When the operator sets Hyrbricka checkbox, contact fields appear; saving without at least phone OR email returns the validation error (D-HB-3)'
    - 'POST /api/competitors with { hired_card: true, hired_contact: {...} } writes the competitors row AND a hired_cards row inside ONE sqlite.transaction'
    - 'Playwright e2e walkup-eventor.spec.ts shows: type bricka → name + club pre-fill from Eventor cache; type prefix in name field → suggestions appear; check Hyrbricka → fields expand'
  artifacts:
    - path: 'apps/edge/src/eventor/lookup.ts'
      provides: 'lookupBySiCard(handle, n) + lookupByNamePrefix(handle, prefix, limit) — read-only SQLite queries against Plan-01 tables'
      min_lines: 40
    - path: 'apps/edge/src/routes/eventor.ts'
      provides: 'Fastify plugin: GET /api/eventor/lookup + GET /api/eventor/status'
      min_lines: 60
    - path: 'apps/web/src/lib/components/EventorAutocomplete.svelte'
      provides: '200ms-debounced name-prefix autocomplete (minLength 2) targeting /api/eventor/lookup?prefix='
      min_lines: 60
    - path: 'apps/web/src/lib/screens/WalkupModal.svelte'
      provides: 'Bana label + Hyrbricka checkbox + expandable contact fieldset + Eventor lookup integration'
      contains: 'hired_card'
    - path: 'apps/web/src/lib/stores/eventorStatus.svelte.ts'
      provides: 'Svelte 5 rune store polling /api/eventor/status; surfaces state to TweaksPanel'
      min_lines: 30
  key_links:
    - from: 'apps/web/src/lib/components/EventorAutocomplete.svelte'
      to: '/api/eventor/lookup'
      via: '200ms-debounced fetch with prefix query param'
      pattern: 'eventor/lookup'
    - from: 'apps/web/src/lib/screens/WalkupModal.svelte'
      to: '/api/competitors'
      via: 'createCompetitor() payload includes hired_card + hired_contact when checkbox set'
      pattern: 'hired_card'
    - from: 'apps/edge/src/routes/competitors.ts'
      to: 'apps/edge/src/db/schema.ts hiredCards'
      via: 'single sqlite.transaction insert into competitors + hired_cards'
      pattern: 'hiredCards|hired_cards'
    - from: 'apps/web/src/lib/components/TweaksPanel.svelte'
      to: '/api/eventor/status'
      via: 'polling fetch on mount + manual refresh button'
      pattern: 'eventor/status'
---

<objective>
Wave 1 plan, parallel-safe with Plans 03 + 04. Delivers the **operator-facing
walk-up surface** for Phase 2.0:

1. **Eventor REST surface** (`/api/eventor/lookup` + `/api/eventor/status`)
   reading Plan-01's cache tables.
2. **WalkupModal extensions**: relabel "Klass" → "Bana" (locked decision #1),
   add the Hyrbricka checkbox + expandable contact fields (D-HB-3), wire the
   Eventor name autocomplete (replaces the empty form when the operator types
   a name) AND the bricka-scan pre-fill (when operator enters SI card → look up
   in cache → pre-fill name + club).
3. **competitors route extension**: accept `hired_card` + `hired_contact` in
   POST body; write `hired_cards` row in the same atomic transaction as the
   competitor row.
4. **TweaksPanel Eventor indicator**: read /api/eventor/status; display
   `Eventor: cache N dagar gammal | offline | nyckel saknas` per D-EV-3 +
   RESEARCH §"UI signaling".

Purpose:

- Honor locked decision #1 (course-only "Bana" label).
- Honor D-HB-3 (Hyrbricka requires at least phone OR email).
- Honor D-EV-3 (offline-friendly UI indicator — never blocks form).
- Honor REQ-STD-004 (Eventor integration; walk-up only, no entries pull).
- Match Phase 1 PATTERNS S-6 snake_case at JSON boundary + Svelte 5 rune idioms.

Output:

- 2 new edge modules (lookup + routes/eventor); 1 extended route
  (competitors); 1 schema-typed DTO addition.
- 1 new Svelte component (EventorAutocomplete) + 1 store (eventorStatus) + 1
  extended modal (WalkupModal) + 1 extended panel (TweaksPanel).
- i18n keys for sv.json + en.json from day one (PATTERNS S-8).
- Playwright e2e covering: bricka pre-fill, name autocomplete, Hyrbricka
  checkbox + validation.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-4-klubbs-mvp/02-CONTEXT.md
@.planning/phases/02-4-klubbs-mvp/02-RESEARCH.md
@.planning/phases/02-4-klubbs-mvp/02-PATTERNS.md
@.planning/phases/02-4-klubbs-mvp/02-01-PLAN.md
@apps/edge/src/db/schema.ts
@apps/edge/src/routes/competitors.ts
@apps/edge/src/routes/clubs.ts
@apps/edge/src/server.ts
@apps/web/src/lib/screens/WalkupModal.svelte
@apps/web/src/lib/components/ClubAutocomplete.svelte
@apps/web/src/lib/components/TweaksPanel.svelte
@apps/web/src/lib/i18n/index.ts
@apps/web/src/lib/api/client.ts
@packages/shared-types/src/dtos.ts

<interfaces>
From Plan 02-01:
- Drizzle tables: eventorCompetitors (pk personId, indexes idx_eventor_si_card + idx_eventor_name), eventorClubs (pk clubId, name), hiredCards (compound PK [competitionId, cardNumber], columns contactName/Phone/Email/note).
- competitors.source column ('walkup' | 'entrylist' | 'meos').

From Plan 02-01's boot module:

- `app.fartolEventor: EventorHandle | undefined` decoration on FastifyInstance — used by /api/eventor/status to read config row.

REST shapes added in this plan:

- GET /api/eventor/lookup?si_card=N → 200 `{ hit: true, person_id, family_name, given_name, club_id, club_name } | { hit: false }`.
- GET /api/eventor/lookup?prefix=S&limit=20 → 200 `{ suggestions: Array<{ person_id, family_name, given_name, club_name, si_card }> }`.
- GET /api/eventor/lookup with neither → 400 `{ error: 'missing_query' }`.
- GET /api/eventor/status → 200 `{ state: 'ready' | 'stale' | 'offline' | 'no_key'; ageDays: number | null; competitorCount: number; fartol_dev: boolean }` — `fartol_dev` is `process.env.FARTOL_DEV === '1'` evaluated at request time so the web TweaksPanel can gate the "Uppdatera" admin button on it.

POST /api/competitors body extension (additive — backward compatible):

- `hired_card?: boolean` (default false).
- `hired_contact?: { name: string | null; phone: string | null; email: string | null; note: string | null } | null`.
- Server-side validation: if `hired_card === true`, at least one of `hired_contact.phone` OR `hired_contact.email` MUST be non-null non-empty; otherwise return 400 `{ error: 'hyrbricka_contact_required' }`.

i18n keys to add to sv.json + en.json:

- walk.bana / walk.banaPlaceholder
- walk.hyrbricka / walk.hyrbricka.name / walk.hyrbricka.phone / walk.hyrbricka.email / walk.hyrbricka.note
- walk.err.hyrbrickaContact ("Ange minst telefon eller e-post för hyrbrickan." / "Enter at least phone or email for the hired card.")
- walk.eventor.fill ("Hämtad från Eventor — kontrollera namn")
- tweaks.eventor.title / tweaks.eventor.ready / tweaks.eventor.stale / tweaks.eventor.offline / tweaks.eventor.no_key / tweaks.eventor.refreshButton
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Eventor lookup module + /api/eventor/lookup + /api/eventor/status routes</name>
  <files>
    apps/edge/src/eventor/lookup.ts,
    apps/edge/src/eventor/lookup.test.ts,
    apps/edge/src/routes/eventor.ts,
    apps/edge/src/routes/eventor.test.ts,
    apps/edge/src/server.ts
  </files>
  <read_first>
    .planning/phases/02-4-klubbs-mvp/02-PATTERNS.md §3 (Fastify route shape from routes/clubs.ts + the Querystring Zod parsing template),
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"UI signaling" (status payload shape: { state, ageDays }),
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Eventor lookup endpoint" code-examples block,
    apps/edge/src/routes/clubs.ts (Zod querystring + 200-row limit pattern — the closest existing analog),
    apps/edge/src/routes/admin.ts (FastifyInstance.fartolEventor decoration shape from Plan 01 Task 4),
    apps/edge/src/db/schema.ts (Plan-01 eventorCompetitors + eventorClubs definitions),
    apps/edge/src/server.ts (the registerXxx + `await app.register(...)` ordering — where to slot registerEventorRoutes)
  </read_first>
  <behavior>
    - Test 1 (lookup.test.ts): lookupBySiCard(handle, 8535005) on seeded DB returns { hit: true, person_id: 1001, family_name: 'Hagberg', given_name: 'Jonas', club_id: 637, club_name: 'Stora Tuna OK' }.
    - Test 2: lookupBySiCard(handle, 99999999) returns { hit: false }.
    - Test 3: lookupByNamePrefix(handle, 'Östb', 20) returns suggestions including the Östberg record with club_name resolved via JOIN.
    - Test 4: lookupByNamePrefix with empty prefix returns [] (cheap guard — caller is supposed to gate on minLength 2).
    - Test 5 (eventor.test.ts): GET /api/eventor/lookup?si_card=8535005 returns 200 with the hit shape; missing both params returns 400 { error: 'missing_query' }; lookup with both params returns 400 { error: 'conflicting_query' }.
    - Test 6: GET /api/eventor/status returns state='no_key' when EVENTOR_API_KEY is undefined; state='ready' when config row exists and is fresh; state='stale' when older than 7 days; competitorCount derived from `SELECT COUNT(*) FROM eventor_competitors`. Response includes `fartol_dev: boolean` matching `process.env.FARTOL_DEV === '1'` (test both branches by toggling env var in test setup).
  </behavior>
  <action>
    Write `apps/edge/src/eventor/lookup.ts` exporting:
      - `interface EventorLookupHit { hit: true; person_id: number; family_name: string; given_name: string; club_id: number | null; club_name: string | null }`.
      - `interface EventorLookupMiss { hit: false }`.
      - `function lookupBySiCard(handle: DbHandle, siCard: number): EventorLookupHit | EventorLookupMiss` — single-row SELECT with a LEFT JOIN onto eventor_clubs for club_name. Index `idx_eventor_si_card` already exists per Plan 01.
      - `interface EventorNameSuggestion { person_id: number; family_name: string; given_name: string; club_name: string | null; si_card: number | null }`.
      - `function lookupByNamePrefix(handle: DbHandle, prefix: string, limit: number): EventorNameSuggestion[]` — `WHERE family_name LIKE ? || '%'` (case-sensitive index hit). Limit clamped to [1, 50] by caller; lookup just trusts.

    Write `apps/edge/src/routes/eventor.ts` exporting `registerEventorRoutes(app: FastifyInstance): Promise<void>`:
      - Zod schema `LookupQuery = z.object({ si_card: z.coerce.number().int().positive().optional(), prefix: z.string().min(1).max(120).optional(), limit: z.coerce.number().int().positive().max(50).optional() })`.
      - `GET /api/eventor/lookup` — validate; if `si_card && prefix` → 400 conflicting_query; if neither → 400 missing_query; otherwise dispatch to lookupBySiCard or lookupByNamePrefix.
      - `GET /api/eventor/status` — read config row `eventor_cache_refreshed_at_ms`. If absent AND `process.env['EVENTOR_API_KEY']` absent → `{ state: 'no_key', ageDays: null, competitorCount: 0, fartol_dev }`. Else compute ageDays from now; state='stale' if age>7d, 'ready' otherwise, 'offline' if competitorCount==0 (cache empty AND a key is present implies the boot fetch failed). Always include `competitorCount` from `SELECT COUNT(*) FROM eventor_competitors` AND `fartol_dev: process.env['FARTOL_DEV'] === '1'` evaluated at request time (NOT bundler time — handler closure captures the env at request time, which is correct).

    Wire in `apps/edge/src/server.ts` adding `await app.register(registerEventorRoutes)` next to the other route registrations (after registerClubs since lookup conceptually parallels clubs autocomplete).

    Write `lookup.test.ts` (4 tests) using in-memory DB seeded with the Plan-01 fixtures (re-use the same competitors-sample.xml via ingestEventorCache, or hand-seed three rows directly via drizzle).

    Write `eventor.test.ts` (3 tests on the routes via `app.inject({ method: 'GET', url: '...' })`).

  </action>
  <verify>
    <automated>pnpm --filter @fartol/edge test --test-name-pattern="eventor/lookup|routes/eventor" 2&gt;&amp;1 | tail -50</automated>
  </verify>
  <done>
    lookup.ts + routes/eventor.ts compile; all 6 tests pass; new route registered in server.ts after clubs route.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/competitors extension — hired_card + hired_cards transactional insert</name>
  <files>
    apps/edge/src/routes/competitors.ts,
    apps/edge/src/routes/competitors.test.ts,
    packages/shared-types/src/dtos.ts
  </files>
  <read_first>
    apps/edge/src/routes/competitors.ts (existing POST handler lines 340-450 — the `sqlite.transaction()` block that already inserts the competitor row + clubs upsert + card_bound event; we add a hired_cards INSERT inside the same transaction),
    apps/edge/src/db/schema.ts (Plan-01 hiredCards table — compound PK shape),
    .planning/phases/02-4-klubbs-mvp/02-CONTEXT.md D-HB-1 / D-HB-3 (the validation contract),
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Pitfall 10: Drizzle ON CONFLICT for compound PK requires explicit target" (hiredCards INSERT shape),
    .planning/phases/02-4-klubbs-mvp/02-PATTERNS.md §S-2 (transactional pattern) + S-5 (pre-flight check before transaction),
    packages/shared-types/src/dtos.ts (existing CompetitorCreateInput Zod schema we extend additively)
  </read_first>
  <behavior>
    - Test 1: POST /api/competitors with `{ ..., hired_card: false }` (or omitted) behaves IDENTICALLY to Phase 1 — no hired_cards row created. Existing competitors.test.ts cases still pass.
    - Test 2: POST /api/competitors with `{ ..., hired_card: true, hired_contact: { name: 'Foo', phone: '0701234567', email: null, note: null } }` returns 201 AND a row exists in hired_cards with (competition_id, card_number, contact_phone='0701234567', returned_at_ms=NULL, marked_at_ms=Date.now()).
    - Test 3: POST /api/competitors with `{ hired_card: true, hired_contact: { name: null, phone: null, email: null, note: null } }` returns 400 with body `{ error: 'hyrbricka_contact_required' }` AND no competitor row inserted (transaction rolled back via early validation BEFORE transaction).
    - Test 4: POST /api/competitors that fails the existing 409 card-collision returns 409 WITHOUT writing a hired_cards row (transactional atomicity).
    - Test 5: Posting twice with hired_card=true and the SAME card_number under the SAME competition (after the first competitor is deleted — exercising re-rental) honors the compound PK — second INSERT uses .onConflictDoUpdate({ target: [competitionId, cardNumber], ... }) to replace the open rental row (marked_at_ms re-set, returned_at_ms re-null).
  </behavior>
  <action>
    Extend `packages/shared-types/src/dtos.ts`'s existing `CompetitorCreateInput` Zod schema additively:

      Add to the object: `hired_card: z.boolean().optional().default(false)`, `hired_contact: z.object({ name: z.string().nullable(), phone: z.string().nullable(), email: z.string().nullable(), note: z.string().nullable() }).nullable().optional()`. Export the inferred type as `CompetitorCreateInput`.

    Extend `apps/edge/src/routes/competitors.ts` POST handler:
      1. Pre-flight validation BEFORE opening the transaction: if `body.hired_card === true`, check that `hired_contact && (hired_contact.phone || hired_contact.email)`; otherwise `return reply.code(400).send({ error: 'hyrbricka_contact_required' })`. Per PATTERNS S-5 — pre-flight before transaction.
      2. Inside the existing `sqlite.transaction(() => { ... })()` block that inserts competitor + clubs + card_bound event, append a conditional `hired_cards` INSERT when `body.hired_card === true`. Use `.insert(hiredCards).values({ competitionId, cardNumber: body.card_number, markedAtMs: now, returnedAtMs: null, contactName: body.hired_contact?.name ?? null, contactPhone: body.hired_contact?.phone ?? null, contactEmail: body.hired_contact?.email ?? null, note: body.hired_contact?.note ?? null }).onConflictDoUpdate({ target: [hiredCards.competitionId, hiredCards.cardNumber], set: { markedAtMs: now, returnedAtMs: null, contactName: ..., contactPhone: ..., contactEmail: ..., note: ... } }).run()`. The compound-target shape is the Pitfall 10 mitigation.
      3. Optional: broadcast a `hired_card_marked` WS envelope on readoutChannel(competitionId) for live operator awareness; payload `{ card_number, contact_phone }`. Defer the toast UI to Plan 05; here we only emit so Plan 05's WS handler has the channel ready.

    Extend `apps/edge/src/routes/competitors.test.ts` with the 5 behaviors above using `app.inject()` against an in-memory bridge.

    DO NOT change the consent flow, the auto-attach reducer call, or the 409 replace-card path (Phase 1 carries forward verbatim).

  </action>
  <verify>
    <automated>pnpm --filter @fartol/edge test --test-name-pattern="competitors" 2&gt;&amp;1 | tail -50</automated>
  </verify>
  <done>
    competitors.ts compiles; all 5 new tests pass; pre-existing competitors tests still pass; CompetitorCreateInput Zod schema accepts hired_card + hired_contact additively (omitting them remains valid).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: EventorAutocomplete component + WalkupModal extensions (Bana, Hyrbricka, Eventor lookup) + i18n keys</name>
  <files>
    apps/web/src/lib/api/client.ts,
    apps/web/src/lib/components/EventorAutocomplete.svelte,
    apps/web/src/lib/screens/WalkupModal.svelte,
    apps/web/src/lib/i18n/sv.json,
    apps/web/src/lib/i18n/en.json
  </files>
  <read_first>
    apps/web/src/lib/components/ClubAutocomplete.svelte (the 200ms-debounced fetch + datalist template — copy verbatim, change endpoint),
    apps/web/src/lib/screens/WalkupModal.svelte (the existing form + cardHolderHint pre-fill + 409 replace-card flow — Plan 5 of RESEARCH §"WalkupModal Hyrbricka extension"),
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Pattern 5: WalkupModal Hyrbricka extension — checkbox + expandable contact fieldset" (the full svelte template — copy structure),
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Plan 2 — WalkupModal + EventorAutocomplete" (minLength 2 gate; eventorHint wins over cardHolderHint),
    .planning/phases/02-4-klubbs-mvp/02-CONTEXT.md locked decision #1 (Bana label) + D-HB-3 (Hyrbricka contact required)
  </read_first>
  <behavior>
    - Test 1 (svelte component test, Vitest): mount EventorAutocomplete; typing 1 char does NOT fire fetch (minLength 2 gate); typing 2 chars schedules fetch after 200ms; mock fetch returns suggestions; suggestions render as <datalist> options.
    - Test 2: WalkupModal renders the label 'Bana' for the course-picker field (assert visible text == t('walk.bana')).
    - Test 3: WalkupModal's Hyrbricka checkbox is initially unchecked; contact fieldset is HIDDEN. Click checkbox → fieldset SHOWS.
    - Test 4: WalkupModal with Hyrbricka checked + empty phone + empty email → onSave shows the validation error from t('walk.err.hyrbrickaContact') AND does NOT call createCompetitor.
    - Test 5: WalkupModal with cardNumber prop set + Eventor lookup returns a hit → name and club pre-fill from the lookup response (eventorHint wins over cardHolderHint per Plan 2 nuance).
  </behavior>
  <action>
    Extend `apps/web/src/lib/api/client.ts` adding:
      - `async function lookupEventorBySiCard(siCard: number): Promise<EventorLookupHit | EventorLookupMiss>` — GET /api/eventor/lookup?si_card=
      - `async function lookupEventorByPrefix(prefix: string, limit = 20): Promise<EventorNameSuggestion[]>` — GET /api/eventor/lookup?prefix=&limit=
      - Re-export the response interfaces from shared-types (add them to packages/shared-types/src/dtos.ts as Zod inferable types).

    Write `apps/web/src/lib/components/EventorAutocomplete.svelte` — copy ClubAutocomplete.svelte's structure verbatim and change:
      - endpoint → /api/eventor/lookup?prefix=
      - minLength gate: `if (trimmed.length < 2) { suggestions = []; return; }` (Plan 2 nuance — 252k rows means no empty-prefix prime).
      - suggestion render: `<option value="{family_name}, {given_name}{club_name ? ' (' + club_name + ')' : ''}">`. The value is what populates the input on selection; the visible " (Klubb)" suffix is decorative (datalist always shows the value).
      - When the operator picks a suggestion, the parent (WalkupModal) reads the selected suggestion's club_name and uses it to populate the klubb field. This is a callback prop `onPick: (suggestion: EventorNameSuggestion) => void`.

    Extend `apps/web/src/lib/screens/WalkupModal.svelte` per RESEARCH Pattern 5:
      1. Add prop `eventorHint?: EventorLookupHit | null = null` and apply pre-fill precedence on mount: if eventorHint?.hit, set `name = '<family>, <given>'` AND `club = eventorHint.club_name ?? ''` (eventorHint wins over cardHolderHint per Plan 2 nuance).
      2. Relabel the course-picker `<Field label={t('walk.bana')}>` (was `t('walk.class')`); update placeholder to `t('walk.banaPlaceholder')`. Existing `walk.class` key remains in i18n for Phase 2.1 alias compatibility.
      3. Wire `<EventorAutocomplete onPick={...}>` next to or replacing the existing freeform name input. Selecting a suggestion populates `name` + `club` via callback.
      4. When the operator types a fresh `cardNumberLocal` (rare in walkup — usually inherited from props), fire `lookupEventorBySiCard(cardNumberLocal)` debounced 200ms; on hit, pre-fill name + club + show a brief inline note via `t('walk.eventor.fill')`.
      5. Add the Hyrbricka block per Pattern 5: `let hiredCard = $state(false)`, `let contactName = $state('')`, `let contactPhone = $state('')`, `let contactEmail = $state('')`, `let note = $state('')`. Markup: a `<label class="consent-row">` with `<input type="checkbox" bind:checked={hiredCard} data-testid="walkup-hired" />`; below it, `{#if hiredCard}` block with the four `<Field>`s for the contact inputs.
      6. Extend `validate()`: if `hiredCard && contactPhone.trim() === '' && contactEmail.trim() === ''` return `t('walk.err.hyrbrickaContact')`.
      7. Extend the createCompetitor() call to pass `hired_card: hiredCard` AND `hired_contact: hiredCard ? { name, phone, email, note } : null`.

    Add i18n keys to `apps/web/src/lib/i18n/sv.json` AND `en.json` (Swedish first, English second):
      - walk.bana: "Bana" / "Course"
      - walk.banaPlaceholder: "Välj bana" / "Select course"
      - walk.hyrbricka: "Hyrbricka" / "Hired card"
      - walk.hyrbricka.name: "Kontaktnamn" / "Contact name"
      - walk.hyrbricka.phone: "Telefon" / "Phone"
      - walk.hyrbricka.email: "E-post" / "Email"
      - walk.hyrbricka.note: "Anteckning" / "Note"
      - walk.err.hyrbrickaContact: "Ange minst telefon eller e-post för hyrbrickan." / "Enter at least phone or email for the hired card."
      - walk.eventor.fill: "Hämtad från Eventor — kontrollera namn" / "Fetched from Eventor — please check the name"

    DO NOT remove the existing `cardHolderHint` prop or its fallback logic — it still applies when Eventor returns a miss.

  </action>
  <verify>
    <automated>pnpm --filter @fartol/web test --run --reporter=verbose 2&gt;&amp;1 | tail -60</automated>
  </verify>
  <done>
    EventorAutocomplete component + WalkupModal extensions compile; all 5 web tests pass; sv.json + en.json include the new keys; existing WalkupModal tests still pass (additive changes only).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: TweaksPanel Eventor status indicator + eventorStatus store</name>
  <files>
    apps/web/src/lib/stores/eventorStatus.svelte.ts,
    apps/web/src/lib/components/TweaksPanel.svelte,
    apps/web/src/lib/i18n/sv.json,
    apps/web/src/lib/i18n/en.json,
    apps/edge/src/routes/admin.ts
  </files>
  <read_first>
    apps/web/src/lib/stores/bridgeStatus.svelte.ts (the Svelte 5 rune store template — copy structure for eventorStatus),
    apps/web/src/lib/components/TweaksPanel.svelte (existing layout — find a logical insertion point for the Eventor row, near bridge status or in its own panel section),
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"UI signaling" (state enum, ageDays display, label keys),
    .planning/phases/02-4-klubbs-mvp/02-01-PLAN.md (admin endpoint /api/__admin/eventor/refresh — confirms FARTOL_DEV gate AND that the route returns the runNow() result that this UI consumes),
    apps/edge/src/routes/admin.ts (existing FARTOL_DEV pattern — verify Plan 01 added the eventor/refresh route here)
  </read_first>
  <behavior>
    - Test 1 (store test): eventorStatus.refresh() fetches /api/eventor/status, sets store state to response. Mock fetch returns `{ state: 'ready', ageDays: 2, competitorCount: 252919 }` → store reflects.
    - Test 2: eventorStatus.refresh() on fetch error sets state='offline'.
    - Test 3 (component test, TweaksPanel): with eventorStatus.state='ready' AND ageDays=2 → renders "Eventor: cache OK (2 dagar gammal)" + green status dot.
    - Test 4: with state='no_key' → renders "Eventor: nyckel saknas (EVENTOR_API_KEY)" + red dot.
    - Test 5: with FARTOL_DEV=1 → renders a 'Uppdatera' button that POSTs /api/__admin/eventor/refresh; clicking refetches status afterward.
  </behavior>
  <action>
    Write `apps/web/src/lib/stores/eventorStatus.svelte.ts` — Svelte 5 rune-based store mirroring bridgeStatus.svelte.ts shape:
      - `interface EventorStatusState { state: 'ready' | 'stale' | 'offline' | 'no_key' | 'refreshing' | 'unknown'; ageDays: number | null; competitorCount: number }`.
      - Module-level `let state: EventorStatusState = $state({ state: 'unknown', ageDays: null, competitorCount: 0 })`.
      - Export `function getEventorStatus(): EventorStatusState { return state; }` and `async function refreshEventorStatus(): Promise<void>` that fetches /api/eventor/status, sets state, catches network errors and sets state='offline'.
      - Export `async function triggerEventorRefresh(): Promise<void>` that POSTs /api/__admin/eventor/refresh (FARTOL_DEV-gated server side); sets state='refreshing' optimistically; calls refreshEventorStatus() after.

    Extend `apps/web/src/lib/components/TweaksPanel.svelte` adding a new row near the bridge status indicator:
      - Subscribe to getEventorStatus() in the template; render the Swedish label + status dot per the 5 keys: tweaks.eventor.title + a conditional value string from tweaks.eventor.{ready|stale|offline|no_key|refreshing}.
      - For `state='ready'`, format the ageDays into the label via i18next interpolation: `tweaks.eventor.ready: "Eventor: cache OK ({{days}} dagar gammal)"`.
      - Extend the Plan 01 `GET /api/eventor/status` response to include a `fartol_dev: boolean` field. Server reads it as `process.env.FARTOL_DEV === '1'` at request time. The TweaksPanel template gates the refresh button on `status.fartol_dev === true` — only renders `<button onclick={triggerEventorRefresh}>` labeled `tweaks.eventor.refreshButton` ("Uppdatera") when the server reports dev mode. Do NOT rely on `import.meta.env.DEV` (which is bundler-time, not runtime; would always be `false` in production builds even when FARTOL_DEV=1).
      - Call `refreshEventorStatus()` on component mount (Svelte 5 `$effect`).

    Add i18n keys (sv.json + en.json):
      - tweaks.eventor.title: "Eventor" (same in both)
      - tweaks.eventor.ready: "Eventor: cache OK ({{days}} dagar gammal)" / "Eventor: cache OK ({{days}} days old)"
      - tweaks.eventor.stale: "Eventor: cachen är gammal — uppdatera när du har internet" / "Eventor: cache is stale — refresh when online"
      - tweaks.eventor.offline: "Eventor: ingen internetkontakt" / "Eventor: no internet connection"
      - tweaks.eventor.no_key: "Eventor: nyckel saknas (EVENTOR_API_KEY)" / "Eventor: API key missing (EVENTOR_API_KEY)"
      - tweaks.eventor.refreshing: "Eventor: hämtar..." / "Eventor: refreshing..."
      - tweaks.eventor.refreshButton: "Uppdatera" / "Refresh"

    NO server-side changes beyond what Plan 01 already added (admin route exists; status route from Task 1 exists).

  </action>
  <verify>
    <automated>pnpm --filter @fartol/web test --run --reporter=verbose --testPathPattern="(eventorStatus|TweaksPanel)" 2&gt;&amp;1 | tail -40</automated>
  </verify>
  <done>
    eventorStatus.svelte.ts + TweaksPanel additions compile; all 5 tests pass; new i18n keys present.
  </done>
</task>

<task type="auto">
  <name>Task 5: Playwright e2e — walkup-eventor flow</name>
  <files>
    apps/web/e2e/walkup-eventor.spec.ts
  </files>
  <read_first>
    apps/web/e2e/walkup.spec.ts (Phase 1 walkup e2e — the bench-fixture seeding + WalkupModal selector strategy; copy the structure),
    apps/web/e2e/results.spec.ts (the createCompetition + import-courseData helper pattern — Plan 5 of Phase 1 e2e),
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Hyrbricka E2E (Playwright)" (the full spec body — adapt verbatim),
    apps/edge/src/routes/dev.ts (the /api/__dev/simulate-read endpoint we trigger to inject a card_read event)
  </read_first>
  <action>
    Write `apps/web/e2e/walkup-eventor.spec.ts` covering:

      1. **Seed**: create a competition + import the 4-klubbs courseData fixture (`.reference/2026-05-20 4-klubbs_coursedata.xml`) via POST /api/competitions/<id>/import.
      2. **Seed Eventor cache**: POST /api/__dev/eventor-seed (NEW dev-route — if missing, this test depends on the Plan-01 fixtures being ingested via the boot path; instead use Playwright's `page.evaluate` to call `lookupEventorBySiCard(8535005)` via the API client). For e2e simplicity, instead pre-seed via direct better-sqlite3 INSERT in a `test.beforeEach` helper that uses `apps/edge/src/eventor/cache.ts.ingestEventorCache()` with the Plan-01 fixture file.
      3. **Test: bricka pre-fill**: navigate to `/competition/<id>/readout`; trigger walk-up modal by simulating a card read for an unknown card (8535005 — Hagberg Jonas from Plan-01 fixtures); assert the WalkupModal name input shows "Hagberg, Jonas" and club shows "Stora Tuna OK".
      4. **Test: name prefix autocomplete**: open the WalkupModal manually (e.g. via a "Add walk-up" button); type "Östb" in the name input; wait for the datalist to populate; assert at least one option contains "Östberg, Pär".
      5. **Test: Bana label**: assert the course-picker field label reads "Bana" (not "Klass").
      6. **Test: Hyrbricka — happy path**: fill name + club + bana=Vit; bricka=88888 (a new card); check Hyrbricka; assert contact fieldset appears; fill phone='0701234567'; click Spara; assert modal closes AND GET /api/competitions/<id>/hired-cards returns exactly one open rental for card_number=88888.
      7. **Test: Hyrbricka — validation**: check Hyrbricka, leave both phone and email empty, click Spara; assert error message visible (matching t('walk.err.hyrbrickaContact')) AND modal does NOT close.

    Use Playwright's `request.post()` for seeding (no UI dependency) and `page.locator('[data-testid="walkup-hired"]')` for the checkbox. data-testid attributes are added in Task 3 per Phase 1 convention.

    Wire the test into the existing Playwright project; reuse the Phase 1 webServer config in `playwright.config.ts` (no changes needed if the apps/edge bin path is unchanged).

  </action>
  <verify>
    <automated>pnpm exec playwright test apps/web/e2e/walkup-eventor.spec.ts --reporter=line 2&gt;&amp;1 | tail -50</automated>
  </verify>
  <done>
    walkup-eventor.spec.ts passes all 5 scenarios under Playwright; e2e is deterministic (no flakes on 3 consecutive runs).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                                                                                           |
| -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| browser → /api/eventor/lookup                | LAN client; CORS allow-list from Phase 1 still applies                                                                                                |
| browser → /api/competitors                   | extended payload now carries PII (hired_contact); same trust as Phase 1                                                                               |
| TweaksPanel → /api/\_\_admin/eventor/refresh | FARTOL_DEV-gated; if exposed in production, operator could DoS Eventor via repeated clicks (rate limited by D-EV-2 7-day skip in scheduleEventorBoot) |

## STRIDE Threat Register

| Threat ID | Category        | Component                                                                     | Disposition | Mitigation Plan                                                                                                                        |
| --------- | --------------- | ----------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| T-02P2-01 | Info Disclosure | /api/eventor/lookup leaks 252 k name DB to anyone on LAN                      | accept      | LAN-only deployment; Phase 1 CORS allow-list + --bind-host gate apply; matches Phase 1 posture for clubs autocomplete                  |
| T-02P2-02 | Tampering       | competitors POST with hired_card=true but missing contact bypasses validation | mitigate    | Server-side pre-flight (Task 2) — UI validation is best-effort UX; server is authoritative                                             |
| T-02P2-03 | Info Disclosure | hired_contact.phone/email written to fastify pino logs                        | mitigate    | Redact body in route logging — opts.routeLogger logs only `{ competitionId, card_number, has_hired: bool }` not the contact\_\* fields |
| T-02P2-04 | XSS             | EventorAutocomplete renders names containing `<script>`                       | mitigate    | Svelte 5 `{name}` text interpolation auto-escapes; PATTERNS check: no `{@html}` introduced                                             |
| T-02P2-05 | DoS             | /api/eventor/lookup?prefix= with 1-char query returns 252 k rows              | mitigate    | Zod schema minLength + server clamps limit to ≤50; UI minLength 2 gate                                                                 |
| T-02P2-06 | Spoofing        | malicious LAN client POSTs /api/competitors creating fake hired rentals       | accept      | Same trust model as Phase 1 walk-up — operator-visible in admin view (Plan 05); D-MIP-1 LAN trust posture                              |
| T-02P2-SC | Tampering       | no new dependencies installed in this plan                                    | mitigate    | Wave 0 saxes gate covers Phase 2.0's only net-new install; this plan adds no further npm installs                                      |

</threat_model>

<verification>
- `pnpm --filter @fartol/edge test` exits 0; all new + existing tests pass.
- `pnpm --filter @fartol/web test --run` exits 0.
- `pnpm exec playwright test apps/web/e2e/walkup-eventor.spec.ts` exits 0.
- Manual smoke: boot bridge with `EVENTOR_API_KEY=<real>` set; open TweaksPanel; the Eventor row shows "cache OK" with the live ageDays value.
- Manual smoke: open WalkupModal for a card known to be in the Plan-01 cache; name + club pre-fill instantly.
</verification>

<success_criteria>

- Operator can register a walk-up runner from Eventor cache by entering bricka OR typing a name prefix.
- Operator can mark a rental card with required contact info, atomically persisted with the competitor row.
- Eventor cache status is visible in TweaksPanel and operators can force-refresh via admin button.
- D-EV-3 honored: TweaksPanel renders gracefully when key missing / cache empty.
  </success_criteria>

<output>
Create `.planning/phases/02-4-klubbs-mvp/02-02-SUMMARY.md` when done.
</output>
