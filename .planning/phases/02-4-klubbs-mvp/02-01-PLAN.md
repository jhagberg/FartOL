---
phase: 02-4-klubbs-mvp
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/edge/package.json
  - apps/edge/src/db/schema.ts
  - apps/edge/drizzle/0002_phase2.sql
  - apps/edge/drizzle/meta/0002_snapshot.json
  - apps/edge/drizzle/meta/_journal.json
  - apps/edge/src/eventor/parser.ts
  - apps/edge/src/eventor/parser.test.ts
  - apps/edge/src/eventor/__fixtures__/competitors-sample.xml
  - apps/edge/src/eventor/__fixtures__/clubs-sample.xml
  - apps/edge/src/eventor/cache.ts
  - apps/edge/src/eventor/cache.test.ts
  - apps/edge/src/eventor/download.ts
  - apps/edge/src/eventor/boot.ts
  - apps/edge/src/eventor/boot.test.ts
  - apps/edge/src/bin/fartola.ts
  - apps/edge/src/routes/admin.ts
  - apps/edge/src/routes/admin.test.ts
  - .planning/adr/0009-eventor-runner-cache.md
  - .planning/REQUIREMENTS.md
autonomous: false
tags: [eventor, sqlite, saxes, streaming, schema, adr, boot-wiring]
requirements:
  - REQ-STD-004
  - REQ-OPS-001
  - REQ-PRIV-002
must_haves:
  truths:
    - "After `pnpm db:generate && pnpm --filter @fartola/edge test`, schema migration 0002_phase2.sql creates 6 new tables (eventor_competitors, eventor_clubs, meos_competitors, meos_classes, meos_clubs, hired_cards) AND adds `source TEXT NOT NULL DEFAULT 'walkup'` to competitors"
    - "streamCompetitorsXml parses the synthetic 3-competitor fixture and emits 3 EventorCompetitor records including the Östberg/Pär UTF-8 row and an orphan-no-Organisation row"
    - "ingestEventorCache wraps the snapshot replace in a single sqlite.transaction so a thrown parse error rolls back to the prior cache state"
    - "config row `eventor_cache_refreshed_at_ms` is written on every successful ingest"
    - "scheduleEventorBoot returns a `{ runNow, stop }` handle whose runNow respects the 7-day staleness gate (D-EV-2): fresh cache → skip + log; stale-or-empty cache → fire-and-forget refresh"
    - "bin/fartola.ts wires app.fartolaEventor AFTER app.listen and never blocks startup on Eventor reachability (D-EV-3)"
    - "POST /api/__admin/eventor/refresh (FARTOLA_DEV-gated) returns 200 { ok: true, competitors, clubs } on success"
    - "REQUIREMENTS.md gains a REQ-EXT-MEOS-001 entry under a new 'External interop' section"
    - ".planning/adr/0009-eventor-runner-cache.md exists with status: accepted and references REQ-STD-004 + REQ-PRIV-002 + REQ-OPS-001"
  artifacts:
    - path: "apps/edge/src/db/schema.ts"
      provides: "Drizzle TS definitions for the 6 new tables plus competitors.source column"
      contains: "eventorCompetitors"
    - path: "apps/edge/drizzle/0002_phase2.sql"
      provides: "Generated migration creating new tables, indexes, and ALTER for competitors.source"
      contains: "eventor_competitors"
    - path: "apps/edge/src/eventor/parser.ts"
      provides: "streamCompetitorsXml saxes-streaming parser emitting EventorCompetitor records with DOCTYPE/ENTITY pre-flight"
      min_lines: 80
    - path: "apps/edge/src/eventor/cache.ts"
      provides: "ingestEventorCache: download → unzip → stream-parse → transactional snapshot replace + config marker"
      min_lines: 100
    - path: "apps/edge/src/eventor/boot.ts"
      provides: "scheduleEventorBoot — { runNow, stop } handle that fires once if cache > 7d old or empty; never blocks bridge startup"
      min_lines: 60
    - path: ".planning/adr/0009-eventor-runner-cache.md"
      provides: "MADR ADR locking the PII trade-off + chosen mitigations (local-only, no phone/email, weekly refresh, clear-cache admin endpoint)"
      contains: "REQ-STD-004"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Adds REQ-EXT-MEOS-001 entry naming MIP/MOP coexistence (mentioned by ROADMAP Phase 2.0 but missing today)"
      contains: "REQ-EXT-MEOS-001"
  key_links:
    - from: "apps/edge/src/eventor/boot.ts"
      to: "apps/edge/src/bin/fartola.ts"
      via: "scheduleEventorBoot decoration after app.listen, fire-and-forget"
      pattern: "scheduleEventorBoot|fartolaEventor"
    - from: "apps/edge/src/eventor/cache.ts"
      to: "apps/edge/src/db/schema.ts"
      via: "drizzle insert into eventorCompetitors / eventorClubs inside sqlite.transaction"
      pattern: "eventorCompetitors|eventorClubs"
    - from: "apps/edge/src/eventor/cache.ts"
      to: "apps/edge/src/db/schema.ts config table"
      via: "config row eventor_cache_refreshed_at_ms = Date.now()"
      pattern: "eventor_cache_refreshed_at_ms"
    - from: "apps/edge/src/routes/admin.ts"
      to: "apps/edge/src/eventor/boot.ts"
      via: "POST /api/__admin/eventor/refresh calls app.fartolaEventor.runNow"
      pattern: "fartolaEventor"
---

<objective>
Wave 0 foundation for Phase 2.0. Land the SQLite schema for ALL six Phase 2.0
tables (eventor_*, meos_*, hired_cards) in ONE generated migration so Plans
02-05 can use them concurrently. Then land the Eventor löpardatabasen
download/parse/cache pipeline and wire it into bridge boot per D-EV-1 / D-EV-2 /
D-EV-3. Also commits ADR-0009 (PII trade-off) and the missing
REQ-EXT-MEOS-001 entry in REQUIREMENTS.md (RESEARCH.md flagged this gap at
line 100).

This plan is the ONLY blocking foundation for Phase 2.0. Plans 02 (Walkup +
EventorAutocomplete), 03 (MIP), 04 (MOP) all read or extend the schema this
plan creates. Plan 05 (Hyrbricka) reads `hired_cards`. Without this plan
landed, no other plan can compile.

Purpose:
- Honor D-EV-1 (boot job + admin button refresh, no cron), D-EV-2 (7-day
  staleness), D-EV-3 (warn + run on offline / missing key — NEVER block boot).
- Honor D-HB-1 by creating the `hired_cards` junction table here so Plan 02
  (walkup checkbox) and Plan 05 (toast) can write/read it without a second
  migration.
- Honor D-MOP-1 by creating shadow `meos_*` tables here so Plan 04's MOP
  receiver writes do not require a second migration.
- Mirror MeOS's Eventor download exactly (TabCompetition.cpp:3107-3108):
  `export/cachedcompetitors?includePreselectedClasses=false&zip=true&version=3.0`
  (Landmine "MeOS Eventor download uses eventorBase + iofExportVersion").

Output:
- Drizzle schema extensions + ONE generated migration (0002_phase2.sql).
- `apps/edge/src/eventor/` module: parser (saxes streaming), cache (transactional
  bulk-upsert with snapshot replace), boot (staleness gate + fire-and-forget),
  download (gunzip + HTTP fetch with EVENTOR_API_KEY).
- `app.fartolaEventor` wired in bin/fartola.ts; FARTOLA_DEV admin route to force
  refresh; tests including a UTF-8 + orphan-no-club fixture.
- ADR-0009 documenting the 252 919-row PII trade-off.
- REQUIREMENTS.md gains REQ-EXT-MEOS-001 entry (ROADMAP Phase 2.0 cited it but
  REQUIREMENTS.md last updated 2026-05-12).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-4-klubbs-mvp/02-CONTEXT.md
@.planning/phases/02-4-klubbs-mvp/02-RESEARCH.md
@.planning/phases/02-4-klubbs-mvp/02-PATTERNS.md
@.planning/research/eventor-api-smoke.md
@.planning/adr/0008-pii-in-append-only-event-log.md
@apps/edge/src/db/schema.ts
@apps/edge/src/ingest/entryImport.ts
@apps/edge/src/xml/parse.ts
@apps/edge/src/backup/daily.ts
@apps/edge/src/privacy/retention.ts
@apps/edge/src/routes/admin.ts
@apps/edge/src/bin/fartola.ts

<interfaces>
From apps/edge/src/db/schema.ts (Phase 1):
- sqliteTable, text, integer, uniqueIndex from drizzle-orm/sqlite-core.
- Existing tables: competitions, classes, courses, courseControls, controls, competitors, clubs, events, config.
- Convention: snake_case at the SQL boundary, camelCase TS field names. JSDoc PII annotations on scrubbable columns.

From apps/edge/src/db/index.ts:
- `DbHandle = { db: BetterSQLite3Database; sqlite: Database; ... }`
- `handle.sqlite.transaction(() => {...})()` is the rollback-on-throw wrapper.

From apps/edge/src/backup/daily.ts:
- BackupHandle shape: `{ runNow: () => Promise<...>; stop: () => void }`. Mirror for EventorHandle.

From apps/edge/src/routes/admin.ts:
- Pattern: `if (process.env['FARTOLA_DEV'] !== '1') return;` at the top of the registration function.
- FastifyInstance module declaration extends interface with `fartolaBackup?: BackupHandle | undefined`. Mirror with `fartolaEventor?: EventorHandle | undefined`.

From apps/edge/src/bin/fartola.ts (lines 507-518):
- After `await app.listen(...)`: `void lifecycle.start();` (SI bridge fire-and-forget).
- Then: `const backup = scheduleDailyBackup(handle, ...); app.fartolaBackup = backup;`. Mirror for eventor.

Wire shapes:
- EventorCompetitor: `{ person_id, family_name, given_name, birth_year, sex, club_id, si_card, emit_card, modify_date_ms }`. Used internally; not a REST DTO.
- EventorHandle: `{ runNow(): Promise<{ competitors: number; clubs: number; skipped?: boolean; reason?: string }>; stop(): void }`.

Schema column placement:
- `eventor_competitors` PK = `person_id INTEGER`; partial index on si_card; index on (family_name, given_name).
- `eventor_clubs` PK = `club_id INTEGER`.
- `meos_competitors` PK = `id INTEGER` (MeOS internal id); NO competition_id FK (global session state).
- `meos_classes` PK = `id INTEGER`; `name TEXT NOT NULL` (the JOIN key for D-MOP-3 auto-merge).
- `meos_clubs` PK = `id INTEGER`.
- `hired_cards` compound PK = `(competition_id TEXT NOT NULL FK, card_number INTEGER NOT NULL)`. Drizzle compound PK via `primaryKey({ columns: [...] })`.
- `competitors` ALTER: ADD COLUMN `source TEXT NOT NULL DEFAULT 'walkup'` (enum at TS layer: 'walkup' | 'entrylist' | 'meos').
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package legitimacy gate — verify saxes before install</name>
  <what-built>Nothing yet; this is the package legitimacy gate per RESEARCH §Package Legitimacy Audit. The researcher marked `saxes` as `[ASSUMED]` because slopcheck was unavailable. Executor MUST verify package origin BEFORE running `pnpm add saxes`.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/saxes in a browser.
    2. Confirm: maintainer is `lddubeau` (Louis-Dominique Dubeau), repository is github.com/lddubeau/saxes, weekly downloads > 5M, last publish within last 24 months.
    3. Run `npm view saxes version` and `npm view saxes repository.url` from the CLI; capture the version into the chat for the executor to pin.
    4. If saxes is NOT present or maintainer is unknown, type "fallback to sax@1.2.4" and the executor will use sax instead (RESEARCH A1 fallback).
  </how-to-verify>
  <resume-signal>Type "approved saxes@X.Y.Z" with the verified version, OR "fallback to sax@1.2.4".</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Drizzle schema extensions + generated migration for all 6 Phase 2.0 tables</name>
  <files>
    apps/edge/src/db/schema.ts,
    apps/edge/drizzle/0002_phase2.sql,
    apps/edge/drizzle/meta/0002_snapshot.json,
    apps/edge/drizzle/meta/_journal.json
  </files>
  <read_first>
    apps/edge/src/db/schema.ts (lines 156-289 for the classes/competitors/clubs/config table idioms — the exact analog),
    apps/edge/drizzle/0000_initial.sql (FK + cascade + partial unique index SQL shape — the migration analog),
    apps/edge/drizzle/0001_append_only_triggers.sql (hand-authored coexistence sample),
    apps/edge/drizzle/meta/_journal.json (so the new entry is appended correctly — DO NOT disturb existing entries; PATTERNS §6 + C-H1 note),
    .planning/phases/02-4-klubbs-mvp/02-PATTERNS.md §6 (db/schema.ts — extend),
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Recommended Project Structure" + §"Hyrbricka data shape" (D-HB-1 PK contract),
    .planning/phases/02-4-klubbs-mvp/02-CONTEXT.md D-HB-1 / D-MOP-1 / D-EV-1
  </read_first>
  <behavior>
    - Test 1 (`apps/edge/src/db/schema.test.ts` extension): after migrate, the SQLite schema includes tables eventor_competitors, eventor_clubs, meos_competitors, meos_classes, meos_clubs, hired_cards AND a `source` column on competitors with default 'walkup'.
    - Test 2: PK constraint on hired_cards is compound `(competition_id, card_number)` — inserting two rows with same card_number under DIFFERENT competition_id succeeds; same card_number under SAME competition_id fails with SQLITE_CONSTRAINT.
    - Test 3: partial index on eventor_competitors.si_card exists (verify via `PRAGMA index_list('eventor_competitors')`) with the partial-WHERE clause; idx on (family_name, given_name) also present.
    - Test 4: meos_* tables have NO competition_id column (Pattern 6 — global session state).
    - Test 5: existing competitors rows backfill source='walkup' after the migration (insert pre-2.0 row sans source → SELECT source → equals 'walkup').
  </behavior>
  <action>
    Extend `apps/edge/src/db/schema.ts` adding six new `sqliteTable()` definitions plus a `source` column on the existing `competitors` table.

    Per D-HB-1 / D-MOP-1 / Plan-2 PATTERNS map:

    eventorCompetitors — table 'eventor_competitors'. Columns: personId integer PK, familyName text NOT NULL, givenName text NOT NULL, birthYear integer NULLABLE, sex text NULLABLE (enum 'M'/'F' enforced at TS layer; SQL keeps text), clubId integer NULLABLE references eventorClubs.clubId (no cascade), siCard integer NULLABLE, emitCard integer NULLABLE, modifyDateMs integer NOT NULL. Indexes: partial uniqueIndex on siCard WHERE siCard IS NOT NULL named 'idx_eventor_si_card'; plain index on (familyName, givenName) named 'idx_eventor_name'. JSDoc must call out: "PII per REQ-PRIV-002 BUT NOT scrub-eligible — re-fetchable from Eventor any time; ADR-0009 records the trade-off."

    eventorClubs — table 'eventor_clubs'. Columns: clubId integer PK, name text NOT NULL, shortName text NULLABLE, mediaName text NULLABLE, parentId integer NULLABLE, modifyDateMs integer NOT NULL.

    meosCompetitors — table 'meos_competitors'. Columns: id integer PK (MeOS internal), name text NOT NULL, classId integer NULLABLE (MeOS class id), orgId integer NULLABLE (MeOS org id), statusCode integer NOT NULL DEFAULT 0, startTimeTenths integer NULLABLE, runningTimeTenths integer NULLABLE, bib text NULLABLE, cardNumber integer NULLABLE, lastMopUpdateMs integer NOT NULL. NO competition_id FK (per Pattern 6 — global session state).

    meosClasses — table 'meos_classes'. Columns: id integer PK, name text NOT NULL, ord integer NULLABLE, lastMopUpdateMs integer NOT NULL.

    meosClubs — table 'meos_clubs'. Columns: id integer PK, name text NOT NULL, nat text NULLABLE, lastMopUpdateMs integer NOT NULL.

    hiredCards — table 'hired_cards'. Columns: competitionId text NOT NULL references competitions.id with onDelete: 'cascade', cardNumber integer NOT NULL, markedAtMs integer NOT NULL, returnedAtMs integer NULLABLE, contactName text NULLABLE, contactPhone text NULLABLE, contactEmail text NULLABLE, note text NULLABLE. Compound PK via `primaryKey({ columns: [t.competitionId, t.cardNumber] })`. JSDoc: "PII per REQ-PRIV-002 — contact_name/phone/email/note are scrubbed by privacy/retention.ts (Plan 06 extension). marked_at_ms/returned_at_ms/card_number are PRESERVED audit trail."

    Extend existing `competitors` table with `source: text('source', { enum: ['walkup', 'entrylist', 'meos'] }).notNull().default('walkup')`. JSDoc: "D-MOP-3 — auto-merge sets 'meos'; entry-list import (Phase 1 Plan 5) sets 'entrylist'; walkup sets 'walkup'."

    AFTER schema edits, run `pnpm --filter @fartola/edge db:generate` to emit `apps/edge/drizzle/0002_phase2.sql` + `meta/0002_snapshot.json` + an appended `meta/_journal.json` entry. DO NOT hand-author the SQL — drizzle-kit is authoritative (RESEARCH §Don't Hand-Roll). The single exception is the 0001 trigger file (Phase 1 C-H1); leave it untouched.

    Extend `apps/edge/src/db/schema.test.ts` with the five tests above. Use the in-memory better-sqlite3 + the migrator from `apps/edge/src/db/migrate.ts` to apply migrations 0000+0001+0002 in a temp DB.
  </action>
  <verify>
    <automated>pnpm --filter @fartola/edge run db:generate &amp;&amp; pnpm --filter @fartola/edge run typecheck &amp;&amp; pnpm --filter @fartola/edge test --test-name-pattern="schema" 2&gt;&amp;1 | tail -50</automated>
  </verify>
  <done>
    Six new `sqliteTable()` calls plus the competitors.source column live in db/schema.ts. `apps/edge/drizzle/0002_phase2.sql` and `meta/0002_snapshot.json` exist; `meta/_journal.json` has exactly one new appended entry. All five schema tests pass. `pnpm typecheck` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Eventor saxes streaming parser + UTF-8/orphan/DOCTYPE fixtures</name>
  <files>
    apps/edge/package.json,
    apps/edge/src/eventor/parser.ts,
    apps/edge/src/eventor/parser.test.ts,
    apps/edge/src/eventor/__fixtures__/competitors-sample.xml,
    apps/edge/src/eventor/__fixtures__/clubs-sample.xml,
    apps/edge/src/eventor/__fixtures__/competitors-with-doctype.xml
  </files>
  <read_first>
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Pattern 1: saxes streaming parse — per-Competitor record emit" (the full code template + the synthetic fixture body at §Test strategy §Tiny synthetic cachedcompetitors.xml fixture),
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Pitfall 6: saxes UTF-8 streaming + cyrillic/non-Latin names",
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Landmines" entries for `cachedcompetitors` gzip and `<Competitor>` shape,
    .planning/research/eventor-api-smoke.md "Sample competitor element" + "Notes on the shape" (orphan Organisation rule, BirthDate YYYY-01-01, multi ControlCard),
    apps/edge/src/xml/parse.ts (lines 93-124 — T-FILE-IMPORT pre-flight prose to mirror in parser.ts)
  </read_first>
  <behavior>
    - Test 1: streamCompetitorsXml against competitors-sample.xml emits 3 EventorCompetitor records with person_id 1001/1002/1003.
    - Test 2: record[0] has si_card=8535005, club_id=637, sex='M', birth_year=1980, modify_date_ms=Date.parse('2024-12-12T09:46:45Z').
    - Test 3: record[1] has BOTH si_card=8303057 AND emit_card=530947 (multi-ControlCard handling) and Organisation absent → club_id=null (orphan rule).
    - Test 4: record[2].family_name='Östberg' AND record[2].given_name='Pär' — UTF-8 multi-byte chars preserved.
    - Test 5: streamCompetitorsXml against competitors-with-doctype.xml (a fixture whose first 200 bytes contain `<!DOCTYPE foo>`) throws Error matching /DOCTYPE not allowed/i.
    - Test 6: parseClubsXmlSync against clubs-sample.xml (a 3-club DOM-parse fixture) returns 3 EventorClub records with club_id 637/320/8.
  </behavior>
  <action>
    Install the package verified in Task 0. If saxes was approved: `pnpm --filter @fartola/edge add saxes@<verified-version>`. If fallback approved: `pnpm --filter @fartola/edge add sax@1.2.4` AND adjust the import in parser.ts to `import { parser as createSaxParser } from 'sax'` with the sax event-callback API (the streaming surface is API-equivalent for our purposes).

    Write `apps/edge/src/eventor/parser.ts` exporting:
      - `interface EventorCompetitor` with the field shape locked in interfaces above.
      - `interface EventorClub { club_id, name, short_name, media_name, parent_id, modify_date_ms }`.
      - `async function streamCompetitorsXml(path: string, onRecord: (rec: EventorCompetitor) => void): Promise<void>` — saxes-streaming, per-Competitor flush on closetag. Per the Pattern 1 template: maintain `current: Partial<EventorCompetitor> | null`, a `pathStack: string[]`, a `textBuf: string`, and an `activePunchingSystem: string | null`. Path-aware text assignment matching the closetag suffix (e.g. `Competitor/Person/Id` vs `Competitor/Organisation/Id`).
      - `function parseClubsXmlSync(path: string): EventorClub[]` — DOM parse via the existing Phase 1 XMLParser config (RESEARCH §Pattern 1 says clubs.xml at 1.3 MB is safe as DOM). Apply T-FILE-IMPORT pre-flight (DOCTYPE/ENTITY regex check on the source string).

    T-FILE-IMPORT mitigation in streamCompetitorsXml: read the first 512 bytes of the file synchronously, throw `Error('DOCTYPE not allowed')` if `/<!DOCTYPE/i.test(head)`, throw `Error('ENTITY declarations not allowed')` on ENTITY. Mirror xml/parse.ts:115-124. Pitfall 6 mitigation: open the stream WITHOUT `{ encoding: 'utf8' }` and let saxes decode buffers internally.

    Write three fixture files:
      - `competitors-sample.xml`: copy the body verbatim from RESEARCH §Test strategy §Tiny synthetic cachedcompetitors.xml fixture (3 competitors including UTF-8 Östberg and the orphan-no-Organisation Larsson row).
      - `clubs-sample.xml`: 3 ClubList entries with ids 637 (Stora Tuna OK), 320 (Sala OK), 8 (Dalarnas OF / parent). Shape per `https://eventor.orientering.se/api/schema` ClubList type.
      - `competitors-with-doctype.xml`: a 50-line fixture whose preamble includes a `<!DOCTYPE root [<!ENTITY x "y">]>` block to exercise the pre-flight reject path.

    Write `parser.test.ts` exercising the six behaviors above using `node:test` + `assert` per Phase 1 testing convention.
  </action>
  <verify>
    <automated>pnpm --filter @fartola/edge test --test-name-pattern="parser" 2&gt;&amp;1 | tail -40</automated>
  </verify>
  <done>
    parser.ts compiles; all six parser tests pass; fixtures are committed; saxes (or sax) is pinned in apps/edge/package.json with the version captured from Task 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Eventor cache + download module, transactional snapshot replace</name>
  <files>
    apps/edge/src/eventor/download.ts,
    apps/edge/src/eventor/cache.ts,
    apps/edge/src/eventor/cache.test.ts
  </files>
  <read_first>
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Pattern 2: Batched INSERT inside sqlite.transaction() (252 919 rows)" (the full ingestEventorCache template — copy verbatim),
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Landmines" entries for Eventor URL exact-match and gzip,
    .planning/research/eventor-api-smoke.md §"Download pipeline" (download URL + ApiKey header shape),
    apps/edge/src/ingest/entryImport.ts (lines 59-173 — outer-transaction template; same `(handle, ..., opts) → result` shape),
    apps/edge/src/eventor/parser.ts (Task 2 output — the input interface)
  </read_first>
  <behavior>
    - Test 1: `downloadEventorPayloads({ apiKey, fetchImpl })` against an in-test mock fetch that returns gzipped sample bytes resolves to two tempfile paths (competitors.xml + clubs.xml) with the gunzipped content equal to the fixture XML.
    - Test 2: downloadEventorPayloads with apiKey=undefined throws Error matching /missing api key/i — without making any HTTP call.
    - Test 3: downloadEventorPayloads URL exactly matches `https://eventor.orientering.se/api/export/cachedcompetitors?includePreselectedClasses=false&zip=true&version=3.0` AND sends `ApiKey: <key>` header (intercept via mock fetch — Landmine "MeOS Eventor download uses eventorBase + iofExportVersion").
    - Test 4: `ingestEventorCache(handle, competitorsPath, clubsPath, nowMs)` on a fresh in-memory DB inserts all 3 fixture competitors + all 3 fixture clubs, writes `config[eventor_cache_refreshed_at_ms]=nowMs`, returns `{ competitors: 3, clubs: 3 }`.
    - Test 5: ingestEventorCache called twice replaces the snapshot (TRUNCATE+INSERT semantics — first call inserts 3, second call with a different fixture of 2 records leaves exactly 2 rows; no merge artifacts).
    - Test 6: ingestEventorCache with a parser that throws midway rolls back the transaction — the prior snapshot remains intact AND the config marker is NOT updated.
  </behavior>
  <action>
    Write `apps/edge/src/eventor/download.ts` exporting:
      - `interface EventorDownloadOpts { apiKey: string | undefined; fetchImpl?: typeof fetch; tmpDir?: string; baseUrl?: string }`.
      - `async function downloadEventorPayloads(opts: EventorDownloadOpts): Promise<{ competitorsPath: string; clubsPath: string }>`. Throws on missing apiKey BEFORE issuing any HTTP request (D-EV-3 fallback). Constructs the URL using a baseUrl default of `https://eventor.orientering.se/api/` plus the exact suffix `export/cachedcompetitors?includePreselectedClasses=false&zip=true&version=3.0` (Landmine — must match MeOS verbatim) and a second URL `export/clubs?version=3.0`. Sends `ApiKey: <opts.apiKey>` header. Receives gzipped response; pipes through `node:zlib` `createGunzip()` into a tempfile under `opts.tmpDir ?? os.tmpdir()` named `eventor-competitors-<pid>-<random>.xml` (and similarly for clubs). Returns both paths.

    Write `apps/edge/src/eventor/cache.ts` exporting:
      - `async function ingestEventorCache(handle: DbHandle, competitorsXmlPath: string, clubsXmlPath: string, nowMs: number): Promise<{ competitors: number; clubs: number }>` — copy the Pattern 2 template verbatim (RESEARCH lines 449-512). Key invariants:
        * Stream all competitors via parser.streamCompetitorsXml into an in-memory array (the bench laptop has the headroom; this is the simpler-error-handling branch the research explicitly endorses).
        * Parse clubs via parser.parseClubsXmlSync (DOM is fine at 1.3 MB).
        * Wrap the snapshot replace in a SINGLE `handle.sqlite.transaction(() => { ... })()` so a thrown parse error rolls back the wipe — the prior cache survives.
        * Inside the transaction: `DELETE FROM eventor_competitors` and `DELETE FROM eventor_clubs` first, then bulk-insert clubs (FK target for competitors.club_id), then competitors in 1000-row batches via Drizzle's `.insert(table).values(chunk).run()`.
        * Final write inside the transaction: upsert config row `key='eventor_cache_refreshed_at_ms'` with `value=String(nowMs)` via onConflictDoUpdate.

    Write `cache.test.ts` exercising the six behaviors above. Use `apps/edge/src/db/migrate.ts` to set up an in-memory DB with 0000+0001+0002 migrations applied. Mock fetch via a closure that returns a `Response` with `.body` as a gzipped ReadableStream of the fixture bytes; use `node:zlib.gzipSync(readFileSync(fixturePath))` to build the test gzip.

    DO NOT call `bin/fartola.ts` from any test — cache + download are pure modules.
  </action>
  <verify>
    <automated>pnpm --filter @fartola/edge test --test-name-pattern="cache|download" 2&gt;&amp;1 | tail -60</automated>
  </verify>
  <done>
    cache.ts + download.ts compile; all six tests pass; the rollback test (test 6) verifies the transaction-atomic snapshot replace; URL string matches Landmine "MeOS Eventor download" suffix exactly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: scheduleEventorBoot + bin/fartola.ts wiring + admin refresh route</name>
  <files>
    apps/edge/src/eventor/boot.ts,
    apps/edge/src/eventor/boot.test.ts,
    apps/edge/src/bin/fartola.ts,
    apps/edge/src/routes/admin.ts,
    apps/edge/src/routes/admin.test.ts
  </files>
  <read_first>
    .planning/phases/02-4-klubbs-mvp/02-PATTERNS.md §2 (apps/edge/src/eventor/boot.ts pattern map — the `{ runNow, stop }` handle + the admin route binding + the FastifyInstance decoration),
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Pitfall 5: Eventor download blocks bridge boot" (D-EV-3 fire-and-forget — never await),
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"UI signaling — recommendation: TweaksPanel indicator + log line" (i18n keys live in Plan 02; here we only need the log line + admin endpoint),
    .planning/phases/02-4-klubbs-mvp/02-CONTEXT.md D-EV-1 / D-EV-2 / D-EV-3 (decisions cited in must_haves),
    apps/edge/src/backup/daily.ts (BackupHandle shape — mirror as EventorHandle),
    apps/edge/src/routes/admin.ts (lines 1-100 — the FARTOLA_DEV gate + admin-route binding template),
    apps/edge/src/bin/fartola.ts (lines 449-580 — the BridgeLifecycle / scheduleDailyBackup wiring AFTER app.listen)
  </read_first>
  <behavior>
    - Test 1 (boot.test.ts): scheduleEventorBoot with fresh cache (config has `eventor_cache_refreshed_at_ms` = nowMs - 1 day) → runNow resolves to `{ skipped: true, reason: 'fresh' }` AND does NOT invoke the injected `downloadFn`.
    - Test 2: scheduleEventorBoot with stale cache (8 days old) → runNow resolves to `{ competitors: N, clubs: M, skipped: false }` AND calls the injected downloadFn + ingestFn exactly once.
    - Test 3: scheduleEventorBoot with EMPTY cache (no config row) → runNow refreshes (same path as stale; reason: 'empty').
    - Test 4: scheduleEventorBoot with downloadFn that rejects (network error) → runNow resolves WITHOUT throwing (D-EV-3 "warn + run with what we have"); logger.warn was called; returns `{ skipped: true, reason: 'network_error', error }`.
    - Test 5: scheduleEventorBoot with apiKey=undefined → runNow short-circuits to `{ skipped: true, reason: 'no_key' }` BEFORE calling downloadFn.
    - Test 6 (admin.test.ts): with FARTOLA_DEV=1 + a mock fartolaEventor decoration, POST /api/__admin/eventor/refresh returns 200 with body `{ ok: true, competitors: N, clubs: M }`; with FARTOLA_DEV unset the route returns 404.
  </behavior>
  <action>
    Write `apps/edge/src/eventor/boot.ts` exporting:
      - `interface EventorBootOpts { apiKey: string | undefined; logger: FastifyBaseLogger; downloadFn?: typeof downloadEventorPayloads; ingestFn?: typeof ingestEventorCache; staleThresholdMs?: number; nowFn?: () => number }` — every external dep injectable for tests.
      - `interface EventorHandle { runNow(): Promise<EventorBootResult>; stop(): void }` where EventorBootResult is `{ competitors?: number; clubs?: number; skipped: boolean; reason?: 'fresh' | 'empty' | 'no_key' | 'network_error'; error?: unknown }`.
      - `function scheduleEventorBoot(handle: DbHandle, opts: EventorBootOpts): EventorHandle`.

    Behavior of runNow():
      1. If `opts.apiKey == null`: log `"Eventor: nyckel saknas (EVENTOR_API_KEY) — fallback to firmware hint"` at info level; return `{ skipped: true, reason: 'no_key' }`.
      2. Else read `config.value` for `key='eventor_cache_refreshed_at_ms'`. If present AND `now - parseInt(value) < staleThresholdMs` (default 7 * 86400000): log `"Eventor: cache <N> dagar gammal — skipping refresh"`; return `{ skipped: true, reason: 'fresh' }`.
      3. Else call `downloadFn({ apiKey })` (or the injected one). On reject: log `"Eventor: refresh failed"` at warn level; return `{ skipped: true, reason: 'network_error', error }` WITHOUT throwing.
      4. On resolve, call `ingestFn(handle, competitorsPath, clubsPath, nowFn())`; log the row counts; return `{ skipped: false, competitors, clubs }`.

    `stop()` is a no-op for boot.ts (no timer to cancel — D-EV-1 explicitly REJECTED cron; admin button is the only re-trigger). Kept in the API for parity with BackupHandle so the admin route binding is uniform.

    Wire into `apps/edge/src/bin/fartola.ts` AFTER the existing `app.fartolaBackup = backup; app.fartolaRetention = retention;` block (around line 518):

      `const eventorApiKey = process.env['EVENTOR_API_KEY'];`
      `const eventor = scheduleEventorBoot(handle, { apiKey: eventorApiKey, logger: app.log });`
      `app.fartolaEventor = eventor;`
      `void eventor.runNow().catch((err) => app.log.warn({ err }, 'eventor boot failed'));`

    The `void` + `.catch` makes this fire-and-forget (Pitfall 5 mitigation — never blocks app.listen). Add `eventor.stop()` to the shutdown handler alongside `backup.stop()` and `retention.stop()`.

    Extend `apps/edge/src/routes/admin.ts`:
      - Add `fartolaEventor?: EventorHandle | undefined` to the `declare module 'fastify' { interface FastifyInstance { ... } }` block.
      - Inside the FARTOLA_DEV gate, register `app.post('/api/__admin/eventor/refresh', ...)`. Handler: if `!app.fartolaEventor` → 200 `{ ok: false, error: 'no_eventor' }`. Else call `await app.fartolaEventor.runNow()` and return 200 with the result spread.

    Write `boot.test.ts` (5 tests above) using an in-memory DB seeded with the migrations from Task 1 and the config row pre-set/absent as the test requires. Inject downloadFn + ingestFn as test doubles. Write `admin.test.ts` extension adding the 6th test using Fastify's `app.inject({ method: 'POST', url: '/api/__admin/eventor/refresh' })`.
  </action>
  <verify>
    <automated>pnpm --filter @fartola/edge test --test-name-pattern="boot|admin" 2&gt;&amp;1 | tail -50 &amp;&amp; pnpm --filter @fartola/edge run typecheck</automated>
  </verify>
  <done>
    boot.ts compiles; all 5 boot tests + admin extension test pass; bin/fartola.ts wires app.fartolaEventor with fire-and-forget; admin route returns the runNow result behind FARTOLA_DEV.
  </done>
</task>

<task type="auto">
  <name>Task 5: ADR-0009 + REQUIREMENTS.md REQ-EXT-MEOS-001 entry</name>
  <files>
    .planning/adr/0009-eventor-runner-cache.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    .planning/adr/0008-pii-in-append-only-event-log.md (the MADR template + the "residual exposure mitigated by 1/2/3" structure — Plan 1 PATTERNS §12 says use this verbatim),
    .planning/research/eventor-api-smoke.md §"Privacy + Eventor terms-of-service considerations" (the trade-off body text — paraphrase, don't quote literally),
    .planning/REQUIREMENTS.md (line 175 onward — the "Standards and interop" section that REQ-EXT-MEOS-001 belongs near; the file is sorted REQ-ID-grouped, so the new entry slots after REQ-STD-008),
    .planning/phases/02-4-klubbs-mvp/02-CONTEXT.md `<canonical_refs>` (D-EV-1/2/3 — drive the ADR's "Decision Drivers" bullets)
  </read_first>
  <action>
    Write `.planning/adr/0009-eventor-runner-cache.md` titled "Eventor löpardatabasen cached locally for walk-up autocomplete". Frontmatter `status: accepted`, `date: 2026-05-17`, `decision-makers: [Jonas Hagberg]`, `consulted: [parallel research agent, MeOS source review]`. Sections per ADR-0008's template:

      - **Context and Problem Statement** (2-3 paragraphs): MeOS-style walk-up autocomplete needs national runner DB lookup by SI card. Eventor exposes `/api/export/cachedcompetitors` open to club keys; 252 919 competitors with 96 918 SI cards. Caching this locally on the bridge laptop is materially more PII than Phase 1's local model (Phase 1 only knew the active event's roster); Eventor's ToS explicitly endorses client-side caching.

      - **Decision Drivers**: REQ-STD-004 (Eventor integration), REQ-OPS-001 (no internet required), REQ-PRIV-002 (30-day retention), D-EV-2 (7-day staleness), D-EV-3 (warn + run offline), Eventor ToS ("members fetched once per day is plenty"), MeOS-as-superset philosophy (TabCompetition.cpp:3107-3108 uses the same endpoint).

      - **Considered Options**:
        * **A. Inline lookup via Eventor API at walk-up time** — rejected: REQ-OPS-001 (offline must work) + per-walkup latency.
        * **B. Weekly cache of national DB, local-only** — chosen.
        * **C. Per-club subset via `/persons/organisations/<id>`** — rejected: 384 names/club, no national coverage, no SI cards on that endpoint.

      - **Decision Outcome**: Chosen B. Pattern: 0002_phase2.sql adds `eventor_competitors` + `eventor_clubs` tables; bridge boot fires `runOnce()` if cache > 7 days old; admin button forces refresh; offline boot keeps prior cache and surfaces a UI indicator (Plan 02 owns the indicator UI).

      The residual exposure (252 919 names + birth years + SI cards on bridge laptop) is mitigated by:
        1. **Local-only**: the cache never leaves the laptop. MIP `<entry>` (Plan 03) only carries actively-registered competitors, not the cache.
        2. **No phone/email**: deliberately uses `/cachedcompetitors` (no contact details) instead of `/persons/organisations/<id>?includeContactDetails=true`. Verified by parallel agent grep — no `<Tele>` / `<Contact>` in the payload.
        3. **Clear-cache admin endpoint**: `/api/__admin/eventor/refresh` reloads from Eventor; a future companion `clear-cache` endpoint can be added if needed.
        4. **Re-fetchable**: the cache is NOT event-log data (lives outside the append-only domain). Deleting + re-fetching is safe and has no retention conflict.
        5. **Disk encryption**: standard apps/edge/README.md guidance applies (operator responsibility).

      - **Consequences**: Good — instant walk-up autocomplete for any SI-card-carrying Swedish runner; matches MeOS UX; respects Eventor ToS. Bad — adds ~20 MB SQLite footprint; introduces a (small) PII surface on the bridge laptop; bench laptop must run the ingest on first boot (~5 s per Pattern 2 assumption A7).

      - **Confirmation**: apps/edge/src/eventor/cache.ts header comment block documents the trade-off; ADR-0009 cross-referenced from REQ-STD-004 and REQ-OPS-001 entries (the latter via the D-EV-3 link).

      - **More Information**: REQ-STD-004 + REQ-OPS-001 + REQ-PRIV-002; implementation `apps/edge/src/eventor/`; originating .planning/research/eventor-api-smoke.md.

    Extend `.planning/REQUIREMENTS.md` adding a NEW section between "## Standards and interop" and "## Operations and reliability" titled "## External integration". Insert one entry:

      **REQ-EXT-MEOS-001 (v1)** — MeOS coexistence: serve MIP `<entry>` polls so a parallel MeOS install receives fartOLa walk-up registrations; accept MOP `<MOPComplete>` / `<MOPDiff>` POSTs so MeOS-side state can be reconciled into fartOLa on bridge restart. Closed LAN; no auth (revisit for sanctioned events in Phase 2.1).

    Do NOT renumber existing REQ-IDs (the file's invariant says "REQ-IDs are stable and never renumbered"). The new section preserves ID stability.
  </action>
  <verify>
    <automated>test -f .planning/adr/0009-eventor-runner-cache.md &amp;&amp; grep -q '^status: accepted' .planning/adr/0009-eventor-runner-cache.md &amp;&amp; grep -q '^# Eventor' .planning/adr/0009-eventor-runner-cache.md &amp;&amp; grep -q 'REQ-EXT-MEOS-001' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'External integration' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    ADR-0009 exists with the MADR shape; REQUIREMENTS.md has a new "External integration" section containing REQ-EXT-MEOS-001 (v1).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Eventor API → bridge | gzipped XML from eventor.orientering.se; HTTPS but no integrity check beyond TLS |
| bridge filesystem → parser | downloaded XML lives on disk briefly; treated as untrusted bytes (T-FILE-IMPORT same as Phase 1) |
| FARTOLA_DEV admin route → bridge | operator-controlled; gated by env var; LAN-reachable when `--allow-lan` set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | eventor/parser.ts — XXE via DOCTYPE/ENTITY in downloaded XML | mitigate | 512-byte head pre-flight rejecting `<!DOCTYPE` / `<!ENTITY` regex; saxes does NOT expand entities by spec (RESEARCH Pattern 1 SECURITY block) |
| T-02-02 | Denial of Service | eventor/cache.ts — billion-laughs explosion via expanded entities | mitigate | T-02-01 mitigation covers this AND saxes streaming caps memory at O(1) per record regardless of input size |
| T-02-03 | Info Disclosure | eventor_competitors PII (252 919 rows) | accept w/ mitigations | ADR-0009 captures the trade-off; local-only; no phone/email; clear-cache endpoint (Task 4); disk encryption is operator-side per apps/edge/README.md |
| T-02-04 | Tampering | bin/fartola.ts — Eventor refresh blocks startup → operator force-kills bridge mid-write | mitigate | Pitfall 5 mitigation — `void runOnce().catch(...)` after app.listen, NEVER awaited (D-EV-3) |
| T-02-05 | Elevation of Privilege | admin /api/__admin/eventor/refresh exposed without auth | accept w/ gate | FARTOLA_DEV env gate (mirrors Phase 1 admin pattern); Phase 2.1 replaces with admin-token auth per REQ-AUTH-* roadmap |
| T-02-06 | Denial of Service | network failure during boot Eventor fetch hangs bridge for 30s default HTTP timeout | mitigate | downloadFn has explicit `AbortController` timeout set to 60 s; boot fire-and-forget means hang impacts only the cache, not the bridge |
| T-02-SC | Tampering | npm install of `saxes` package | mitigate | Task 0 blocking-human checkpoint verifies origin (lddubeau/saxes); slopcheck unavailable; fallback to sax@1.2.4 acceptable |
</threat_model>

<verification>
- `pnpm --filter @fartola/edge run db:generate` produces 0002_phase2.sql without errors.
- `pnpm --filter @fartola/edge run typecheck` exits 0.
- `pnpm --filter @fartola/edge test` exits 0 (all new tests pass; Phase 1 tests still pass).
- `EVENTOR_API_KEY= node --import tsx apps/edge/src/bin/fartola.ts --port 3000 --db-path :memory: --no-bridge` boots within 3 seconds (no Eventor blocking); log shows "Eventor: nyckel saknas".
- With a valid EVENTOR_API_KEY and the bench laptop online, `runNow()` completes in <60 s and writes ~252 k rows (manual bench check before Wednesday).
</verification>

<success_criteria>
- All schema migrations applied cleanly to a fresh DB.
- Eventor parser handles UTF-8, orphan-no-club, and DOCTYPE-rejection paths.
- Boot wiring is non-blocking; missing EVENTOR_API_KEY or network errors degrade gracefully.
- ADR-0009 and REQ-EXT-MEOS-001 entry exist; ROADMAP gap closed.
</success_criteria>

<output>
Create `.planning/phases/02-4-klubbs-mvp/02-01-SUMMARY.md` when done.
</output>
