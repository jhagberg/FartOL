---
phase: 02-4-klubbs-mvp
plan: 05
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - apps/edge/src/routes/hiredCards.ts
  - apps/edge/src/routes/hiredCards.test.ts
  - apps/edge/src/routes/readout.ts
  - apps/edge/src/routes/readout.test.ts
  - apps/edge/src/server.ts
  - packages/shared-types/src/dtos.ts
  - apps/web/src/lib/api/client.ts
  - apps/web/src/lib/components/HyrbrickaToast.svelte
  - apps/web/src/lib/screens/ReadoutView.svelte
  - apps/web/src/lib/screens/ActiveHyrbrickorView.svelte
  - apps/web/src/routes/competition/[id]/hyrbrickor/+page.svelte
  - apps/web/src/lib/layout/Sidebar.svelte
  - apps/web/src/lib/i18n/sv.json
  - apps/web/src/lib/i18n/en.json
  - apps/web/e2e/hyrbricka.spec.ts
autonomous: true
tags: [hyrbricka, readout, toast, returnerad, admin-view, ui, rest, e2e]
requirements:
  - REQ-PRIV-002
  - REQ-EVT-CMP-004
  - REQ-UI-003
must_haves:
  truths:
    - 'GET /api/competitions/:id/hired-cards returns an array of open + recently-returned hired_cards entries with contact_*, marked_at_ms, returned_at_ms'
    - 'PATCH /api/competitions/:id/hired-cards/:cardNumber/return sets returned_at_ms = now() and returns 200; second PATCH returns 200 with already_returned=true (idempotent)'
    - 'GET /api/competitions/:id/readout response includes hired_card_open: { contact_name, contact_phone, contact_email, note } | null per history row (single source of truth — no per-card extra fetch)'
    - 'On a card_read for a card with an open hired_cards row, ReadoutView shows the HyrbrickaToast with contact info + a Returnerad button'
    - 'Clicking Returnerad PATCHes the hired card AND dismisses the toast AND adds the card to a returnedHiredCardNumbers Set so the toast does NOT re-pop on a subsequent read'
    - 'ActiveHyrbrickorView at /competition/[id]/hyrbrickor lists all open rentals (returned_at_ms IS NULL) with contact info + a Returnerad button per row'
    - "Sidebar has a 'Hyrbrickor' nav item visible only when the active competition has at least one hired rental"
    - 'Playwright e2e covers walkup → finish-readout → HyrbrickaToast appears → click Returnerad → next read does NOT re-pop the toast'
  artifacts:
    - path: 'apps/edge/src/routes/hiredCards.ts'
      provides: 'Fastify plugin: GET list + PATCH return endpoint; reuses Plan-01 hiredCards table'
      min_lines: 60
    - path: 'apps/web/src/lib/components/HyrbrickaToast.svelte'
      provides: 'Toast component rendering ⚠️ Hyrbricka header + contact info + Returnerad button'
      min_lines: 50
    - path: 'apps/web/src/lib/screens/ActiveHyrbrickorView.svelte'
      provides: 'Admin backstop view listing all open + returned rentals for a competition'
      min_lines: 60
  key_links:
    - from: 'apps/web/src/lib/screens/ReadoutView.svelte'
      to: 'apps/web/src/lib/components/HyrbrickaToast.svelte'
      via: 'card_read triggers pendingHyrbrickaToast state when hired_card_open is non-null AND !returnedHiredCardNumbers.has(cardNumber)'
      pattern: 'pendingHyrbrickaToast|HyrbrickaToast'
    - from: 'apps/edge/src/routes/readout.ts'
      to: 'apps/edge/src/db/schema.ts hiredCards'
      via: 'LEFT JOIN hiredCards ON (competitionId, cardNumber) returning open-rental fields'
      pattern: 'hiredCards|hired_card_open'
    - from: 'apps/edge/src/routes/hiredCards.ts'
      to: 'apps/edge/src/db/schema.ts hiredCards'
      via: 'PATCH UPDATE SET returnedAtMs WHERE competitionId AND cardNumber'
      pattern: 'returnedAtMs|returned_at_ms'
---

<objective>
Wave 2 plan — depends on Plan 02-01 (hired_cards table) and Plan 02-02
(walkup writes hired_cards rows). Parallel-safe with Plan 04 on the edge
(different routes; both touch ReadoutView.svelte but Plan 04 only adds a
case to handleWs and Plan 05 adds toast UI + extends handleWs differently —
these merge cleanly; if conflict, Plan 04 lands first since this plan
depends on Plan 04's WS envelope contract for the meos_merge case).

Delivers the **Hyrbricka return UX** per D-HB-1 / D-HB-2:

- D-HB-1 (Plan-01 schema already done): junction table hired_cards with
  contact info MeOS lacks.
- D-HB-2: "Returnerad" button at finish-readout + admin "Aktiva hyrbrickor"
  view as backstop for end-of-event reconciliation.

Purpose:

- Operator hands the rental card back at finish-readout; one tap marks it
  returned and the toast doesn't re-pop on subsequent reads.
- Admin view exists for the end-of-event "who didn't return?" pass.
- The finish-readout toast surfaces contact info (phone/email) so operators
  can chase down non-returners during the event.

Output:

- New REST endpoints under /api/competitions/:id/hired-cards/ (list + return).
- Extended /api/competitions/:id/readout response with hired_card_open per row.
- HyrbrickaToast.svelte component (red urgency styling, contact info).
- ReadoutView.svelte handler additions (pendingHyrbrickaToast state + Set-based
  dismissal).
- ActiveHyrbrickorView.svelte + a SvelteKit route at
  /competition/[id]/hyrbrickor.
- Sidebar nav item conditional on open-rental presence.
- i18n keys for the toast + view + button.
- Playwright e2e covering the happy path.
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-4-klubbs-mvp/02-CONTEXT.md
@.planning/phases/02-4-klubbs-mvp/02-RESEARCH.md
@.planning/phases/02-4-klubbs-mvp/02-PATTERNS.md
@.planning/phases/02-4-klubbs-mvp/02-01-PLAN.md
@.planning/phases/02-4-klubbs-mvp/02-02-PLAN.md
@apps/edge/src/db/schema.ts
@apps/edge/src/routes/readout.ts
@apps/edge/src/routes/competitors.ts
@apps/edge/src/server.ts
@apps/web/src/lib/screens/ReadoutView.svelte
@apps/web/src/lib/components/ConsentConfirmationToast.svelte
@apps/web/src/lib/layout/Sidebar.svelte
@apps/web/src/lib/api/client.ts
@packages/shared-types/src/dtos.ts

<interfaces>
From Plan 02-01:
- hiredCards table (compound PK [competitionId, cardNumber], markedAtMs, returnedAtMs nullable, contactName/Phone/Email/note).

From Plan 02-02:

- POST /api/competitors with hired_card=true writes the hired_cards row in the same transaction.

REST shapes added in this plan:

- GET /api/competitions/:id/hired-cards → 200 { open: Array<HiredCardRow>; returned: Array<HiredCardRow> } where HiredCardRow = { competition_id, card_number, marked_at_ms, returned_at_ms, contact_name, contact_phone, contact_email, note }.
- PATCH /api/competitions/:id/hired-cards/:cardNumber/return → 200 { ok: true, returned_at_ms: number, already_returned?: boolean }; 404 if not found.

readout.ts response extension (additive — backward compatible):

- Each history row gains an optional `hired_card_open: { contact_name, contact_phone, contact_email, note } | null` field; non-null when the card_number for that row has an open hired_cards entry (returnedAtMs IS NULL) in this competition.

WS envelope (additive to Phase 1's readoutChannel):

- { type: 'hired_card_returned', payload: { card_number, returned_at_ms } } — emitted by PATCH handler AFTER commit; ReadoutView optionally reacts (already-dismissed via Set, so the WS receipt is informational).

i18n keys (sv + en):

- readout.hyrbricka.title: "⚠️ Hyrbricka — be om att få tillbaka brickan!" / "⚠️ Hired card — please ask for it back!"
- readout.hyrbricka.contact.name / .phone / .email / .note
- readout.hyrbricka.returned: "Returnerad" / "Returned"
- readout.hyrbricka.dismiss: "Ignorera" / "Dismiss"
- hyrbrickor.title: "Aktiva hyrbrickor" / "Active hired cards"
- hyrbrickor.empty: "Inga öppna hyrbrickor" / "No open hired cards"
- hyrbrickor.markedAt: "Hyrd kl. {{time}}" / "Hired at {{time}}"
- hyrbrickor.returnedAt: "Återlämnad kl. {{time}}" / "Returned at {{time}}"
- nav.hyrbrickor: "Hyrbrickor" / "Hired cards"

Reactivity nuance (RESEARCH §Plan 5 nuance + Assumption A8):

- returnedHiredCardNumbers: Set<number> in $state(). MUTATION via .add() may NOT trigger Svelte 5 reactivity reliably; use replacement form: `returnedHiredCardNumbers = new Set([...returnedHiredCardNumbers, cardNumber])`.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hiredCards.ts route (GET list + PATCH return) + readout.ts extension</name>
  <files>
    apps/edge/src/routes/hiredCards.ts,
    apps/edge/src/routes/hiredCards.test.ts,
    apps/edge/src/routes/readout.ts,
    apps/edge/src/routes/readout.test.ts,
    apps/edge/src/server.ts,
    packages/shared-types/src/dtos.ts
  </files>
  <read_first>
    apps/edge/src/routes/readout.ts (existing GET /readout shape — Plan 09 of Phase 1; identifies the history row shape + the JOIN strategy; we add a LEFT JOIN onto hiredCards),
    apps/edge/src/routes/competitors.ts (lines 211-243 — sqlite.transaction pattern + PATCH endpoint shape; pattern for PATCH /api/competitors/:id),
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Pattern 6: ReadoutView Hyrbricka toast" lines 1116-1122 (the rationale for putting hired_card_open in /readout response instead of a per-card extra fetch — single source of truth),
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Plan 5 — Hyrbricka toast on ReadoutView" lines 1704-1709 (runtime nuance — PATTERNS S-4 broadcast-after-commit; readout.ts is the data source not the WS emitter for hired-card state),
    .planning/phases/02-4-klubbs-mvp/02-CONTEXT.md D-HB-1 / D-HB-2 (junction PK shape; finish-readout EXISTS query pattern; admin view backstop)
  </read_first>
  <behavior>
    - Test 1 (hiredCards.test.ts): GET /api/competitions/:id/hired-cards on a competition with 2 open + 1 returned rentals returns { open: [2 rows], returned: [1 row] } with all contact fields populated.
    - Test 2: PATCH /api/competitions/:id/hired-cards/12345/return on an open rental sets returned_at_ms = now() and returns 200 { ok: true, returned_at_ms: <ms> }.
    - Test 3: PATCH again on the same card returns 200 { ok: true, already_returned: true, returned_at_ms: <original ms> } — idempotent; the existing timestamp is preserved.
    - Test 4: PATCH on a non-existent card returns 404 { error: 'not_found' }.
    - Test 5: PATCH on a card from a DIFFERENT competition returns 404 (composite PK isolation).
    - Test 6 (readout.test.ts): GET /api/competitions/:id/readout on a competition where one card_read history row's card is in an open hired_cards row returns that row's payload including `hired_card_open: { contact_name, contact_phone, contact_email, note }`. Rows for cards with NO open rental have `hired_card_open: null` (explicit null, not absent).
    - Test 7 (PATTERNS S-4 broadcast): PATCH /api/competitions/:id/hired-cards/12345/return triggers wsBroadcast on readoutChannel with `{ type: 'hired_card_returned', payload: { card_number: 12345, returned_at_ms: <ms> } }` AFTER the transaction commits.
  </behavior>
  <action>
    Write apps/edge/src/routes/hiredCards.ts exporting registerHiredCardsRoutes(app: FastifyInstance): Promise<void>:

      GET /api/competitions/:id/hired-cards
        - Params: `{ id: string }` (competition id, UUID — Zod validate).
        - SELECT * FROM hired_cards WHERE competition_id = :id; partition into open (returned_at_ms IS NULL) and returned arrays; ORDER BY marked_at_ms DESC.
        - Return 200 { open: HiredCardRow[], returned: HiredCardRow[] }.

      PATCH /api/competitions/:id/hired-cards/:cardNumber/return
        - Params: `{ id: string; cardNumber: string }` (cardNumber as coerced int).
        - Pre-flight: SELECT the row WHERE competitionId=:id AND cardNumber=:cardNumber. If missing → 404 { error: 'not_found' }.
        - If returnedAtMs is already set → return 200 { ok: true, already_returned: true, returned_at_ms: existing }.
        - Else inside sqlite.transaction: UPDATE hired_cards SET returnedAtMs = now (a function reference passed via opts.testClock?.now ?? Date.now — same pattern as retention.ts:96) WHERE competitionId AND cardNumber.
        - After commit (PATTERNS S-4): app.wsBroadcast(readoutChannel(id), { type: 'hired_card_returned', payload: { card_number, returned_at_ms } }); app.projectionStore.markDirty(id) is OPTIONAL (no projection depends on hired_cards in 2.0; skip).
        - Return 200 { ok: true, returned_at_ms }.

    Extend apps/edge/src/routes/readout.ts (Phase 1 — already exists):
      - To each history-row result, append the field `hired_card_open: { contact_name, contact_phone, contact_email, note } | null`.
      - Computation: a single LEFT JOIN onto hired_cards ON (hired_cards.competition_id = readout.competition_id AND hired_cards.card_number = history_row.card_number AND hired_cards.returned_at_ms IS NULL). Drizzle's leftJoin is the canonical shape.
      - If the joined row is present, populate the field; else null. JSON serialization preserves null explicitly so the web client can branch on `hired_card_open !== null` without `in`-checks.

    Extend packages/shared-types/src/dtos.ts adding:
      - HiredCardRow Zod schema + inferred type.
      - HiredCardsListResponse Zod schema.
      - Extend ReadoutHistoryRow with `hired_card_open: z.object({ contact_name, contact_phone, contact_email, note }).nullable()` (additive).

    Wire in apps/edge/src/server.ts: `import registerHiredCardsRoutes from './routes/hiredCards.ts';` + `await app.register(registerHiredCardsRoutes);`.

    Write hiredCards.test.ts (5 + 1 broadcast test = 6 tests) and extend readout.test.ts with the new field shape test (1 test). Use the in-memory DB + Plan-01 migrations + seed hired_cards rows directly via drizzle. Mock wsBroadcast for Test 7 via a spy.

  </action>
  <verify>
    <automated>pnpm --filter @fartol/edge test --test-name-pattern="hiredCards|readout" 2&gt;&amp;1 | tail -60</automated>
  </verify>
  <done>
    hiredCards.ts compiles; all 7 tests pass; readout.ts gains hired_card_open field; shared-types DTOs export the new shapes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HyrbrickaToast.svelte component + ReadoutView integration + Set-based dismissal</name>
  <files>
    apps/web/src/lib/components/HyrbrickaToast.svelte,
    apps/web/src/lib/screens/ReadoutView.svelte,
    apps/web/src/lib/api/client.ts,
    apps/web/src/lib/i18n/sv.json,
    apps/web/src/lib/i18n/en.json
  </files>
  <read_first>
    apps/web/src/lib/components/ConsentConfirmationToast.svelte (the structural analog — Plan 5 RESEARCH calls it out; this Hyrbricka toast follows the same component-with-callback-onResolved pattern),
    apps/web/src/lib/screens/ReadoutView.svelte (existing pendingConsentToast pattern lines 416-450 — the closest analog for the Set-based dismissal; copy structure for pendingHyrbrickaToast),
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Pattern 6: ReadoutView Hyrbricka toast (extends C-M4 consent toast pattern)" — the full design,
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Plan 5 — Hyrbricka toast on ReadoutView" lines 1704-1709 (runtime nuance — Svelte 5 Set mutation gotcha + WS subscription template),
    .planning/phases/02-4-klubbs-mvp/02-CONTEXT.md D-HB-2 (Returnerad button + admin view)
  </read_first>
  <behavior>
    - Test 1: rendering HyrbrickaToast with props { cardNumber, contactName, contactPhone, contactEmail, note, onReturn, onDismiss } shows the localized title; contact fields visible only when non-null; tapping Returnerad invokes onReturn(cardNumber); tapping Ignorera invokes onDismiss(cardNumber).
    - Test 2: ReadoutView receives a readout history row with hired_card_open non-null AND card_number=12345 AND !returnedHiredCardNumbers.has(12345) → pendingHyrbrickaToast becomes { cardNumber:12345, contactName:..., contactPhone:..., contactEmail:..., note:... }; the toast renders.
    - Test 3: clicking Returnerad in the toast calls the api client (mocked), then sets returnedHiredCardNumbers via `returnedHiredCardNumbers = new Set([...returnedHiredCardNumbers, 12345])` AND dismisses the toast (pendingHyrbrickaToast = null).
    - Test 4: a subsequent readout row for card 12345 with hired_card_open still non-null (e.g. WS race) does NOT re-pop the toast because returnedHiredCardNumbers.has(12345) is true.
    - Test 5: clicking Ignorera dismisses without API call; subsequent reads do NOT re-pop (also via Set).
    - Test 6: when the WS envelope hired_card_returned arrives for a card already in the Set, it is a no-op (informational only); when it arrives for a card NOT in the Set (e.g. another operator returned it via the admin view), it ADDs the card to the Set so future reads don't re-pop.
  </behavior>
  <action>
    Write apps/web/src/lib/components/HyrbrickaToast.svelte (Svelte 5 runes):
      - Props: `{ cardNumber: number; contactName: string | null; contactPhone: string | null; contactEmail: string | null; note: string | null; onReturn: (cardNumber: number) => void | Promise<void>; onDismiss: (cardNumber: number) => void }`.
      - Render with red-urgency styling (use existing CSS variables from app-shell; pattern: copy ConsentConfirmationToast.svelte's structure but apply a `--urgent` class with red border + bold header). Title from t('readout.hyrbricka.title').
      - Show contact_phone (with `tel:` link), contact_email (with `mailto:` link), contact_name (plain text), note (plain text). Hide rows whose value is null.
      - Two buttons in the footer: `<button data-testid="hyrbricka-return" onclick={...}>Returnerad</button>` + `<button data-testid="hyrbricka-dismiss" onclick={...}>Ignorera</button>`.

    Extend apps/web/src/lib/api/client.ts:
      - `async function listHiredCards(competitionId: string): Promise<HiredCardsListResponse>` — GET /api/competitions/:id/hired-cards.
      - `async function returnHiredCard(competitionId: string, cardNumber: number): Promise<{ ok: true; returned_at_ms: number; already_returned?: boolean }>` — PATCH /api/competitions/:id/hired-cards/:cardNumber/return.

    Extend apps/web/src/lib/screens/ReadoutView.svelte per RESEARCH Pattern 6:
      - Add `let pendingHyrbrickaToast: { cardNumber: number; contactName: string | null; contactPhone: string | null; contactEmail: string | null; note: string | null } | null = $state(null);`
      - Add `let returnedHiredCardNumbers: Set<number> = $state(new Set());`
      - In the card_read side-effect path (existing place where pendingConsentToast is set):
        - Read the matching readout history row's hired_card_open field.
        - If hired_card_open !== null AND !returnedHiredCardNumbers.has(cardNumber) AND pendingHyrbrickaToast === null:
          - Set pendingHyrbrickaToast = { cardNumber, ...hired_card_open fields }.
      - Render in the template after the existing pendingConsentToast block: `{#if pendingHyrbrickaToast} <HyrbrickaToast {...pendingHyrbrickaToast} onReturn={onHyrbrickaReturn} onDismiss={onHyrbrickaDismiss} /> {/if}`.
      - Define handlers:
        - `async function onHyrbrickaReturn(cardNumber: number) { try { await returnHiredCard(competitionId, cardNumber); } catch (e) { toast(t('err.network')); return; } returnedHiredCardNumbers = new Set([...returnedHiredCardNumbers, cardNumber]); pendingHyrbrickaToast = null; toast(t('readout.hyrbricka.returnedConfirmed')); }`.
        - `function onHyrbrickaDismiss(cardNumber: number) { returnedHiredCardNumbers = new Set([...returnedHiredCardNumbers, cardNumber]); pendingHyrbrickaToast = null; }`.
      - Extend handleWs dispatch with case 'hired_card_returned': `returnedHiredCardNumbers = new Set([...returnedHiredCardNumbers, env.payload.card_number]);` (no-op if already present; defensive add covers the cross-operator case).

    Add i18n keys to sv.json + en.json:
      - readout.hyrbricka.title: "⚠️ Hyrbricka — be om att få tillbaka brickan!" / "⚠️ Hired card — please ask for it back!"
      - readout.hyrbricka.contact.name: "Kontakt:" / "Contact:"
      - readout.hyrbricka.contact.phone: "Telefon:" / "Phone:"
      - readout.hyrbricka.contact.email: "E-post:" / "Email:"
      - readout.hyrbricka.contact.note: "Anteckning:" / "Note:"
      - readout.hyrbricka.returned: "Returnerad" / "Returned"
      - readout.hyrbricka.dismiss: "Ignorera" / "Dismiss"
      - readout.hyrbricka.returnedConfirmed: "Brickan markerad som återlämnad" / "Card marked as returned"

    Write Vitest tests for the 6 behaviors. Use @testing-library/svelte for component rendering; mock the api client at the module boundary.

  </action>
  <verify>
    <automated>pnpm --filter @fartol/web test --run --testPathPattern="(HyrbrickaToast|ReadoutView)" 2&gt;&amp;1 | tail -50</automated>
  </verify>
  <done>
    HyrbrickaToast.svelte + ReadoutView extensions compile; all 6 tests pass; Set-based dismissal verified to prevent re-pop; api client extensions present.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ActiveHyrbrickorView.svelte + SvelteKit route + Sidebar nav item</name>
  <files>
    apps/web/src/lib/screens/ActiveHyrbrickorView.svelte,
    apps/web/src/routes/competition/[id]/hyrbrickor/+page.svelte,
    apps/web/src/lib/layout/Sidebar.svelte,
    apps/web/src/lib/i18n/sv.json,
    apps/web/src/lib/i18n/en.json
  </files>
  <read_first>
    apps/web/src/lib/screens/ResultsView.svelte (analog — list view with per-row data; the layout/styling baseline for ActiveHyrbrickorView),
    apps/web/src/routes/competition/[id]/+layout.svelte AND any sibling +page.svelte (the SvelteKit routing pattern for competition-scoped pages),
    apps/web/src/lib/layout/Sidebar.svelte (existing nav item structure — copy for the Hyrbrickor entry; identify the conditional-visibility pattern from any existing example such as Export view visibility)
  </read_first>
  <behavior>
    - Test 1 (ActiveHyrbrickorView mount): renders title "Aktiva hyrbrickor"; calls listHiredCards on mount; renders the open list with contact info + Returnerad button per row.
    - Test 2: empty state — open=[] renders "Inga öppna hyrbrickor".
    - Test 3: clicking Returnerad on a row PATCHes the card AND re-renders without that row in the open list.
    - Test 4: returned list shows recently-returned cards with returned_at_ms timestamp formatted via the existing time helper.
    - Test 5: Sidebar shows the "Hyrbrickor" nav item only when the active competition's hired card count > 0 (read from a store or a one-off fetch — accept a polling fetch every 30s for 2.0; cheap).
  </behavior>
  <action>
    Write apps/web/src/lib/screens/ActiveHyrbrickorView.svelte:
      - Props: `{ competitionId: string }`.
      - $state: `let open: HiredCardRow[] = $state([]); let returned: HiredCardRow[] = $state([]); let loading = $state(true); let error: string | null = $state(null);`
      - On mount via $effect: call listHiredCards(competitionId); on resolve set open + returned + loading=false; on reject set error.
      - Template: title from t('hyrbrickor.title'); two sections (Open + Returned); each section renders a list of cards with Hyrd kl. <time>, contact info inline, Returnerad button per open row (calls returnHiredCard then refetches).
      - Use AppShell + Card components from Phase 1 for visual consistency.

    Write apps/web/src/routes/competition/[id]/hyrbrickor/+page.svelte:
      - Read the competition id from $page.params.id (SvelteKit's load function pattern).
      - Render `<ActiveHyrbrickorView competitionId={id} />`.

    Extend apps/web/src/lib/layout/Sidebar.svelte:
      - Add a nav item for Hyrbrickor pointing to /competition/<activeCompetitionId>/hyrbrickor. Visibility: read from a new module-level state or an additive store; the simplest implementation is to listen to the WS hired_card_marked + hired_card_returned envelopes and maintain a count in a store. For 2.0 simplicity: do an unconditional render of the nav item when on a competition page (gate on `$page.params.id` being set); the page's empty-state message handles the "no rentals" case gracefully.

    Add i18n keys (sv + en):
      - nav.hyrbrickor: "Hyrbrickor" / "Hired cards"
      - hyrbrickor.title: "Aktiva hyrbrickor" / "Active hired cards"
      - hyrbrickor.empty: "Inga öppna hyrbrickor" / "No open hired cards"
      - hyrbrickor.openSection: "Öppna" / "Open"
      - hyrbrickor.returnedSection: "Återlämnade" / "Returned"
      - hyrbrickor.markedAt: "Hyrd kl. {{time}}" / "Hired at {{time}}"
      - hyrbrickor.returnedAt: "Återlämnad kl. {{time}}" / "Returned at {{time}}"

    Write Vitest tests for the 5 behaviors using @testing-library/svelte. Mock the api client.

  </action>
  <verify>
    <automated>pnpm --filter @fartol/web test --run --testPathPattern="(ActiveHyrbrickor|Sidebar)" 2&gt;&amp;1 | tail -40</automated>
  </verify>
  <done>
    ActiveHyrbrickorView + the SvelteKit route + Sidebar nav item compile; all 5 tests pass; new i18n keys present.
  </done>
</task>

<task type="auto">
  <name>Task 4: Playwright e2e — full Hyrbricka walkup → readout → Returnerad flow</name>
  <files>
    apps/web/e2e/hyrbricka.spec.ts
  </files>
  <read_first>
    .planning/phases/02-4-klubbs-mvp/02-RESEARCH.md §"Hyrbricka E2E (Playwright)" lines 1877-1890 (the full spec body — adapt verbatim),
    apps/web/e2e/walkup.spec.ts (Phase 1 walkup e2e — bench-fixture seeding patterns and WalkupModal selectors),
    apps/web/e2e/results.spec.ts (the create-competition + import-course helper pattern),
    apps/edge/src/routes/dev.ts (the simulate-read endpoint we trigger to inject card_read events)
  </read_first>
  <action>
    Write apps/web/e2e/hyrbricka.spec.ts covering the Plan 02-02 + Plan 02-05 round-trip:

      1. Seed: POST /api/competitions to create competition; POST /api/competitions/<id>/import with the 4-klubbs courseData fixture (.reference/2026-05-20 4-klubbs_coursedata.xml).
      2. Navigate to /competition/<id>/readout?walkup=88888 (or open walkup modal via the UI affordance).
      3. Fill walkup form: name='Test Runner', klubb='Test OK', bana='Vit', check Hyrbricka, fill phone='0701234567'.
      4. Click Spara → expect modal to close → expect GET /api/competitions/<id>/hired-cards to return one open rental for card 88888 with contact_phone='0701234567'.
      5. Trigger a card read for 88888 via POST /api/__dev/simulate-read (Phase 1 dev route).
      6. Expect [data-testid="hyrbricka-return"] to be visible AND the toast contains the text '0701234567'.
      7. Click [data-testid="hyrbricka-return"] → expect the toast to disappear → GET /api/competitions/<id>/hired-cards → expect returned_at_ms set for card 88888.
      8. Trigger ANOTHER simulate-read for card 88888 → expect [data-testid="hyrbricka-return"] to remain ABSENT (Set-based dismissal honored).
      9. Navigate to /competition/<id>/hyrbrickor → expect the card in the Returned section.

    Use Playwright `request` API for seeding (no UI dependency for steps 1, 5, 7-check). Use deterministic timeouts (no arbitrary sleeps; use locator.waitFor or expect.poll).

    Reuse the Phase 1 webServer config in playwright.config.ts.

  </action>
  <verify>
    <automated>pnpm exec playwright test apps/web/e2e/hyrbricka.spec.ts --reporter=line 2&gt;&amp;1 | tail -50</automated>
  </verify>
  <done>
    hyrbricka.spec.ts passes all 9 steps on 3 consecutive runs (no flakes); covers the full Plan 02-02 + Plan 02-05 user journey.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                   | Description                                                    |
| ---------------------------------------------------------- | -------------------------------------------------------------- |
| browser → /api/competitions/:id/hired-cards                | LAN client; PII surface — contact_phone, contact_email visible |
| PATCH /api/competitions/:id/hired-cards/:cardNumber/return | LAN client; no auth in 2.0 (Phase 1 posture)                   |
| HyrbrickaToast → onReturn callback                         | client-side action; server is authoritative on returned_at_ms  |

## STRIDE Threat Register

| Threat ID | Category        | Component                                                                | Disposition | Mitigation Plan                                                                                                                                                                         |
| --------- | --------------- | ------------------------------------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02P5-01 | Info Disclosure | hired*cards.contact*\* leak via /api/competitions/:id/hired-cards        | accept      | LAN-only; Phase 1 posture; PII retention scrub (Plan 06) removes contact\_\* after 30 days                                                                                              |
| T-02P5-02 | Tampering       | malicious PATCH marks all rentals returned without the operator's action | accept      | Same LAN trust as Phase 1 walk-up route                                                                                                                                                 |
| T-02P5-03 | Privacy         | contact_phone/email visible on the readout big-screen overlay            | mitigate    | HyrbrickaToast renders inline (not on big-screen); the toast lives in the operator-only view; document in Plan 06 playbook that screen-shared views should NOT include the toast region |
| T-02P5-04 | Integrity       | Svelte 5 Set mutation via .add() may not trigger reactivity              | mitigate    | Per RESEARCH A8 — use replacement form `new Set([...prev, x])` everywhere (Task 2 action note)                                                                                          |
| T-02P5-05 | Tampering       | hired_card_returned WS envelope spoofed by malicious WS client           | accept      | Phase 1 WS posture — no auth on WS; LAN-only; impact bounded to UI dismissal (server still enforces returned_at_ms)                                                                     |
| T-02P5-06 | Tampering       | PATCH return on non-existent card returns 404                            | mitigate    | Test 4 of Task 1 verifies                                                                                                                                                               |
| T-02P5-07 | Tampering       | XSS via contact_name containing script tag                               | mitigate    | Svelte text interpolation auto-escapes; no `{@html}` introduced; PATTERNS check                                                                                                         |
| T-02P5-SC | Tampering       | no new npm dependencies                                                  | mitigate    | Reuses Phase 1 stack                                                                                                                                                                    |

</threat_model>

<verification>
- pnpm --filter @fartol/edge test --test-name-pattern="hiredCards|readout" exits 0.
- pnpm --filter @fartol/web test --run exits 0.
- pnpm exec playwright test apps/web/e2e/hyrbricka.spec.ts exits 0 (3 consecutive runs).
- Manual smoke: walkup with Hyrbricka checked + phone filled; simulate card read; toast appears with phone; click Returnerad; subsequent read does not re-pop.
- Manual smoke: navigate to /competition/<id>/hyrbrickor → see the rental in the Returned section.
</verification>

<success_criteria>

- Operator sees the Hyrbricka toast on finish-readout with contact info.
- One tap marks the card returned and prevents re-popping.
- Admin view at /competition/:id/hyrbrickor lists open + returned rentals.
- All 3 D-HB decisions verifiable.
- Playwright e2e covers the full happy path.
  </success_criteria>

<output>
Create `.planning/phases/02-4-klubbs-mvp/02-05-SUMMARY.md` when done.
</output>
