---
phase: 01-single-laptop-training-mvp
plan: 08
type: execute
wave: 3
depends_on: [03, 04, 06, 07]
files_modified:
  - apps/edge/src/routes/results.ts
  - apps/edge/src/routes/results.test.ts
  - apps/edge/src/projection/store.ts
  - apps/edge/src/projection/store.test.ts
  - apps/edge/src/projection/loader.ts
  - apps/edge/src/server.ts
  - apps/edge/src/si/bridge.ts
  - apps/edge/src/si/bridge.test.ts
  - apps/edge/src/routes/dev.ts
  - apps/edge/src/routes/competitors.ts
  - apps/edge/src/ws/index.ts
  - apps/edge/src/ws/index.test.ts
autonomous: true
tags: [websocket, results, projection-store, live-update]
requirements:
  - REQ-EVT-CMP-007
  - REQ-EVT-003
must_haves:
  truths:
    - 'GET /api/competitions/:id/results returns ResultView[] grouped by class, computed via reduce() on the current event log'
    - 'WebSocket channel results:<id> receives a results_update envelope after every event that could change the projection — but only when an active competition is set'
    - 'Re-emit logic: a card_read or manual_dnf or un_dnf or card_bound triggers a debounced (50ms) reduce + broadcast'
    - 'results_update envelope payload: { class_id, rows: ResultView[] } (one envelope per affected class; full set on first hello-replay)'
    - 'Hello-replay on results:<id> emits EXACTLY ONE results_full envelope (C-M1). Raw `replay`-type envelopes are NEVER emitted on a results: channel — that wire shape is exclusive to readout: channels (plan 03 locked the per-channel-kind branch; plan 08 fills in the results_full body).'
    - 'ProjectionStore caches the last computed state per competition_id; loadCompetitionInputs reads competitors+classes+courses+events once and reduce() runs incrementally'
    - 'Bridge calls projectionStore.markDirty ONLY when getActiveCompetitionId() returns a non-null id. With no active competition, neither WS broadcast (plan 06) nor markDirty (plan 08) fire.'
  artifacts:
    - path: 'apps/edge/src/routes/results.ts'
      provides: 'GET /api/competitions/:id/results REST handler'
      contains: 'results'
    - path: 'apps/edge/src/projection/store.ts'
      provides: 'ProjectionStore class — keeps last reduce() output per competition; markDirty + recompute API'
      min_lines: 60
    - path: 'apps/edge/src/projection/loader.ts'
      provides: 'loadCompetitionInputs(handle, competitionId) -> ReduceInput by querying DB'
      contains: 'loadCompetitionInputs'
  key_links:
    - from: 'apps/edge/src/si/bridge.ts'
      to: 'apps/edge/src/projection/store.ts'
      via: 'Inside event handlers: const id = opts.getActiveCompetitionId(); if (id !== null) opts.projectionStore.markDirty(id); — store schedules debounced recompute + broadcast on results:<id>'
      pattern: 'markDirty'
    - from: 'apps/edge/src/routes/competitors.ts'
      to: 'apps/edge/src/projection/store.ts'
      via: 'After successful POST /api/competitors with card_number, markDirty so projection re-runs'
      pattern: 'markDirty'
    - from: 'apps/edge/src/ws/index.ts'
      to: 'apps/edge/src/projection/store.ts'
      via: 'C-M1: hello handler on a results: channel emits ONE results_full envelope (NEVER a replay envelope); plan 03 stubbed the branch, plan 08 fills in the body'
      pattern: 'results_full'
---

<objective>
Land the live-results pipeline. Plan 07 produced the pure reducer; plan 08 wires it into both REST (GET /api/competitions/:id/results) and WS broadcast (results:<id> channel) with a small ProjectionStore that caches the last computed state and recomputes on event log mutations.

Honors:

- REQ-EVT-CMP-007: live HTML results page on localhost during event.
- UI-SPEC §"Live results auto-update": LOCKED contract — every card_read re-derives projection server-side, push `results_update` message to subscribed clients, `.new` row flash 4s on the just-added row (client-side flag).
- ADR-0003 + REQ-EVT-003: derived state from log, never persisted.
- Plan 06's no-phantom-channel contract: with no active competition, bridge fires neither wsBroadcast nor markDirty. Plan 08 extends `BridgeOpts` with `projectionStore` and embeds the explicit conditional shown in the key_links pattern.
- **Codex C-M1 (MEDIUM):** Plan 03 locked the per-channel-kind hello-replay branch — `replay`-type envelopes are EXCLUSIVE to `readout:` channels. Plan 08 lifts the `results:` stub into a real `results_full` emission. The contract — never emit `replay` on a `results:` channel — survives plan 08 (the existing stub branch is upgraded to send `results_full`, not changed to permit `replay`). The C-M1 regression gate in `apps/edge/src/ws/index.test.ts` test 5 is amended to additionally assert exactly ONE `results_full` envelope is received on a `results:` hello AND ZERO `replay` envelopes (plan 03's invariant remains).

Output: GET /api/competitions/:id/results, WS results:<id> publishes results_update on every relevant event AND results_full on hello (C-M1), ProjectionStore with markDirty + debounced recompute, integration tests proving an end-to-end card_read → results_update flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md
@.planning/phases/01-single-laptop-training-mvp/01-PATTERNS.md
@.planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md
@.planning/phases/01-single-laptop-training-mvp/01-REVIEWS.md
@.planning/phases/01-single-laptop-training-mvp/01-03-PLAN.md
@.planning/phases/01-single-laptop-training-mvp/01-04-PLAN.md
@.planning/phases/01-single-laptop-training-mvp/01-06-PLAN.md
@.planning/phases/01-single-laptop-training-mvp/01-07-PLAN.md

<interfaces>
From plan 07: `reduce(input: ReduceInput): CompetitionState`, `CompetitorView`, `ResultView`, `CompetitionState`.
From plan 02: schema + internal row types in apps/edge/src/db/types.ts (C-H5 boundary).
From plan 03: app.wsBroadcast, WsClient pattern, **hello-replay per-channel-kind branch (C-M1 stub)**. The plan-03 results: stub is a no-op that emits zero `replay` envelopes; plan 08 lifts it to emit one `results_full`.
From plan 04: GET /api/competitions/:id/courses (returns courses with embedded control_codes ordered).
From plan 06: app.activeCompetitionId, attachBridge wiring, BridgeOpts surface (getActiveCompetitionId getter). Plan 06 LOCKED that the bridge skips wsBroadcast when getActiveCompetitionId() returns null — plan 08 extends BridgeOpts to add projectionStore and applies the SAME conditional.

**WS envelope shapes (LOCKED here for results channel; aligned with plan 03's per-channel-kind contract):**

- `readout:<id>` channels carry: `card_read | card_inserted | card_removed | card_bound | frame_error | connection_changed | replay`. RAW EVENT REPLAY (`type: 'replay'`) is EXCLUSIVE to readout:.
- `results:<id>` channels carry: `results_full | results_update`. NEVER `replay`.
- `{ type: 'results_update'; channel: 'results:<id>'; payload: { class_id: string; class_name: string; rows: ResultView[] }; seq: number }` — per-class envelope after every relevant event.
- `{ type: 'results_full'; channel: 'results:<id>'; payload: { classes: Array<{ class_id: string; class_name: string; rows: ResultView[] }>; pending_unknown_cards: number[] }; seq: number }` — sent ONCE on hello as the full state replay (cheaper than re-walking events).

Debounce rationale: a real Tuesday training never produces > 1 card_read/sec, but the reducer is fast (< 200ms for 1000 events per plan 07). Debounce 50ms protects against bursts.

ProjectionStore API:

```
export interface ProjectionStore {
  get(competitionId: string): CompetitionState | null;
  markDirty(competitionId: string): void;
  recomputeNow(competitionId: string): CompetitionState;
  dispose(): void;
}
export function createProjectionStore(opts: {
  handle: DbHandle;
  broadcast: (channel: ChannelName, envelope: { type: string; payload: unknown; seq: number }) => void;
  debounceMs?: number;
}): ProjectionStore;
```

markDirty schedules a setTimeout(0) -> recomputeNow + broadcast IF not already pending. Tests inject debounceMs=0 for synchronous assertions.

Bridge integration (plan 06 + plan 08):

- Plan 06 ships BridgeOpts with `handle`, `nodeId`, `getActiveCompetitionId`, `broadcast`. Plan 08 EXTENDS BridgeOpts to add `projectionStore: ProjectionStore` (plan 15 adds more — printerSink, getCompetition).
- Inside each event handler in apps/edge/src/si/bridge.ts, the integration code is the explicit conditional:
  ```
  const id = opts.getActiveCompetitionId();
  if (id !== null) opts.projectionStore.markDirty(id);
  ```

**Hello-replay on results: channels (C-M1 contract — plan 03 stubbed, plan 08 fills in):**

Plan 03's apps/edge/src/ws/index.ts has the per-channel-kind branch:

```
for (const ch of state.channels) {
  if (channelKind(ch) === 'readout') {
    // emit `replay` envelopes from replayChannel(...)
  } else {
    // C-M1: results: channel — plan 03 stub: emit NOTHING.
    // Plan 08 fills in: emit ONE `results_full` envelope.
  }
}
```

Plan 08 lifts the results: branch:

```
} else {
  // C-M1: results: channel hello. NEVER emit `replay` envelopes here.
  // Emit exactly one `results_full` envelope carrying the current projection state.
  const competitionId = ch.slice('results:'.length);
  let projection = app.projectionStore.get(competitionId);
  if (!projection) projection = app.projectionStore.recomputeNow(competitionId);
  if (projection) {
    const classes = app.fartolaDb.db.select().from(classesTable).where(eq(classesTable.competitionId, competitionId)).all();
    const payload = {
      classes: classes.map(c => ({ class_id: c.id, class_name: c.name, rows: projection!.results_by_class.get(c.id) ?? [] })),
      pending_unknown_cards: projection.pending_unknown_cards,
    };
    socket.send(JSON.stringify({ type: 'results_full', channel: ch, payload, seq: projection.last_event_seq }));
  }
  // If projection is null (unknown competition), emit NOTHING — never fall through to a replay path.
}
```

Critically: the executor MUST NOT add a fallback that emits `replay` envelopes on the results: branch under any condition. If the projection lookup fails, emit nothing and log to stderr; the client's hello succeeds (the WS stays open) but no state is replayed. The contract from plan 03 is preserved: zero `replay` envelopes on a results: channel.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProjectionStore + loader + REST /api/competitions/:id/results</name>
  <files>
    apps/edge/src/projection/store.ts,
    apps/edge/src/projection/store.test.ts,
    apps/edge/src/projection/loader.ts,
    apps/edge/src/routes/results.ts,
    apps/edge/src/routes/results.test.ts,
    apps/edge/src/server.ts
  </files>
  <read_first>
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Pattern 4: WebSocket channels with hello-replay reconnect",
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Pattern 5: Pure-reducer projection",
    .planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md §"Live results auto-update",
    apps/edge/src/projection/reduce.ts (plan 07),
    apps/edge/src/projection/index.ts barrel,
    apps/edge/src/db/schema.ts
  </read_first>
  <action>
    apps/edge/src/projection/loader.ts — pull ReduceInput from DB. Single function:
    ```
    import { eq, and, sql, asc } from 'drizzle-orm';
    import { events, competitions, classes, courses, course_controls, controls, competitors } from '../db/schema.ts';
    import type { DbHandle } from '../db/index.ts';
    import type { ReduceInput } from './reduce.ts';

    export function loadCompetitionInputs(handle: DbHandle, competitionId: string): ReduceInput | null {
      const competition = handle.db.select().from(competitions).where(eq(competitions.id, competitionId)).get();
      if (!competition) return null;
      const competitorsRows = handle.db.select().from(competitors).where(eq(competitors.competitionId, competitionId)).all();
      const classesRows = handle.db.select().from(classes).where(eq(classes.competitionId, competitionId)).all();
      const coursesRows = handle.db.select().from(courses).where(eq(courses.competitionId, competitionId)).all();
      const coursesWithCodes = coursesRows.map(c => {
        const codes = handle.db
          .select({ code: controls.code, orderIdx: course_controls.orderIdx })
          .from(course_controls).innerJoin(controls, eq(course_controls.controlId, controls.id))
          .where(eq(course_controls.courseId, c.id))
          .orderBy(asc(course_controls.orderIdx))
          .all();
        return { ...c, control_codes: codes.map(r => r.code) };
      });
      // INTENTIONAL: events with competition_id = NULL (recorded by an
      // idle bridge before an active competition was set) are EXCLUDED.
      // The reducer is per-competition; orphan reads are not retroactively
      // pulled in by event_time_ms heuristics because that would let
      // pre-event bench / test card reads leak into a real competition.
      // Operators register competitors through the wizard before the
      // first card read for a competition. Revisit only if a concrete
      // operator workflow needs the merge.
      const eventsRows = handle.db.select().from(events).where(eq(events.competitionId, competitionId)).orderBy(asc(events.eventTimeMs), asc(events.localSeq)).all();
      return { competition_id: competitionId, events: eventsRows, competitors: competitorsRows, classes: classesRows, courses: coursesWithCodes };
    }
    ```

    apps/edge/src/projection/store.ts (unchanged from prior revision — debounce + cache + recompute + broadcast):
    ```
    import { reduce } from './reduce.ts';
    import { loadCompetitionInputs } from './loader.ts';
    import type { DbHandle } from '../db/index.ts';
    import type { CompetitionState } from './types.ts';
    import { resultsChannel, type ChannelName } from '@fartola/shared-types';

    export interface ProjectionStore {
      get(competitionId: string): CompetitionState | null;
      markDirty(competitionId: string): void;
      recomputeNow(competitionId: string): CompetitionState | null;
      dispose(): void;
    }

    export function createProjectionStore(opts: {
      handle: DbHandle;
      broadcast: (channel: ChannelName, envelope: { type: string; payload: unknown; seq: number }) => void;
      debounceMs?: number;
    }): ProjectionStore {
      const debounceMs = opts.debounceMs ?? 50;
      const cache = new Map<string, CompetitionState>();
      const pending = new Map<string, NodeJS.Timeout>();
      let disposed = false;

      function recomputeNow(competitionId: string): CompetitionState | null {
        const input = loadCompetitionInputs(opts.handle, competitionId);
        if (!input) return null;
        const next = reduce(input);
        cache.set(competitionId, next);
        for (const [classId, rows] of next.results_by_class) {
          const cls = input.classes.find(c => c.id === classId);
          opts.broadcast(resultsChannel(competitionId), { type: 'results_update', payload: { class_id: classId, class_name: cls?.name ?? '', rows }, seq: next.last_event_seq });
        }
        return next;
      }

      return {
        get(id) { return cache.get(id) ?? null; },
        markDirty(id) {
          if (disposed) return;
          if (pending.has(id)) return;
          const handle = setTimeout(() => { pending.delete(id); recomputeNow(id); }, debounceMs);
          pending.set(id, handle);
        },
        recomputeNow,
        dispose() { disposed = true; for (const t of pending.values()) clearTimeout(t); pending.clear(); },
      };
    }
    ```

    Update apps/edge/src/server.ts to construct the ProjectionStore in buildServer and decorate `app.projectionStore`.

    apps/edge/src/routes/results.ts — REST handler (unchanged from prior revision).

    apps/edge/src/projection/store.test.ts — node:test (unchanged 4 tests from prior revision).
    apps/edge/src/routes/results.test.ts — node:test (unchanged 3 tests).

    Apply PATTERNS S-1 header to every new .ts file.

  </action>
  <verify>
    <automated>cd /home/jonas/src/fartOLa-phase-1 &amp;&amp; pnpm --filter @fartola/edge typecheck &amp;&amp; pnpm --filter @fartola/edge test 2>&amp;1 | grep -E '(pass|fail) [0-9]+|projection|results'</automated>
  </verify>
  <done>
    ProjectionStore + loader + GET /api/competitions/:id/results all working. Debounce coalesces markDirty calls. All Task-1 tests pass (~7 new tests).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire markDirty into SI bridge + simulate-read + POST /api/competitors + lift plan-03 results: stub into results_full emission (C-M1) + no-active-competition + no-raw-replay-on-results regression tests</name>
  <files>
    apps/edge/src/si/bridge.ts,
    apps/edge/src/si/bridge.test.ts,
    apps/edge/src/routes/dev.ts,
    apps/edge/src/routes/competitors.ts,
    apps/edge/src/ws/index.ts,
    apps/edge/src/ws/index.test.ts,
    apps/edge/src/projection/store.test.ts
  </files>
  <read_first>
    apps/edge/src/si/bridge.ts (plan 06 — extend BridgeOpts and add the conditional markDirty),
    apps/edge/src/si/bridge.test.ts (plan 06 — extend with markDirty spy assertion),
    apps/edge/src/routes/dev.ts (plan 03 + plan 06 — same conditional),
    apps/edge/src/routes/competitors.ts (plan 04 — markDirty after walk-up),
    apps/edge/src/ws/index.ts (plan 03 — lift the per-channel-kind results: stub into a results_full emission),
    apps/edge/src/ws/index.test.ts (plan 03 — amend test 5 to assert exactly one results_full AND zero replay on results: hello),
    .planning/phases/01-single-laptop-training-mvp/01-REVIEWS.md §C-M1 (results: hello replay envelope leak — must emit only results_full),
    .planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md §"Live results auto-update"
  </read_first>
  <action>
    Update apps/edge/src/si/bridge.ts BridgeOpts to add `projectionStore: ProjectionStore`. After every `insertEvent` call in the 5 event handlers, apply the LOCKED conditional pattern:
    ```
    const id = opts.getActiveCompetitionId();
    if (id !== null) opts.projectionStore.markDirty(id);
    ```
    Refactor handlers to share a `maybeBroadcastAndMarkDirty(type, payload, seq, markDirty)` helper:
    ```
    function maybeBroadcastAndMarkDirty(type: string, payload: unknown, seq: number, markDirty: boolean) {
      const id = opts.getActiveCompetitionId();
      if (id === null) return;             // no active competition → no broadcast, no markDirty (B-2 contract)
      opts.broadcast(readoutChannel(id), { type, payload, seq });
      if (markDirty) opts.projectionStore.markDirty(id);
    }
    ```
    Per-event markDirty policy:
    - card_inserted: markDirty=true.
    - card_read: markDirty=true.
    - card_removed: markDirty=false.
    - frame_error: markDirty=false.
    - connection_changed: markDirty=false.

    Update apps/edge/src/routes/dev.ts simulate-read: after insertEvent, `if (body.competition_id) app.projectionStore.markDirty(body.competition_id);`.

    Update apps/edge/src/routes/competitors.ts POST handler: after card_bound event commits, `app.projectionStore.markDirty(body.competition_id)`.

    **C-M1 — Update apps/edge/src/ws/index.ts hello handler.** Plan 03 lands a per-channel-kind branch with a NO-OP stub for results: channels (zero envelopes emitted). Plan 08 LIFTS the stub into a results_full emission. The contract from plan 03 — never emit a `replay` envelope on a results: channel — is preserved:
    ```
    // Inside the hello-message handler, after parsing hello.channels:
    for (const ch of state.channels) {
      if (channelKind(ch) === 'readout') {
        // Plan 03 emitted `replay` envelopes via replayChannel. Unchanged here.
        const replayed = replayChannel(app.fartolaDb, ch, hello.last_seen_seq, app.fartolaNodeId);
        for (const row of replayed) {
          socket.send(JSON.stringify({ type: 'replay', channel: ch, payload: row.payload, seq: row.seq }));
        }
      } else {
        // C-M1 LOCKED: results: channel. NEVER emit `replay` envelopes here.
        // Emit exactly one `results_full` envelope carrying the current projection state.
        const competitionId = ch.slice('results:'.length);
        let projection = app.projectionStore.get(competitionId);
        if (!projection) projection = app.projectionStore.recomputeNow(competitionId);
        if (projection) {
          const classesRows = app.fartolaDb.db.select().from(classesTable).where(eq(classesTable.competitionId, competitionId)).all();
          const payload = {
            classes: classesRows.map(c => ({ class_id: c.id, class_name: c.name, rows: projection!.results_by_class.get(c.id) ?? [] })),
            pending_unknown_cards: projection.pending_unknown_cards,
          };
          socket.send(JSON.stringify({ type: 'results_full', channel: ch, payload, seq: projection.last_event_seq }));
        }
        // If projection is null (unknown competition), emit NOTHING. The client's hello
        // succeeds (WS stays open). Do NOT fall through to any replay path — the contract
        // from plan 03 is preserved: zero `replay` envelopes on a results: channel.
      }
    }
    ```

    **B-2 + C-M1 regression tests:**

    Extend `apps/edge/src/si/bridge.test.ts` (do NOT replace plan-06 tests):
    - test (markDirty NOT called when activeCompetitionId is null): with markDirtySpy + activeCompetitionId=null + SI10 fixture replay → markDirtySpy.calls.length === 0.
    - test (markDirty fires when activeCompetitionId is set): activeCompetitionId='comp-uuid-1' → spy.calls.length >= 2.

    Extend `apps/edge/src/projection/store.test.ts`:
    - test (store.markDirty NOT invoked from bridge when no active competition): spy on store.markDirty + replay fixture with activeCompetitionId=null → zero calls.

    **C-M1 — Extend `apps/edge/src/ws/index.test.ts` (plan 03 wrote the file with the test-5 stub; plan 08 amends it):**

    Amend test 5 ("results: hello emits no replay envelopes — C-M1 stub from plan 03") to additionally assert plan 08's results_full emission. After plan 08 lifts the stub, the test 5 expectations are:
    1. Insert 3 card_read events for competition_id='test-id'. Seed competition + class + course + competitor rows so the projection has content.
    2. Open WS client; send `{ type: 'hello', channels: ['results:test-id'], last_seen_seq: 0 }`.
    3. Collect ALL frames for 300ms (a touch longer than plan 03's 200ms to give recomputeNow time to walk the events table).
    4. Assert ZERO frames have `type === 'replay'` (the plan-03 contract — C-M1 LOCKED).
    5. Assert EXACTLY ONE frame has `type === 'results_full'` AND `channel === 'results:test-id'` AND `payload.classes` is an array AND `payload.pending_unknown_cards` is an array.

    Add a new test (test 7) — `results:` hello for an unknown competition emits zero frames AND zero `replay` envelopes:
    1. Open WS; send hello with `channels: ['results:does-not-exist']`.
    2. Collect frames 300ms.
    3. Assert ZERO frames received total (no replay, no results_full — the unknown-competition fall-through is silent per the C-M1 contract).

    Add an integration test (executor's choice — store.test.ts or a new `apps/edge/src/projection/integration.test.ts`):
    - test 1 (end-to-end): Boot server with FARTOLA_DEV=1 + in-memory DB seeded with 1 competition + 1 class + 1 course + 1 competitor (card 7501853). Connect WS, subscribe results:<comp-id>. Within ~100ms of `hello`, client receives a `results_full` envelope with classes[].rows=[1 pending row]. POST /api/__dev/simulate-read with card 7501853 + valid CardReadEvent shape (per plan 06 C-H2: payload.start non-null, payload.finish non-null, punches matching course) → within ~100ms client receives a `results_update` envelope with rows[0].status='OK'.
    - test 2 (walk-up): POST /api/competitors (walk-up) for unknown card → results_update envelope arrives; pending_unknown_cards excludes that card after card_bound.

    Apply PATTERNS S-1 header to all modified files.

  </action>
  <verify>
    <automated>cd /home/jonas/src/fartOLa-phase-1 &amp;&amp; pnpm --filter @fartola/edge typecheck &amp;&amp; pnpm --filter @fartola/edge test 2>&amp;1 | grep -E '(pass|fail) [0-9]+|results_update|results_full|markDirty|replay'</automated>
  </verify>
  <done>
    Every event-producing path calls markDirty ONLY when an active competition is set. The WS results:<id> channel emits ONE results_full envelope on hello + results_update on every relevant event. The C-M1 regression gate (ws/index.test.ts test 5) now passes both halves: ZERO `replay` envelopes on results: hello AND exactly ONE results_full envelope. test 7 covers the unknown-competition silent-fall-through path. The B-2 regression gates (bridge.test.ts no-markDirty-when-idle + store.test.ts no-markDirty-from-bridge-when-idle) still pass.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                                                                |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| events → projection → WS            | Read-only DB queries; broadcast uses validated channel name                                                                                |
| GET /api/.../results                | Public to localhost; returns derived data only — no events leaked                                                                          |
| readout: vs results: hello dispatch | C-M1: `replay` envelopes exclusive to readout: (plan 03 contract); results: gets `results_full` on hello (plan 08 fills in plan 03's stub) |

## STRIDE Threat Register

| Threat ID                      | Category                  | Component                                                     | Disposition                | Mitigation Plan                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------ | ------------------------- | ------------------------------------------------------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-PROJECTION-DOS               | DoS                       | reduce() on large competitions                                | accept (P1)                | 20-40 starters target; idempotent.test.ts gates < 200ms for 1000 events. Plan 17 / Phase 2 may introduce incremental reducers.                                                                                                                                                                                                                                |
| T-DEBOUNCE-COALESCE-LOSS       | Reliability               | ProjectionStore markDirty                                     | mitigate                   | Debounce coalesces multiple events into one broadcast — latest state wins. Last_event_seq monotonic so clients detect gaps via seq.                                                                                                                                                                                                                           |
| T-IDLE-MARKDIRTY-LEAK          | Reliability               | bridge → projectionStore.markDirty when no active competition | mitigate                   | Bridge skips markDirty when getActiveCompetitionId() returns null — symmetric to the wsBroadcast skip from plan 06. bridge.test.ts + store.test.ts regression gates assert zero calls.                                                                                                                                                                        |
| T-RESULTS-CHANNEL-LEAK         | Tampering / Wire Contract | apps/edge/src/ws/index.ts hello handler results: branch       | mitigate (C-M1)            | Plan 03 locked the per-channel-kind branch with a no-op stub for results:. Plan 08 lifts the stub to a `results_full` emission; the contract — zero `replay` envelopes on results: channels — survives. ws/index.test.ts test 5 + test 7 are regression gates. The branch never falls through to any `replay` codepath even when the projection lookup fails. |
| T-WS-FAN-OUT (results channel) | Spoofing                  | Anyone with /ws access subscribes                             | accept (P1 localhost-only) | Inherits plan 01 + 03 mitigations. Multi-operator auth in Phase 2.                                                                                                                                                                                                                                                                                            |

</threat_model>

<verification>
1. `pnpm --filter @fartola/edge test` exits 0 with new tests passing (~13 new across Task 1 + Task 2).
2. GET /api/competitions/:id/results returns the live projection.
3. Subscribing to results:<id> receives results_full + results_update on event flow.
4. Plan 11/14 SvelteKit results page can rely on this channel + endpoint.
5. With no active competition set, neither wsBroadcast nor markDirty fire (B-2 regression gates).
6. **C-M1 regression:** ws/index.test.ts test 5 asserts exactly ONE `results_full` AND ZERO `replay` envelopes on a `results:` hello. test 7 asserts silent fall-through on unknown-competition results: hello.
</verification>

<success_criteria>

- WS results:<id> channel + REST /api/competitions/:id/results are the two interfaces plan 14 will consume.
- Debounced recompute + broadcast keeps the bench laptop CPU calm during 20-40 starter Tuesday.
- The reducer remains pure (plan 07 contract preserved); ProjectionStore is the IO layer.
- markDirty wiring uses the explicit `if (id !== null)` conditional, matching plan 06's no-phantom-channel contract.
- **C-M1 LOCKED:** hello on a results: channel emits EXACTLY ONE `results_full` envelope; ZERO `replay` envelopes on results: channels EVER. Plan 03's per-channel-kind contract survives plan 08's stub-lift.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-single-laptop-training-mvp/01-08-SUMMARY.md` using the GSD summary template.
Record:
- chosen debounce time post-bench-verification.
- any markDirty placement adjustments.
- whether the integration tests landed in store.test.ts or a separate integration.test.ts file.
- confirmation that the bridge.test.ts + store.test.ts B-2 regression gates pass.
- **C-M1 confirmation:** ws/index.test.ts test 5 confirms zero `replay` + exactly one `results_full` on results: hello with 3 events in the DB. test 7 confirms silent fall-through on unknown-competition results: hello.
</output>
