---
phase: 01-single-laptop-training-mvp
plan: 17
type: execute
wave: 5
depends_on: [02, 06, 11]
files_modified:
  - apps/edge/src/backup/daily.ts
  - apps/edge/src/backup/daily.test.ts
  - apps/edge/src/privacy/retention.ts
  - apps/edge/src/privacy/retention.test.ts
  - apps/edge/src/bin/fartola.ts
  - apps/edge/src/server.ts
  - apps/edge/src/routes/admin.ts
  - apps/edge/src/routes/admin.test.ts
autonomous: true
tags: [backup, privacy, retention, gdpr, cron-in-process]
requirements:
  - REQ-OPS-003
  - REQ-PRIV-002
must_haves:
  truths:
    - 'Daily backup runs at next-midnight + every 24h thereafter; produces fartola.db.bak-YYYY-MM-DD in FARTOLA_BACKUP_DIR (default ./backups)'
    - 'Backup uses db.backup() online API (consistent under WAL) — not file copy (RESEARCH Pitfall 3)'
    - 'Backup retention: keep last 7 daily snapshots; older snapshots pruned'
    - "Retention scrub runs daily: competitor rows in competitions with date older than 30 days get name='Anonymiserad', club=null, scrubbed_at_ms=now; card_number kept (hardware ID, not PII per research.md §6)"
    - 'Scrub does NOT touch `consent_status` or `consent_at_ms` — consent metadata persists; only PII (name, club) is scrubbed'
    - 'Both backup and retention are cron-in-process (setTimeout chain anchored on midnight); no node-cron dependency'
    - 'POST /api/__admin/run-backup-now and /api/__admin/run-retention-now (gated on FARTOLA_DEV=1 OR an admin token) trigger one-off runs for operators/tests'
    - 'Backup failure: log to stderr, retry in 1h; retention failure same'
  artifacts:
    - path: 'apps/edge/src/backup/daily.ts'
      provides: 'scheduleDailyBackup(handle, opts) — sets up setTimeout chain anchored on next midnight; idempotent on re-call'
      min_lines: 50
    - path: 'apps/edge/src/privacy/retention.ts'
      provides: 'scheduleDailyRetention(handle, retentionDays) + runRetentionNow(handle) — scrubs old PII'
      min_lines: 50
    - path: 'apps/edge/src/routes/admin.ts'
      provides: 'POST /api/__admin/run-backup-now + run-retention-now — gated on FARTOLA_DEV=1 (matches dev.ts gating pattern from plan 03)'
      min_lines: 30
  key_links:
    - from: 'apps/edge/src/bin/fartola.ts'
      to: 'apps/edge/src/backup/daily.ts'
      via: 'scheduleDailyBackup(handle, { backupDir }) on boot'
      pattern: 'scheduleDailyBackup'
    - from: 'apps/edge/src/privacy/retention.ts'
      to: 'apps/edge/src/db/schema.ts'
      via: "UPDATE competitors SET name='Anonymiserad', club=null, scrubbed_at_ms=? WHERE scrubbed_at_ms IS NULL AND competition_id IN (SELECT id FROM competitions WHERE date < ?)"
      pattern: 'scrubbed_at_ms'
---

<objective>
REQ-OPS-003 daily backup + REQ-PRIV-002 30-day retention scrub. Both are cron-in-process per RESEARCH §"Don't Hand-Roll" (pure setTimeout chain at midnight).

Honors:

- REQ-OPS-003: daily SQLite backup; no human action.
- REQ-PRIV-002: 30 days post-event for contact information.
- RESEARCH §Pitfall 3 (WAL + file copy): use db.backup() online API.
- RESEARCH A7 (retention interpretation): SCRUB (name/club nulled) not DELETE (preserves event log + results structure).
- Open Question 2 from RESEARCH: confirmed SCRUB approach. card_number kept (not PII per research.md §6).

Output: Daily backup scheduler + retention scrub + 2 admin endpoints + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md
@.planning/phases/01-single-laptop-training-mvp/01-CONTEXT.md
@.planning/phases/01-single-laptop-training-mvp/01-02-PLAN.md
@.planning/research/architecture.md

<interfaces>
scheduleDailyBackup signature:
```
export interface BackupOpts {
  backupDir: string;                       // default './backups'
  keepLast?: number;                       // default 7
  testClock?: { now: () => number };       // PATTERNS S-2 — tests inject a clock
}
export interface BackupHandle { runNow: () => Promise<void>; stop: () => void; }
export function scheduleDailyBackup(handle: DbHandle, opts: BackupOpts): BackupHandle;
```

scheduleDailyBackup behavior:

- On call, compute msUntilNextMidnight = next 00:00:00 local time minus now.
- setTimeout(runBackup, msUntilNextMidnight).
- runBackup: await handle.sqlite.backup(destPath) (WAL-consistent), prune dir to keep last N, schedule next tick.
- On error: stderr log + setTimeout(retry, 1h).
- runNow exposed for admin endpoint + tests.

scheduleDailyRetention signature:

```
export interface RetentionOpts {
  retentionDays?: number;        // default 30
  testClock?: { now: () => number };
}
export interface RetentionHandle { runNow: () => Promise<RetentionResult>; stop: () => void; }
export interface RetentionResult { scrubbed_count: number; cutoff_date: string; }
export function scheduleDailyRetention(handle: DbHandle, opts: RetentionOpts): RetentionHandle;
```

Retention scrub query (Drizzle):

```
const cutoffDate = new Date(now() - retentionDays * 86400000).toISOString().slice(0, 10);  // YYYY-MM-DD
const result = handle.db.update(competitors).set({ name: 'Anonymiserad', club: null, scrubbedAtMs: now() })
  .where(and(isNull(competitors.scrubbedAtMs), sql`competition_id IN (SELECT id FROM competitions WHERE date < ${cutoffDate})`))
  .run();
return { scrubbed_count: result.changes, cutoff_date: cutoffDate };
```

PRESERVE card_number — RESEARCH A7 + research.md §6 (hardware ID, not PII). PRESERVE consent_at_ms (audit trail). The result rows have status="scrubbed" implicitly via scrubbed_at_ms !== null.

Admin endpoints (gated like plan 03's dev.ts):

```
if (process.env.FARTOLA_DEV !== '1') return;  // don't register in production
app.post('/api/__admin/run-backup-now', async () => { await backup.runNow(); return { ok: true }; });
app.post('/api/__admin/run-retention-now', async () => { const r = await retention.runNow(); return { ok: true, ...r }; });
```

Phase 2 will replace the dev gate with admin-token auth.

Bin wiring:

- In main(), after openDatabase + buildServer + attachBridge:
  - const backup = scheduleDailyBackup(handle, { backupDir: opts.backupDir });
  - const retention = scheduleDailyRetention(handle, { retentionDays: 30 });
  - SIGINT handler: backup.stop(); retention.stop().
- CLI flags: --backup-dir <path> (default './backups'), --retention-days <n> (default 30).
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: scheduleDailyBackup + tests + admin route for run-backup-now</name>
  <files>
    apps/edge/src/backup/daily.ts,
    apps/edge/src/backup/daily.test.ts,
    apps/edge/src/routes/admin.ts,
    apps/edge/src/routes/admin.test.ts,
    apps/edge/src/server.ts
  </files>
  <read_first>
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Daily backup via better-sqlite3 online API" excerpt (lines 832-856 — verbatim usable),
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Pitfall 3: WAL mode and SQLite file copy",
    apps/edge/src/db/index.ts (plan 02 — DbHandle.sqlite),
    apps/edge/src/routes/dev.ts (plan 03 — admin gate pattern)
  </read_first>
  <action>
    apps/edge/src/backup/daily.ts — cron-in-process:
    ```
    import path from 'node:path';
    import { mkdirSync, readdirSync, unlinkSync, statSync, existsSync } from 'node:fs';
    import type { DbHandle } from '../db/index.ts';

    export interface BackupOpts { backupDir: string; keepLast?: number; testClock?: { now: () => number } }
    export interface BackupHandle { runNow: () => Promise<{ dest: string }>; stop: () => void }

    function nextMidnightMs(nowMs: number): number {
      const d = new Date(nowMs);
      d.setHours(24, 0, 0, 0);
      return d.getTime();
    }

    function prune(dir: string, keepLast: number): void {
      if (!existsSync(dir)) return;
      const files = readdirSync(dir).filter(f => /^fartola\.db\.bak-\d{4}-\d{2}-\d{2}$/.test(f));
      // Filename embeds YYYY-MM-DD; lexicographic sort + reverse gives
      // newest-first without depending on filesystem mtime (which drifts
      // across copy / clone / restore-from-backup).
      files.sort();
      files.reverse();
      for (const f of files.slice(keepLast)) {
        try { unlinkSync(path.join(dir, f)); } catch { /* swallow */ }
      }
    }

    export function scheduleDailyBackup(handle: DbHandle, opts: BackupOpts): BackupHandle {
      const now = opts.testClock?.now ?? Date.now;
      const keepLast = opts.keepLast ?? 7;
      let timer: NodeJS.Timeout | null = null;
      let stopped = false;
      mkdirSync(opts.backupDir, { recursive: true });

      async function runOnce(): Promise<{ dest: string }> {
        const dateStr = new Date(now()).toISOString().slice(0, 10);
        const dest = path.join(opts.backupDir, `fartola.db.bak-${dateStr}`);
        await handle.sqlite.backup(dest);
        prune(opts.backupDir, keepLast);
        return { dest };
      }

      function schedule() {
        if (stopped) return;
        const delay = nextMidnightMs(now()) - now();
        timer = setTimeout(async () => {
          try { await runOnce(); } catch (e) { process.stderr.write(`[backup] failed: ${(e as Error).message}\n`); setTimeout(schedule, 60 * 60 * 1000); return; }
          schedule();
        }, delay);
      }

      schedule();
      return { runNow: runOnce, stop: () => { stopped = true; if (timer) clearTimeout(timer); } };
    }
    ```

    apps/edge/src/routes/admin.ts:
    ```
    export default async function adminRoutes(app: FastifyInstance) {
      if (process.env.FARTOLA_DEV !== '1') return;
      app.post('/api/__admin/run-backup-now', async () => {
        if (!app.fartolaBackup) return { ok: false, error: 'no_backup' };
        const r = await app.fartolaBackup.runNow();
        return { ok: true, dest: r.dest };
      });
      app.post('/api/__admin/run-retention-now', async () => {
        if (!app.fartolaRetention) return { ok: false, error: 'no_retention' };
        const r = await app.fartolaRetention.runNow();
        return { ok: true, ...r };
      });
    }
    ```
    Decorate FastifyInstance with `fartolaBackup: BackupHandle | null` and `fartolaRetention: RetentionHandle | null`. The bin (Task 2) sets these.

    apps/edge/src/backup/daily.test.ts — node:test:
    - test 1: With testClock set to a known "now", scheduleDailyBackup computes the correct first-fire delay (msUntilNextMidnight).
    - test 2: runNow() produces a file at backupDir/fartola.db.bak-YYYY-MM-DD.
    - test 3: runNow() called twice on the same day overwrites the same file (no duplicates).
    - test 4: prune keeps the N most-recent files (use `fs.utimes` to back-date older files; assert older snapshots removed).
    - test 5: runNow() with the DB closed throws and the error is surfaced via the returned promise's rejection.

    apps/edge/src/routes/admin.test.ts — node:test:
    - test 1: With FARTOLA_DEV='1' set, POST /api/__admin/run-backup-now → 200 { ok: true, dest } AND a backup file exists.
    - test 2: With FARTOLA_DEV unset → 404.

    Apply PATTERNS S-1 header to all new files.

  </action>
  <verify>
    <automated>cd /home/jonas/src/fartOLa-phase-1 &amp;&amp; pnpm --filter @fartola/edge typecheck &amp;&amp; pnpm --filter @fartola/edge test 2>&amp;1 | grep -E '(pass|fail) [0-9]+|backup|admin'</automated>
  </verify>
  <done>
    Daily backup scheduler with prune; admin route triggers manual run; tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: scheduleDailyRetention + tests + bin wiring</name>
  <files>
    apps/edge/src/privacy/retention.ts,
    apps/edge/src/privacy/retention.test.ts,
    apps/edge/src/bin/fartola.ts,
    apps/edge/src/server.ts
  </files>
  <read_first>
    apps/edge/src/db/schema.ts (plan 02 — competitors.scrubbedAtMs column),
    apps/edge/src/backup/daily.ts (Task 1 — pattern to mirror),
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §A7 (SCRUB interpretation),
    .planning/research/architecture.md §6 (PII vs hardware-ID distinction)
  </read_first>
  <action>
    apps/edge/src/privacy/retention.ts:
    ```
    import { sql, and, isNull } from 'drizzle-orm';
    import { competitors } from '../db/schema.ts';
    import type { DbHandle } from '../db/index.ts';

    export interface RetentionOpts { retentionDays?: number; testClock?: { now: () => number } }
    export interface RetentionResult { scrubbed_count: number; cutoff_date: string }
    export interface RetentionHandle { runNow: () => Promise<RetentionResult>; stop: () => void }

    function nextMidnightMs(nowMs: number): number { const d = new Date(nowMs); d.setHours(24, 0, 0, 0); return d.getTime(); }

    export function scheduleDailyRetention(handle: DbHandle, opts: RetentionOpts = {}): RetentionHandle {
      const now = opts.testClock?.now ?? Date.now;
      const retentionDays = opts.retentionDays ?? 30;
      let timer: NodeJS.Timeout | null = null; let stopped = false;

      async function runOnce(): Promise<RetentionResult> {
        const cutoffMs = now() - retentionDays * 86400000;
        const cutoffDate = new Date(cutoffMs).toISOString().slice(0, 10);
        const result = handle.db.update(competitors).set({ name: 'Anonymiserad', club: null, scrubbedAtMs: now() })
          .where(and(isNull(competitors.scrubbedAtMs), sql`competition_id IN (SELECT id FROM competitions WHERE date < ${cutoffDate})`))
          .run();
        return { scrubbed_count: result.changes, cutoff_date: cutoffDate };
      }

      function schedule() {
        if (stopped) return;
        const delay = nextMidnightMs(now()) - now();
        timer = setTimeout(async () => {
          try { await runOnce(); } catch (e) { process.stderr.write(`[retention] failed: ${(e as Error).message}\n`); setTimeout(schedule, 60 * 60 * 1000); return; }
          schedule();
        }, delay);
      }
      schedule();
      return { runNow: runOnce, stop: () => { stopped = true; if (timer) clearTimeout(timer); } };
    }
    ```

    Update apps/edge/src/bin/fartola.ts:
    - After buildServer + attachBridge: `const backup = scheduleDailyBackup(handle, { backupDir: opts.backupDir ?? './backups' }); const retention = scheduleDailyRetention(handle, { retentionDays: opts.retentionDays ?? 30 });`.
    - `app.fartolaBackup = backup; app.fartolaRetention = retention;`.
    - SIGINT handler also stops both.
    - CLI flags: --backup-dir, --retention-days (defaults applied).

    Update apps/edge/src/server.ts FastifyInstance decoration declaration (TypeScript module augmentation) to add `fartolaBackup` and `fartolaRetention`.

    apps/edge/src/privacy/retention.test.ts — node:test:
    - test 1 (REQ-PRIV-002 happy path): Set up DB with a competition dated 35 days ago + 1 competitor with name/club; testClock set to "today". runRetentionNow → scrubbed_count: 1, cutoff_date matches today-30. The competitor row's name is now 'Anonymiserad', club is NULL, scrubbed_at_ms is now(), card_number is UNCHANGED, consent_at_ms is UNCHANGED.
    - test 2: Competition dated 25 days ago + 1 competitor → scrubbed_count: 0 (within retention window).
    - test 3: Competitor already scrubbed (scrubbed_at_ms NOT NULL) → not re-scrubbed (count remains 0 on second runRetentionNow call).
    - test 4: Cross-competition isolation: scrubbing competition A's competitors doesn't touch competition B's competitors (different dates).
    - test 5: Events table is UNTOUCHED by retention scrub (REQ-EVT-002 append-only still holds; events.payload may contain the old name, which is the historical event — operator-aware tradeoff documented in summary).

    Note on test 5: The payload of card_bound events contains competitor_id but the competitor's NAME (e.g., walkup events) carries the consent metadata not the name. Verify by inspecting EventPayload (plan 02): card_bound payload has competitor_id only — names live in competitors table, which IS scrubbed. RESEARCH A7's "scrub vs delete" tradeoff is acceptable.

    Apply PATTERNS S-1 header to new files.

  </action>
  <verify>
    <automated>cd /home/jonas/src/fartOLa-phase-1 &amp;&amp; pnpm --filter @fartola/edge typecheck &amp;&amp; pnpm --filter @fartola/edge test 2>&amp;1 | grep -E '(pass|fail) [0-9]+|retention|scrub'</automated>
  </verify>
  <done>
    Retention scrub anonymizes PII after 30 days while preserving event log integrity. Bin wires backup + retention. All Task-2 tests pass.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                        |
| ------------------------------ | ------------------------------------------------------------------ |
| backups dir → filesystem       | Operator-owned filesystem; backups inherit DB file permissions     |
| retention UPDATE → competitors | Scrub is irreversible by design (the rollback is the daily backup) |

## STRIDE Threat Register

| Threat ID                | Category  | Component                      | Disposition | Mitigation Plan                                                                                                                                                                                             |
| ------------------------ | --------- | ------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-RETENTION-MISS         | Privacy   | scheduleDailyRetention         | mitigate    | Runs daily anchored on midnight; scrubbed_at_ms idempotent (already-scrubbed rows skipped). Tests 1-3 in retention.test.ts.                                                                                 |
| T-RETENTION-OVERREACH    | Tampering | retention WHERE clause         | mitigate    | Tested against same-day competitions (test 2: 25-day-old not scrubbed). Cross-competition isolation tested (test 4).                                                                                        |
| T-BACKUP-WAL-CORRUPT     | Tampering | scheduleDailyBackup            | mitigate    | Uses db.backup() online API (not file copy — RESEARCH Pitfall 3). Test 2 + 4 in backup.test.ts.                                                                                                             |
| T-BACKUP-DISK-FULL       | DoS       | backup writes                  | accept      | Backups go to operator-chosen FARTOLA_BACKUP_DIR; full disk surfaces via stderr log. Phase 2 may add disk-space monitoring (REQ-OPS-004).                                                                    |
| T-EVENT-PAYLOAD-PII-LEAK | Privacy   | events.payload after retention | accept      | Documented in summary: events.payload may still reference historical names; the events table is append-only by REQ-EVT-002. Operators advised to encrypt the disk; Phase 2 may add per-event PII filtering. |

</threat_model>

<verification>
1. `pnpm --filter @fartola/edge test` exits 0 with new tests passing.
2. Manual smoke: `FARTOLA_DEV=1 fartola --backup-dir /tmp/fartola-backups`; in another shell `curl -X POST localhost:3000/api/__admin/run-backup-now` → 200 + file at /tmp/fartola-backups/fartola.db.bak-YYYY-MM-DD.
3. Same with run-retention-now after seeding an old competition.
</verification>

<success_criteria>

- REQ-OPS-003 + REQ-PRIV-002 binding contracts honored with automated tests.
- Both schedulers idempotent + restart-safe.
- Wave 5 plan 18 (packaging) unblocked.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-single-laptop-training-mvp/01-17-SUMMARY.md` using the GSD summary template.
Record: backup file format chosen (.bak vs .sqlite — plan says .bak), retention scrub PII tradeoffs documented for operator awareness, any timezone handling quirks for nextMidnightMs.
</output>
