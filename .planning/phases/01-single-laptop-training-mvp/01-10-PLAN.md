---
phase: 01-single-laptop-training-mvp
plan: 10
type: execute
wave: 3
depends_on: [04, 06, 07, 08]
files_modified:
  - apps/edge/src/routes/manual.ts
  - apps/edge/src/routes/manual.test.ts
  - apps/edge/src/server.ts
  - packages/shared-types/src/dtos.ts
  - apps/edge/src/routes/competitors.ts
autonomous: true
tags: [manual-dnf, un-dnf, readout-ops, walk-up]
requirements:
  - REQ-EVT-CMP-006
  - REQ-EVT-CMP-004
  - REQ-PRIV-001
must_haves:
  truths:
    - 'POST /api/competitions/:id/competitors/:competitorId/manual-dnf inserts a manual_dnf event with reason; projection marks status=DNF'
    - 'POST /api/competitions/:id/competitors/:competitorId/un-dnf inserts an un_dnf event; projection reverts to punch-only status'
    - "Both endpoints reject if competitor doesn't belong to the competition (404); both emit WS readout: envelope; both mark projection dirty"
    - "Walk-up endpoint (plan 04) accepts an additional `replace_card_for_competitor_id` field — when set, the new card_number replaces the existing competitor's card_number (operator corrects a misread)"
  artifacts:
    - path: 'apps/edge/src/routes/manual.ts'
      provides: 'manual-dnf and un-dnf REST handlers + WS broadcast + projectionStore.markDirty integration'
      min_lines: 80
    - path: 'packages/shared-types/src/dtos.ts'
      provides: 'Additional Zod schemas: ManualDnfInput, UnDnfInput, CompetitorReplaceCardInput'
      contains: 'ManualDnfInput'
  key_links:
    - from: 'apps/edge/src/routes/manual.ts'
      to: 'apps/edge/src/si/eventInserter.ts'
      via: 'insertEvent for manual_dnf and un_dnf'
      pattern: 'insertEvent'
---

<objective>
Close the operator-action REST surface that the readout-view UI (plan 13) calls when the operator manually overrides status. Two endpoints land:

- POST /api/competitions/:id/competitors/:competitorId/manual-dnf (UI-SPEC §"Manual DNF override")
- POST /api/competitions/:id/competitors/:competitorId/un-dnf (UI-SPEC §"Manual DNF override" reversible)

Plus a small enhancement to plan 04's POST /api/competitors: the walk-up modal can also `replace_card_for_competitor_id` — when the operator notices a misread (UI-SPEC §"Walk-up modal" — Bricka editable to correct misread), the new card number replaces the existing competitor's card_number atomically.

Honors:

- REQ-EVT-CMP-006 D-12: DNF/MP detection includes a manual override path; plan 07 reduce already handles the event types — plan 10 lands the REST that emits the events.
- UI-SPEC §"Destructive actions": Manual DNF popover is reversible via un_dnf; no irreversible destructive actions in Phase 1.
- REQ-PRIV-001: replace-card retains the original consent_at_ms (the consent was already given at walk-up; only the card_number is corrected).

Output: 2 new routes, Zod input types, integration tests, and a small route update in competitors.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md
@.planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md
@.planning/phases/01-single-laptop-training-mvp/01-CONTEXT.md
@.planning/phases/01-single-laptop-training-mvp/01-04-PLAN.md
@.planning/phases/01-single-laptop-training-mvp/01-06-PLAN.md
@.planning/phases/01-single-laptop-training-mvp/01-07-PLAN.md
@.planning/phases/01-single-laptop-training-mvp/01-08-PLAN.md

<interfaces>
manual_dnf event payload (from plan 02 EventPayload):
- { event_type: 'manual_dnf', competitor_id: string, reason: string }

un_dnf event payload:

- { event_type: 'un_dnf', competitor_id: string }

WS broadcast on both:

- channel: readoutChannel(competitionId)
- envelope.type: 'manual_dnf' or 'un_dnf'
- envelope.payload: the event payload itself

The reduce()'s un_dnf handler (plan 07) re-derives status from latest_punches. If a competitor was DNF'd manually then un-DNF'd before any card_read happened, projection returns to status='PEND'. Plan 07 test 6 already covers this.

Replace-card behavior (extends plan 04 POST /api/competitors):

- Operator opens walk-up with a misread card number X; correct card is Y.
- Body includes `replace_card_for_competitor_id`. Server:
  1. Validates the named competitor exists and belongs to the competition.
  2. In a single transaction:
     - UPDATE competitors SET card_number = Y WHERE id = competitorId.
     - INSERT card_bound event with payload.competitor_id = competitorId, card_number = Y, walkup = true.
  3. Returns 200 + the updated CompetitorDTO.
- This path does NOT create a new competitor row. consent_at_ms is preserved.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: manual-dnf + un-dnf REST routes + tests</name>
  <files>
    apps/edge/src/routes/manual.ts,
    apps/edge/src/routes/manual.test.ts,
    apps/edge/src/server.ts,
    packages/shared-types/src/dtos.ts
  </files>
  <read_first>
    apps/edge/src/routes/competitors.ts (plan 04 — transaction + ws.broadcast + markDirty pattern),
    apps/edge/src/si/eventInserter.ts (plan 06),
    apps/edge/src/db/schema.ts EventPayload (plan 02),
    .planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md §"Manual DNF override"
  </read_first>
  <action>
    Add Zod schemas to packages/shared-types/src/dtos.ts:
    ```
    export const ManualDnfInput = z.object({ reason: z.string().min(1).max(500) });
    export type ManualDnfInput = z.infer<typeof ManualDnfInput>;
    export const UnDnfInput = z.object({}).optional();   // no body; presence of POST is enough
    export type UnDnfInput = z.infer<typeof UnDnfInput>;
    ```
    Export from shared-types/src/index.ts.

    apps/edge/src/routes/manual.ts:
    ```
    import { eq, and } from 'drizzle-orm';
    import { competitors as competitorsTable } from '../db/schema.ts';
    import { insertEvent } from '../si/eventInserter.ts';
    import { readoutChannel, ManualDnfInput } from '@fartola/shared-types';

    export default async function manualRoutes(app: FastifyInstance) {
      app.post('/api/competitions/:id/competitors/:competitorId/manual-dnf', async (req, reply) => {
        const { id: competitionId, competitorId } = req.params as { id: string; competitorId: string };
        const parsed = ManualDnfInput.safeParse(req.body);
        if (!parsed.success) return reply.code(400).send({ errors: parsed.error.issues });
        const competitor = app.fartolaDb.db.select().from(competitorsTable).where(and(eq(competitorsTable.id, competitorId), eq(competitorsTable.competitionId, competitionId))).get();
        if (!competitor) return reply.code(404).send({ error: 'competitor_not_found' });
        const r = insertEvent(app.fartolaDb, app.fartolaNodeId, 'manual_dnf', Date.now(), { event_type: 'manual_dnf', competitor_id: competitorId, reason: parsed.data.reason }, competitionId);
        app.wsBroadcast(readoutChannel(competitionId), { type: 'manual_dnf', payload: { competitor_id: competitorId, reason: parsed.data.reason }, seq: r.local_seq });
        app.projectionStore.markDirty(competitionId);
        return reply.code(201).send({ local_seq: r.local_seq });
      });

      app.post('/api/competitions/:id/competitors/:competitorId/un-dnf', async (req, reply) => {
        const { id: competitionId, competitorId } = req.params as { id: string; competitorId: string };
        const competitor = app.fartolaDb.db.select().from(competitorsTable).where(and(eq(competitorsTable.id, competitorId), eq(competitorsTable.competitionId, competitionId))).get();
        if (!competitor) return reply.code(404).send({ error: 'competitor_not_found' });
        const r = insertEvent(app.fartolaDb, app.fartolaNodeId, 'un_dnf', Date.now(), { event_type: 'un_dnf', competitor_id: competitorId }, competitionId);
        app.wsBroadcast(readoutChannel(competitionId), { type: 'un_dnf', payload: { competitor_id: competitorId }, seq: r.local_seq });
        app.projectionStore.markDirty(competitionId);
        return reply.code(201).send({ local_seq: r.local_seq });
      });
    }
    ```

    Register manualRoutes in apps/edge/src/server.ts.

    apps/edge/src/routes/manual.test.ts — node:test:
    - test 1: POST manual-dnf for a known competitor → 201; events row with eventType='manual_dnf' exists; projection.competitors.get(competitorId).status === 'DNF'; manual_dnf_reason equals body.reason.
    - test 2: POST manual-dnf with empty reason → 400.
    - test 3: POST manual-dnf for unknown competitor → 404.
    - test 4: POST un-dnf after manual-dnf → 201; projection status reverts (PEND if no card_read; OK/MP if punches present).
    - test 5: POST un-dnf for competitor without any prior manual_dnf → 201 (idempotent at REST level; projection just doesn't change anything).
    - test 6: Cross-competition reject: POST manual-dnf with competitor id from a different competition → 404.
    - test 7: WS broadcast asserted — register a spy on app.wsBroadcast; POST manual-dnf; assert spy called with channel='readout:<id>' and type='manual_dnf'.

    Apply PATTERNS S-1 header.

  </action>
  <verify>
    <automated>cd /home/jonas/src/fartOLa-phase-1 &amp;&amp; pnpm --filter @fartola/edge typecheck &amp;&amp; pnpm --filter @fartola/edge test 2>&amp;1 | grep -E '(pass|fail) [0-9]+|manual-dnf|un-dnf'</automated>
  </verify>
  <done>
    Both endpoints work, broadcast WS, mark projection dirty, return correct status codes. ~7 new tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Walk-up endpoint extension — replace-card-for-competitor</name>
  <files>
    apps/edge/src/routes/competitors.ts,
    apps/edge/src/routes/competitors.test.ts,
    packages/shared-types/src/dtos.ts
  </files>
  <read_first>
    apps/edge/src/routes/competitors.ts (plan 04 — extend POST /api/competitors),
    .planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md §"Walk-up modal" (Bricka editable to correct misread),
    apps/edge/src/db/schema.ts competitors partial unique index
  </read_first>
  <action>
    Update packages/shared-types/src/dtos.ts CompetitorCreateInput → add optional fields:
    ```
    export const CompetitorCreateInput = z.object({
      competition_id: UUID,
      name: z.string().trim().min(2).max(200).optional(),  // optional when replace_card_for_competitor_id is set
      club: z.string().trim().max(120).nullable().optional().transform(v => v ?? null),
      class_id: UUID.optional(),                            // optional when replacing
      card_number: POSITIVE_INT.nullable().optional().transform(v => v ?? null),
      consent: z.literal(true).optional(),                  // optional when replacing (consent already on file)
      replace_card_for_competitor_id: UUID.optional(),
    }).superRefine((val, ctx) => {
      if (val.replace_card_for_competitor_id) {
        // Replace mode requires card_number; everything else optional.
        if (val.card_number === null || val.card_number === undefined) ctx.addIssue({ code: 'custom', path: ['card_number'], message: 'card_number required for replace' });
      } else {
        if (!val.name) ctx.addIssue({ code: 'custom', path: ['name'], message: 'name required' });
        if (!val.class_id) ctx.addIssue({ code: 'custom', path: ['class_id'], message: 'class_id required' });
        if (val.consent !== true) ctx.addIssue({ code: 'custom', path: ['consent'], message: 'consent required' });
      }
    });
    ```

    Update apps/edge/src/routes/competitors.ts POST handler:
    - If body.replace_card_for_competitor_id is set:
      1. Lookup competitor: must exist in body.competition_id; else 404.
      2. Check the new card_number isn't already taken by a DIFFERENT competitor in this competition (409 if so).
      3. Transaction:
         - UPDATE competitors SET cardNumber = body.card_number WHERE id = replace_card_for_competitor_id.
         - INSERT card_bound event { event_type, competitor_id, card_number, walkup: true, consent_at_ms: competitor.consentAtMs }.
      4. wsBroadcast readout:<id> with type=card_bound + payload.
      5. markDirty.
      6. Return 200 + updated CompetitorDTO.
    - Else: behave as plan 04 (create new competitor row).

    Add tests to apps/edge/src/routes/competitors.test.ts:
    - test: POST with replace_card_for_competitor_id pointing at an existing competitor + new card_number → 200; DB row updated; card_bound event emitted; consent_at_ms unchanged from original.
    - test: replace target in different competition → 404.
    - test: new card_number collides with a different competitor in same competition → 409.

    Apply PATTERNS S-1 header where modified.

  </action>
  <verify>
    <automated>cd /home/jonas/src/fartOLa-phase-1 &amp;&amp; pnpm --filter @fartola/edge typecheck &amp;&amp; pnpm --filter @fartola/edge test 2>&amp;1 | grep -E '(pass|fail) [0-9]+|replace_card'</automated>
  </verify>
  <done>
    Walk-up modal can now correct misreads atomically. consent_at_ms preserved per REQ-PRIV-001. ~3 new tests pass.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                            |
| -------------------------------------------- | -------------------------------------------------------------------------------------- |
| operator action → manual_dnf event           | Operator-attested via localhost-only route; event log preserves the action             |
| replace-card flow → competitors row mutation | Mutable config table (D-09); event log captures the binding for projection determinism |

## STRIDE Threat Register

| Threat ID                 | Category  | Component         | Disposition | Mitigation Plan                                                                     |
| ------------------------- | --------- | ----------------- | ----------- | ----------------------------------------------------------------------------------- |
| T-CROSS-COMP-MANUAL       | Privacy   | manual-dnf URL    | mitigate    | competitor existence query includes competition_id; 404 returned otherwise. Test 6. |
| T-REPLACE-CARD-COLLISION  | Tampering | replace-card flow | mitigate    | DB partial unique index + 409 response on collision.                                |
| T-MISSING-CONSENT-REPLACE | Privacy   | replace-card path | mitigate    | consent_at_ms preserved from original row; not re-captured (already on file).       |

</threat_model>

<verification>
1. `pnpm --filter @fartola/edge test` exits 0 with ~10 new tests passing.
2. UI plan 13 (readout view) can POST manual-dnf inline from the active row.
3. UI plan 14 (walk-up modal) can POST replace-card via the extended endpoint when the operator corrects a misread Bricka.
</verification>

<success_criteria>

- Manual override flow lands without persisting any "results" table — events drive everything.
- Reversible per UI-SPEC §"Destructive actions" — no irreversible mutations.
- Wave 4 UI plans 13+14 unblocked.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-single-laptop-training-mvp/01-10-SUMMARY.md` using the GSD summary template.
Record any Zod superRefine quirks encountered.
</output>
