---
phase: 01-single-laptop-training-mvp
plan: 07
type: execute
wave: 2
depends_on: [02, 04, 06]
files_modified:
  - apps/edge/src/projection/types.ts
  - apps/edge/src/projection/reduce.ts
  - apps/edge/src/projection/dnfMp.ts
  - apps/edge/src/projection/matching.ts
  - apps/edge/src/projection/halfDayClockMath.ts
  - apps/edge/src/projection/halfDayClockMath.test.ts
  - apps/edge/src/projection/reduce.test.ts
  - apps/edge/src/projection/dnfMp.test.ts
  - apps/edge/src/projection/matching.test.ts
  - apps/edge/src/projection/idempotent.test.ts
  - apps/edge/src/projection/index.ts
autonomous: true
tags: [projection, reducer, dnf, mp, matching, event-sourcing]
requirements:
  - REQ-EVT-003
  - REQ-EVT-004
  - REQ-EVT-CMP-005
  - REQ-EVT-CMP-006
must_haves:
  truths:
    - "reduce(events, course, competitors) is a pure function — same inputs produce identical outputs across runs (REQ-EVT-004 idempotent)"
    - "Card-to-competitor matching: card_read with card_number matching a competitor row auto-attaches; unknown cards become pendingUnknownCards"
    - "DNF detection reads payload.finish: if payload.finish === null, the competitor is DNF regardless of punches[] content (C-H2 — start/finish come from CardReadEvent top-level fields, NOT from punch codes)"
    - "Elapsed time is computed from payload.finish - payload.start (both HalfDayClock objects, converted to ms via halfDayClockMath.ts), NOT from a punch with magic control_code"
    - "MP: a competitor whose card_read punches are missing or out-of-order vs course.controls is marked MP with the offending control codes — punches[] still drives MP detection, but start/finish are no longer guessed from punches[]"
    - "manual_dnf event overrides MP/OK status; un_dnf reverts to the punch-only projection"
    - "Re-running reduce over the same event log twice produces structurally identical CompetitionState"
  artifacts:
    - path: "apps/edge/src/projection/reduce.ts"
      provides: "Pure reducer: events + course + competitors -> CompetitionState; orchestrates dnfMp.ts and matching.ts"
      min_lines: 80
    - path: "apps/edge/src/projection/dnfMp.ts"
      provides: "detectStatus(card_read, course) -> { status: 'OK'|'MP'|'DNF', missing_codes, extra_codes, out_of_order_codes }. Reads start/finish from the CardReadEvent top-level fields (C-H2)."
      contains: "missing_codes"
    - path: "apps/edge/src/projection/halfDayClockMath.ts"
      provides: "halfDayClockToMs(clock) + diffMs(start, finish) — converts Phase 0 HalfDayClock objects to absolute milliseconds within a 24h reference, handles half-day wrap-around"
      min_lines: 30
    - path: "apps/edge/src/projection/matching.ts"
      provides: "matchCardToCompetitor(cardNumber, competitors) -> Competitor | null"
      contains: "matchCardToCompetitor"
    - path: "apps/edge/src/projection/types.ts"
      provides: "CompetitionState, CompetitorView, ResultView, PunchStatus types"
      contains: "CompetitionState"
  key_links:
    - from: "apps/edge/src/projection/reduce.ts"
      to: "apps/edge/src/projection/dnfMp.ts"
      via: "detectStatus call per card_read with payload.start, payload.finish, payload.punches"
      pattern: "detectStatus"
    - from: "apps/edge/src/projection/dnfMp.ts"
      to: "apps/edge/src/projection/halfDayClockMath.ts"
      via: "diffMs(payload.start, payload.finish) for elapsed time"
      pattern: "diffMs"
    - from: "apps/edge/src/projection/reduce.ts"
      to: "apps/edge/src/projection/matching.ts"
      via: "matchCardToCompetitor on every card_read"
      pattern: "matchCardToCompetitor"
---

<objective>
Pure reducer that turns the events table + (course + competitors) into the in-memory CompetitionState used by REST GET endpoints + WS results pushes (plan 08). Honors:

- ADR-0003 + REQ-EVT-003: all derived state computed by reducers; no precomputed results table.
- REQ-EVT-004 deterministic + idempotent.
- REQ-EVT-CMP-005 D-11: auto-attach card to competitor when card_number is in start list.
- REQ-EVT-CMP-006 D-12: DNF/MP from punch sequence + payload.finish; no time-based auto-DNF in P1.
- UI-SPEC §"Walk-up modal" + §"Readout view live behavior": pendingUnknownCards drives the walk-up trigger; status drives readout color.
- UI-SPEC §"Live results auto-update": reducer output drives /api/competitions/:id/results AND the WS results:<id> channel.

**Codex C-H2 (HIGH) — what changed in revision 2:**

Prior revision derived start/finish from "punch with control_code = 1 OR 4" / "punch with control_code = 2 OR 3 (or 240)". Per codex review C-H2 verified against `packages/sportident/src/output/ndjson.ts` lines 265-279: those code values in Phase 0's surface are control-station punches, NOT start/finish events. Phase 0 emits start/finish/check/clear as **top-level fields** on `CardReadEvent`, converted via `toHalfDayClock(raceResult.startTime)` etc.

Revision 2 LOCKS the reducer to read `payload.start` (HalfDayClock | null) and `payload.finish` (HalfDayClock | null) directly:
- DNF when `payload.finish === null` — regardless of punch sequence.
- Elapsed time = `diffMs(payload.start, payload.finish)` via the new `halfDayClockMath.ts` helper (HalfDayClock → ms within a 24h reference, handles half_day=0/1 wrap-around).
- The START_CODES / FINISH_CODES / CHECK_CODES filtering in the punch loop is GONE — punches[] from Phase 0's `card.raceResult.punches` are already control punches only (Phase 0 strips start/finish/check at the decoder layer). The reducer compares punches[].code against course.controls in order to detect MP.

This plan owns ZERO IO. Four pure modules + five test files. Plan 08 wires reduce() to the WS broadcast loop.

Output: A deterministic, exhaustively-tested reducer the rest of the phase consumes via `import { reduce, detectStatus, matchCardToCompetitor } from '@fartola/edge/src/projection'`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-single-laptop-training-mvp/01-CONTEXT.md
@.planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md
@.planning/phases/01-single-laptop-training-mvp/01-PATTERNS.md
@.planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md
@.planning/phases/01-single-laptop-training-mvp/01-REVIEWS.md
@.planning/adr/0003-event-sourcing-as-core-data-model.md
@.planning/phases/01-single-laptop-training-mvp/01-02-PLAN.md
@.planning/phases/01-single-laptop-training-mvp/01-06-PLAN.md
@packages/sportident/src/output/ndjson.ts

<interfaces>
From plan 02:
- EventPayload union (card_inserted, card_read, card_removed, card_bound, manual_dnf, un_dnf, frame_error, connection_changed). The `card_read` arm carries `start, finish, check, clear: HalfDayClock | null` AND `punches: NdjsonPunch[]` AND `card_holder, punch_count` etc.
- Row types: Event, Competitor, Course, Class, Control, CourseControl.
From plan 06: buildCardReadPayload populates the full CardReadEvent shape; the persisted payload typechecks as Phase 0's `CardReadEvent`.
From `@fartola/sportident`: `HalfDayClock`, `NdjsonPunch` types.

**DNF/MP detection rules (CONTEXT D-12 + RESEARCH §Pattern 5 + codex C-H2):**

- Course has an ordered list of expected control codes (course.control_codes from plan 08 loader; sourced via the course_controls join in plan 02 schema).
- A card_read payload has TOP-LEVEL `start: HalfDayClock | null`, `finish: HalfDayClock | null`, AND `punches: NdjsonPunch[]`.
- DNF detection reads `payload.finish`:
  - `payload.finish === null` → status = 'DNF' regardless of punches.
  - `payload.finish !== null` AND `payload.punches` matches `course.control_codes` in order with no missing/extra → status = 'OK'.
  - `payload.finish !== null` AND `payload.punches` diverges → status = 'MP' with diff arrays.
- Elapsed time:
  - Both `payload.start` and `payload.finish` non-null → `diffMs(start, finish)`. The helper handles wrap-around (start.half_day=1, finish.half_day=0 next-cycle is a 12h+ run).
  - Either null → `elapsed_time_ms = null`.
- The Phase 1 punch-filter constants (START_CODES / FINISH_CODES / CHECK_CODES) that the prior revision used are NO LONGER NEEDED. Phase 0's decoder layer already separates start/finish/check from regular punches; punches[] passed to the reducer contains only control punches. Document this in dnfMp.ts header — "the start/finish/check punch-code constants from revision 1 were removed (codex C-H2). Start/finish times come from payload.start / payload.finish (HalfDayClock fields populated by buildCardReadPayload). If a future Phase 0 codec stuffs start/finish punches BACK into punches[] for some dialect, that's a Phase 0 codec change — re-introduce the filter then, not now."
- manual_dnf event sets status='DNF' regardless of punches; payload.reason shown on receipt.
- un_dnf reverts: the projection re-runs the dnfMp rules against the latest card_read for that competitor.

Reduce ordering: sort events by (eventTimeMs, localSeq). A competitor may have multiple card_read events (re-read); use the latest as canonical; preserve `card_read_history` for receipt mirror.

Matching (REQ-EVT-CMP-005 D-11): card_read with card_number X matches competitor where competitor.cardNumber === X. Course is looked up via competitor.classId → courses.classId. Unknown cards become pendingUnknownCards. card_bound event removes the card_number from pendingUnknownCards.

CompetitionState type (unchanged from prior revision):
```
export interface CompetitionState {
  competition_id: string;
  competitors: Map<string, CompetitorView>;
  results_by_class: Map<string, ResultView[]>;
  pending_unknown_cards: number[];
  last_event_seq: number;
}

export interface CompetitorView {
  id: string;
  name: string;
  club: string | null;
  class_id: string;
  card_number: number | null;
  status: 'PEND' | 'OK' | 'MP' | 'DNF';
  card_read_history: Array<{ event_time_ms: number; card_number: number; punches: NdjsonPunch[]; start: HalfDayClock | null; finish: HalfDayClock | null }>;
  latest_punches: NdjsonPunch[];
  latest_start: HalfDayClock | null;       // NEW (C-H2): kept on the view so the IOF export (plan 16) can render <StartTime>
  latest_finish: HalfDayClock | null;      // NEW (C-H2)
  missing_codes: number[];
  extra_codes: number[];
  out_of_order_codes: number[];
  elapsed_time_ms: number | null;
  manual_dnf_reason: string | null;
}

export interface ResultView {
  competitor_id: string;
  name: string;
  club: string | null;
  status: CompetitorView['status'];
  elapsed_time_ms: number | null;
  place: number | null;
  behind_leader_ms: number | null;
}
```

**HalfDayClock semantics (lifted from Phase 0 toHalfDayClock):**
- `seconds_in_half_day`: 0..(12*3600 - 1) — seconds since the half-day boundary (midnight or noon).
- `half_day`: 0 = AM (00:00-11:59:59), 1 = PM (12:00-23:59:59).
- `weekday`: 0..6 (Sun..Sat) or null if unknown.

`halfDayClockToMs(clock: HalfDayClock): number` → absolute ms within a 24h reference (treating weekday=null as a 24h window): `clock.half_day * 12 * 3600 * 1000 + clock.seconds_in_half_day * 1000`. Caller treats the result as a 24h-relative time.

`diffMs(start: HalfDayClock | null, finish: HalfDayClock | null): number | null`:
- If either null → null.
- Else: `(halfDayClockToMs(finish) - halfDayClockToMs(start) + DAY_MS) % DAY_MS`. The wrap-around handles a midnight crossing (e.g. start 23:50 → finish 01:00 next day = 1h10m, NOT a negative number). For Phase 1 club training, courses are < 12h; this is safe. Document the < 12h assumption in the helper's header.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + matching + halfDayClockMath + dnfMp pure functions (C-H2 start/finish from payload) with exhaustive unit tests</name>
  <files>
    apps/edge/src/projection/types.ts,
    apps/edge/src/projection/matching.ts,
    apps/edge/src/projection/matching.test.ts,
    apps/edge/src/projection/halfDayClockMath.ts,
    apps/edge/src/projection/halfDayClockMath.test.ts,
    apps/edge/src/projection/dnfMp.ts,
    apps/edge/src/projection/dnfMp.test.ts
  </files>
  <read_first>
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Pattern 5: Pure-reducer projection from event log",
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Security Domain V11 Business Logic",
    .planning/phases/01-single-laptop-training-mvp/01-CONTEXT.md D-11 D-12,
    .planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md §"Readout view live behavior" + §"Walk-up modal",
    .planning/phases/01-single-laptop-training-mvp/01-REVIEWS.md §C-H2 (DNF when finish=null; elapsed from start/finish HalfDayClock — NOT from punch codes),
    .planning/adr/0003-event-sourcing-as-core-data-model.md,
    packages/sportident/src/output/ndjson.ts (lines 71-99 for HalfDayClock + NdjsonPunch types; lines 175-185 for toHalfDayClock conversion logic — mirrored by halfDayClockToMs),
    packages/sportident/src/SiCard/IRaceResultData.ts (IPunch shape — control_code, time_ms — but note: ndjson.ts wraps into NdjsonPunch with seconds_in_half_day/half_day/weekday at the boundary; reducer reads NdjsonPunch from events.payload.punches),
    apps/edge/src/db/schema.ts (EventPayload + row types)
  </read_first>
  <action>
    apps/edge/src/projection/types.ts — exports per planner-locked shape (note the new `latest_start` + `latest_finish` fields + `start` + `finish` in card_read_history per C-H2). PATTERNS S-1 header.

    apps/edge/src/projection/matching.ts — pure matching function (unchanged from prior revision):
    ```
    import type { Competitor } from '../db/types.ts';   // C-H5: internal Drizzle row type, not shared-types
    export function matchCardToCompetitor(cardNumber: number, competitors: readonly Competitor[]): Competitor | null {
      for (const c of competitors) if (c.cardNumber === cardNumber) return c;
      return null;
    }
    ```

    **apps/edge/src/projection/halfDayClockMath.ts — NEW (C-H2):**
    ```
    // Authored for fartola. Not ported from upstream.
    // HalfDayClock → milliseconds conversion + start/finish elapsed time.
    // Per codex review C-H2: start/finish on a CardReadEvent are top-level
    // HalfDayClock fields (Phase 0 ndjson.ts lines 265-279), NOT punches with
    // magic control codes. The reducer reads payload.start + payload.finish and
    // computes elapsed via diffMs() — no punch-code branching.
    //
    // ASSUMPTION (Phase 1): club training courses are < 12h. The midnight wrap
    // (start.half_day=1 PM → finish.half_day=0 AM next day) means a real
    // ~12-13h crossing event would compute as the SHORT side of the 24h ring,
    // which for Phase 1 is correct because no Phase 1 training crosses 12h.
    // If Phase 2 introduces multi-day relay legs or rogaining (>12h), this
    // helper needs the weekday field to disambiguate — Phase 0's toHalfDayClock
    // already populates weekday when known.
    import type { HalfDayClock } from '@fartola/sportident';

    const HALF_DAY_MS = 12 * 3600 * 1000;
    const DAY_MS = 24 * 3600 * 1000;

    export function halfDayClockToMs(clock: HalfDayClock): number {
      // half_day * 12h + seconds_in_half_day (already an integer 0..43199)
      return clock.half_day * HALF_DAY_MS + clock.seconds_in_half_day * 1000;
    }

    export function diffMs(start: HalfDayClock | null, finish: HalfDayClock | null): number | null {
      if (start === null || finish === null) return null;
      const s = halfDayClockToMs(start);
      const f = halfDayClockToMs(finish);
      // Wrap to handle midnight crossing within a single 24h ring.
      return ((f - s) % DAY_MS + DAY_MS) % DAY_MS;
    }
    ```

    apps/edge/src/projection/halfDayClockMath.test.ts — node:test:
    - test 1: halfDayClockToMs({ seconds_in_half_day: 0, half_day: 0, weekday: null }) === 0 (midnight).
    - test 2: halfDayClockToMs({ seconds_in_half_day: 0, half_day: 1, weekday: null }) === 12 * 3600 * 1000 (noon).
    - test 3: halfDayClockToMs({ seconds_in_half_day: 30 * 60, half_day: 0, weekday: null }) === 30 * 60 * 1000 (00:30).
    - test 4: diffMs(start AM 10:00, finish AM 10:30) === 30 * 60 * 1000.
    - test 5: diffMs(start AM 11:50, finish PM 12:10) === 20 * 60 * 1000 (across half-day boundary, NOT 12h).
    - test 6: diffMs(start PM 23:50, finish AM 00:10 next day) === 20 * 60 * 1000 (across midnight wrap).
    - test 7: diffMs(null, anything) === null AND diffMs(anything, null) === null.
    - test 8: diffMs same-clock = 0.

    **apps/edge/src/projection/dnfMp.ts — REVISED per C-H2:**
    ```
    // Authored for fartola. Not ported from upstream.
    // DNF/MP detection over a card_read payload. Per codex review C-H2 (revision 2):
    //
    //   - DNF when payload.finish === null. The prior revision filtered punches[]
    //     for FINISH_CODES = {2, 3, 240}; those codes in Phase 0's surface are
    //     CONTROL-STATION punches, not finish events. Phase 0 emits start/finish
    //     as top-level HalfDayClock fields on CardReadEvent
    //     (packages/sportident/src/output/ndjson.ts lines 265-279). The reducer
    //     now reads payload.start and payload.finish directly.
    //
    //   - The START_CODES / FINISH_CODES / CHECK_CODES filter constants from
    //     revision 1 are REMOVED. punches[] from a Phase-0-decoded card_read
    //     contains only control-station punches — the decoder layer already
    //     separates start/finish/check at the storage→raceResult boundary.
    //
    //   - Elapsed time = diffMs(payload.start, payload.finish) from
    //     halfDayClockMath.ts. Caller wraps the call; we return null when either
    //     half-day clock is null.
    //
    // Per CONTEXT D-12 (punch-only DNF, no time-auto-DNF in Phase 1) and UI-SPEC
    // §"Manual DNF override" (manual_dnf wins).
    import type { NdjsonPunch, HalfDayClock } from '@fartola/sportident';
    import { diffMs } from './halfDayClockMath.ts';

    export interface DetectInput {
      start: HalfDayClock | null;
      finish: HalfDayClock | null;
      punches: readonly NdjsonPunch[];
    }
    export interface StatusResult {
      status: 'OK' | 'MP' | 'DNF';
      missing_codes: number[];
      extra_codes: number[];
      out_of_order_codes: number[];
      elapsed_time_ms: number | null;
    }

    export function detectStatus(input: DetectInput, expectedControlCodes: readonly number[]): StatusResult {
      const elapsed = diffMs(input.start, input.finish);

      // DNF gate: no finish stamp → DNF, regardless of how many punches were collected.
      // This is the codex C-H2 LOCKED rule.
      if (input.finish === null) {
        return {
          status: 'DNF',
          missing_codes: [...expectedControlCodes],
          extra_codes: input.punches.map(p => p.code),
          out_of_order_codes: [],
          elapsed_time_ms: null,
        };
      }

      // Order match: expected vs actual punches[], in-order, no skips, no extras.
      // punches[] are control-station punches only (Phase 0 decoder separates start/finish).
      const expected = [...expectedControlCodes];
      const actual = input.punches.map(p => p.code);
      const missing: number[] = [];
      const extra: number[] = [];
      const outOfOrder: number[] = [];
      let ei = 0, ai = 0;
      while (ei < expected.length || ai < actual.length) {
        if (ei < expected.length &amp;&amp; ai < actual.length &amp;&amp; expected[ei] === actual[ai]) { ei++; ai++; continue; }
        if (ei < expected.length &amp;&amp; ai < actual.length &amp;&amp; actual.slice(ai).includes(expected[ei]!)) {
          if (expected.includes(actual[ai]!)) outOfOrder.push(actual[ai]!); else extra.push(actual[ai]!);
          ai++;
          continue;
        }
        if (ei < expected.length) { missing.push(expected[ei]!); ei++; continue; }
        if (ai < actual.length) { extra.push(actual[ai]!); ai++; continue; }
      }
      const status = (missing.length === 0 &amp;&amp; extra.length === 0 &amp;&amp; outOfOrder.length === 0) ? 'OK' : 'MP';
      return { status, missing_codes: missing, extra_codes: extra, out_of_order_codes: outOfOrder, elapsed_time_ms: elapsed };
    }
    ```

    apps/edge/src/projection/matching.test.ts — node:test (unchanged):
    - test 1: empty competitors → null.
    - test 2: cardNumber match → matched competitor; cardNumber mismatch → null.
    - test 3: multiple competitors with overlapping classes — match by cardNumber only.

    apps/edge/src/projection/dnfMp.test.ts — node:test (REVISED LOCKED matrix per C-H2):

    Helper (top of test file) — builds a HalfDayClock concisely:
    ```
    const hd = (totalSeconds: number): HalfDayClock => ({
      seconds_in_half_day: totalSeconds % (12*3600),
      half_day: totalSeconds < 12*3600 ? 0 : 1,
      weekday: null,
    });
    ```

    - **test 1 OK:** detectStatus({ start: hd(10*3600), finish: hd(10*3600 + 600), punches: [{code:31,...},{code:32,...},{code:33,...},{code:34,...}] }, [31,32,33,34]) → status='OK', elapsed_time_ms=600*1000, no missing/extra/out_of_order. (10-minute leg, AM, all 4 controls in order.)
    - **test 2 DNF (finish=null) — codex C-H2 explicit gate:** detectStatus({ start: hd(10*3600), finish: null, punches: [{code:31,...},{code:32,...}] }, [31,32,33,34]) → status='DNF', elapsed_time_ms=null, missing_codes=[31,32,33,34], extra_codes=[31,32], out_of_order_codes=[]. The DNF mark comes from `finish === null`, NOT from "punches doesn't contain a finish-code" reasoning.
    - **test 2b NEW (codex C-H2 explicit gate — finish=null trumps full punches):** detectStatus({ start: hd(10*3600), finish: null, punches: [{code:31},{code:32},{code:33},{code:34}] }, [31,32,33,34]) → status='DNF' (NOT 'OK'). Even with all 4 control punches present, missing payload.finish marks DNF. This is the regression gate for the revision-1 bug where MP would have been emitted because the reducer guessed "finish punch = code 2 OR 3" was missing — but a clean run with no finish stamp (because the operator killed the read before completing the round) is genuinely DNF.
    - **test 3 MP missing-middle:** detectStatus({ start: hd(10*3600), finish: hd(10*3600+600), punches: [{code:31},{code:33},{code:34}] }, [31,32,33,34]) → status='MP', missing_codes=[32].
    - **test 4 MP extra:** punches: [31, 32, 99, 33, 34] (with finish ≠ null) vs [31,32,33,34] → status='MP', extra_codes=[99].
    - **test 5 MP out-of-order:** punches: [31, 33, 32, 34] (with finish ≠ null) → status='MP', out_of_order_codes=[33].
    - **test 6 elapsed null when no start:** detectStatus({ start: null, finish: hd(10*3600+600), punches: [{code:31}..{code:34}] }, [31,32,33,34]) → status='OK', elapsed_time_ms=null.
    - **test 7 empty punches AND finish null:** detectStatus({ start: null, finish: null, punches: [] }, [31,32,33,34]) → status='DNF', elapsed_time_ms=null.
    - **test 8 empty punches but finish present:** detectStatus({ start: hd(10*3600), finish: hd(10*3600+5), punches: [] }, [31,32,33,34]) → status='MP', missing_codes=[31,32,33,34], elapsed_time_ms=5000.
    - **test 9 elapsed across midnight wrap:** detectStatus({ start: hd(23*3600 + 50*60), finish: hd(24*3600 + 10*60) — i.e. AM 00:10 next day }, punches: full match) → elapsed_time_ms=20*60*1000.
    - **test 10 elapsed across AM/PM half-day boundary:** detectStatus({ start: hd(11*3600 + 50*60), finish: hd(12*3600 + 10*60) }, punches: full match) → elapsed_time_ms=20*60*1000.

    NOTE: The prior revision had a test "adversarial — duplicate finish in punches[]" — that test is REMOVED because punches[] no longer carries start/finish events in Phase 0's surface. If Phase 2's codec changes to merge start/finish back into punches[] for a specific dialect, re-introduce a Phase 0 filter step then.

    Apply PATTERNS S-1 header to every .ts file. The dnfMp.ts header MUST cite CONTEXT D-12 + UI-SPEC §"Manual DNF override" + codex C-H2 + the explicit removal of START_CODES / FINISH_CODES / CHECK_CODES from revision 1.
  </action>
  <verify>
    <automated>cd /home/jonas/src/fartOLa-phase-1 &amp;&amp; pnpm --filter @fartola/edge typecheck &amp;&amp; pnpm --filter @fartola/edge test 2>&amp;1 | grep -E '(pass|fail) [0-9]+|matching|dnfMp|halfDayClock'</automated>
  </verify>
  <done>
    detectStatus passes all 10 LOCKED scenarios including test 2b (the explicit C-H2 regression — finish=null → DNF regardless of punches). halfDayClockMath passes 8 scenarios including wrap-around cases. matchCardToCompetitor passes 3 scenarios. Total ~21 new tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: reduce.ts orchestration (reads payload.start + payload.finish per C-H2) + idempotency tests + index.ts barrel</name>
  <files>
    apps/edge/src/projection/reduce.ts,
    apps/edge/src/projection/reduce.test.ts,
    apps/edge/src/projection/idempotent.test.ts,
    apps/edge/src/projection/index.ts
  </files>
  <read_first>
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Pattern 5: Pure-reducer projection",
    .planning/phases/01-single-laptop-training-mvp/01-CONTEXT.md D-11 D-12,
    .planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md §"Live results auto-update",
    .planning/phases/01-single-laptop-training-mvp/01-REVIEWS.md §C-H2 (reducer reads payload.start + payload.finish; new DNF scenario finish=null → DNF),
    apps/edge/src/projection/dnfMp.ts (just written),
    apps/edge/src/projection/matching.ts (just written),
    apps/edge/src/projection/halfDayClockMath.ts (just written),
    apps/edge/src/db/schema.ts (EventPayload + the C-H2 card_read arm with start/finish/check/clear),
    apps/edge/src/db/types.ts (internal Drizzle row types per C-H5)
  </read_first>
  <action>
    apps/edge/src/projection/reduce.ts — revised to read payload.start + payload.finish:
    ```
    // Authored for fartola. Not ported from upstream.
    // Pure reducer over the event log → CompetitionState. Per codex review C-H2:
    // card_read payload carries top-level start/finish/check/clear HalfDayClock
    // fields. The reducer reads payload.start + payload.finish for elapsed-time
    // and DNF detection — NOT from punches[] with magic control codes.
    import type { Event, Competitor, Course, Class } from '../db/types.ts';
    import type { EventPayload } from '../db/schema.ts';
    import type { NdjsonPunch, HalfDayClock } from '@fartola/sportident';
    import { detectStatus } from './dnfMp.ts';
    import { matchCardToCompetitor } from './matching.ts';
    import type { CompetitionState, CompetitorView, ResultView } from './types.ts';

    export interface ReduceInput {
      competition_id: string;
      events: readonly Event[];
      competitors: readonly Competitor[];
      classes: readonly Class[];
      courses: readonly (Course &amp; { control_codes: readonly number[] })[];
    }

    export function reduce(input: ReduceInput): CompetitionState {
      const sortedEvents = [...input.events].sort((a, b) => (a.eventTimeMs - b.eventTimeMs) || (a.localSeq - b.localSeq));

      const courseByClass = new Map<string, Course &amp; { control_codes: readonly number[] }>();
      for (const c of input.courses) if (c.classId) courseByClass.set(c.classId, c);
      const competitorsByCompetition = input.competitors.filter(c => c.competitionId === input.competition_id);

      const competitorViews = new Map<string, CompetitorView>();
      for (const c of competitorsByCompetition) {
        competitorViews.set(c.id, {
          id: c.id, name: c.name, club: c.club, class_id: c.classId, card_number: c.cardNumber,
          status: 'PEND', card_read_history: [],
          latest_punches: [], latest_start: null, latest_finish: null,
          missing_codes: [], extra_codes: [], out_of_order_codes: [],
          elapsed_time_ms: null, manual_dnf_reason: null,
        });
      }
      const pendingUnknownCards = new Set<number>();
      let lastEventSeq = 0;

      for (const e of sortedEvents) {
        if (e.competitionId !== input.competition_id) continue;
        lastEventSeq = Math.max(lastEventSeq, e.localSeq);
        const payload = e.payload as EventPayload;
        switch (payload.event_type) {
          case 'card_read': {
            const competitor = matchCardToCompetitor(payload.card_number, competitorsByCompetition);
            if (!competitor) { pendingUnknownCards.add(payload.card_number); break; }
            const view = competitorViews.get(competitor.id);
            if (!view) break;
            view.card_read_history.push({
              event_time_ms: e.eventTimeMs,
              card_number: payload.card_number,
              punches: payload.punches,
              start: payload.start,
              finish: payload.finish,
            });
            view.latest_punches = payload.punches;
            view.latest_start = payload.start;
            view.latest_finish = payload.finish;
            const course = courseByClass.get(competitor.classId);
            const expected = course ? course.control_codes : [];
            const detected = detectStatus({ start: payload.start, finish: payload.finish, punches: payload.punches }, expected);
            if (view.manual_dnf_reason === null) {
              view.status = detected.status;
              view.missing_codes = detected.missing_codes;
              view.extra_codes = detected.extra_codes;
              view.out_of_order_codes = detected.out_of_order_codes;
              view.elapsed_time_ms = detected.elapsed_time_ms;
            }
            break;
          }
          case 'card_bound': {
            pendingUnknownCards.delete(payload.card_number);
            break;
          }
          case 'manual_dnf': {
            const view = competitorViews.get(payload.competitor_id);
            if (view) { view.status = 'DNF'; view.manual_dnf_reason = payload.reason; }
            break;
          }
          case 'un_dnf': {
            const view = competitorViews.get(payload.competitor_id);
            if (view) {
              view.manual_dnf_reason = null;
              const competitor = competitorsByCompetition.find(c => c.id === payload.competitor_id);
              const course = competitor ? courseByClass.get(competitor.classId) : undefined;
              const expected = course ? course.control_codes : [];
              if (view.latest_punches.length > 0 || view.latest_finish !== null || view.latest_start !== null) {
                const detected = detectStatus({ start: view.latest_start, finish: view.latest_finish, punches: view.latest_punches }, expected);
                view.status = detected.status;
                view.missing_codes = detected.missing_codes; view.extra_codes = detected.extra_codes; view.out_of_order_codes = detected.out_of_order_codes; view.elapsed_time_ms = detected.elapsed_time_ms;
              } else { view.status = 'PEND'; view.missing_codes = []; view.extra_codes = []; view.out_of_order_codes = []; view.elapsed_time_ms = null; }
            }
            break;
          }
          // card_inserted, card_removed, frame_error, connection_changed do not change projection state.
        }
      }

      // results_by_class (unchanged from prior revision).
      const resultsByClass = new Map<string, ResultView[]>();
      for (const cls of input.classes) {
        const inClass: CompetitorView[] = [];
        for (const v of competitorViews.values()) if (v.class_id === cls.id) inClass.push(v);
        const order = { OK: 0, MP: 1, DNF: 2, PEND: 3 };
        inClass.sort((a, b) => order[a.status] - order[b.status]
          || ((a.elapsed_time_ms ?? Number.MAX_SAFE_INTEGER) - (b.elapsed_time_ms ?? Number.MAX_SAFE_INTEGER))
          || a.name.localeCompare(b.name));
        let place = 0; let leaderTime: number | null = null;
        const rows: ResultView[] = inClass.map(v => {
          let p: number | null = null;
          let behind: number | null = null;
          if (v.status === 'OK' &amp;&amp; v.elapsed_time_ms !== null) {
            place++;
            p = place;
            if (leaderTime === null) leaderTime = v.elapsed_time_ms;
            behind = v.elapsed_time_ms - leaderTime;
          }
          return { competitor_id: v.id, name: v.name, club: v.club, status: v.status, elapsed_time_ms: v.elapsed_time_ms, place: p, behind_leader_ms: behind };
        });
        resultsByClass.set(cls.id, rows);
      }

      return {
        competition_id: input.competition_id,
        competitors: competitorViews,
        results_by_class: resultsByClass,
        pending_unknown_cards: [...pendingUnknownCards].sort((a, b) => a - b),
        last_event_seq: lastEventSeq,
      };
    }
    ```

    apps/edge/src/projection/index.ts — barrel:
    ```
    export * from './types.ts';
    export { reduce } from './reduce.ts';
    export { detectStatus } from './dnfMp.ts';
    export { matchCardToCompetitor } from './matching.ts';
    export { halfDayClockToMs, diffMs } from './halfDayClockMath.ts';
    ```

    apps/edge/src/projection/reduce.test.ts — node:test (REVISED matrix per C-H2):

    Helper (top of test file) — builds a card_read event with the full CardReadEvent payload shape:
    ```
    const hd = (totalSeconds: number): HalfDayClock => ({ seconds_in_half_day: totalSeconds % (12*3600), half_day: totalSeconds < 12*3600 ? 0 : 1, weekday: null });
    const cardReadEvent = (overrides: Partial<{competitionId: string; cardNumber: number; localSeq: number; eventTimeMs: number; start: HalfDayClock | null; finish: HalfDayClock | null; punches: NdjsonPunch[]}>) => ({
      nodeId: 'test-node', localSeq: overrides.localSeq ?? 1, competitionId: overrides.competitionId ?? 'A',
      eventType: 'card_read' as const, eventTimeMs: overrides.eventTimeMs ?? Date.now(), recordedAtMs: Date.now(),
      payload: {
        event_type: 'card_read' as const,
        card_number: overrides.cardNumber ?? 7501853, card_type: 'SI10',
        start: overrides.start ?? hd(10*3600), finish: overrides.finish ?? hd(10*3600 + 600),
        check: null, clear: null, punch_count: (overrides.punches ?? []).length,
        punches: overrides.punches ?? [], card_holder: null,
      },
    });
    ```

    - test 1 hello world: empty events + 0 competitors → empty state.
    - test 2 single OK: 1 competitor (Anna, card 7501853, class H21), 1 course (controls 31,32,33,34), card_read with finish !== null + valid punches → Anna status='OK', elapsed_time_ms=600*1000; results_by_class.get(H21)[0].place=1.
    - test 3 walk-up flow: card_read with unknown card_number=9999999 → pending_unknown_cards=[9999999]. After card_bound, pending_unknown_cards=[].
    - test 4 multiple competitors mixed status (3 in same class — OK, MP, DNF). results_by_class sort order: OK → MP → DNF.
    - test 5 manual_dnf override: card_read OK → manual_dnf → status='DNF'.
    - test 6 un_dnf reverts: card_read OK → manual_dnf → un_dnf → status='OK'.
    - test 7 cross-competition isolation.
    - test 8 latest card_read wins (two reads for same competitor).
    - test 9 places skip null elapsed.
    - test 10 behind_leader: 3 OK competitors places 1/2/3.
    - **test 11 NEW (C-H2 explicit scenario — finish=null → DNF regardless of punch sequence):** Anna has card_read with payload.finish=null AND all 4 control punches present in the right order. reduce() output: Anna.status='DNF' (NOT 'OK'). This is the explicit reducer-level C-H2 regression gate — proves the reducer reads payload.finish, not "punches contains a magic code".
    - **test 12 NEW (elapsed reads HalfDayClock pair):** Anna has card_read with payload.start = AM 10:00, payload.finish = AM 10:15, punches matching course. reduce() output: Anna.elapsed_time_ms === 15*60*1000.
    - test 13 NEW (history preserves start + finish): card_read x2 for Anna; assert view.card_read_history[0].finish + history[1].finish are the two distinct HalfDayClock objects, not nulled.

    apps/edge/src/projection/idempotent.test.ts — REQ-EVT-004:
    - test 1: For each scenario in reduce.test.ts test 2-12, run reduce TWICE → structurally identical.
    - test 2: Shuffled-event-order input produces same output as sorted.
    - test 3: 1000-event synthetic stream completes in < 200ms.

    Apply PATTERNS S-1 header to every new .ts file.
  </action>
  <verify>
    <automated>cd /home/jonas/src/fartOLa-phase-1 &amp;&amp; pnpm --filter @fartola/edge typecheck &amp;&amp; pnpm --filter @fartola/edge test 2>&amp;1 | grep -E '(pass|fail) [0-9]+|reduce|idempotent'</automated>
  </verify>
  <done>
    reduce.ts is a pure function reading payload.start + payload.finish (NOT punch codes) per C-H2. reduce.test.ts covers 13 scenarios including test 11 (finish=null → DNF explicit gate) + test 12 (elapsed from HalfDayClock pair) + test 13 (history preserves clocks). idempotent.test.ts asserts REQ-EVT-004. ~16 new tests pass. The barrel index.ts is the import path Plan 08 + Plan 11 + Plan 16 will use.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| events table → reduce() | All inputs trusted (already validated); pure function has no IO |
| reducer output → REST + WS | Snake_case fields ready for wire |
| payload.start + payload.finish → status derivation | C-H2: Phase 0's CardReadEvent shape is the wire contract; reducer reads top-level fields, not punch-code guesses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DNF-LOGIC | Business Logic | dnfMp.ts | mitigate | 10 locked test scenarios covering OK/MP/DNF including the codex C-H2 test 2b (finish=null → DNF regardless of full punches[]). |
| T-PAYLOAD-DRIFT (consumer side) | Tampering / Reliability | reduce.ts reads payload.start + payload.finish | mitigate (C-H2) | The reducer's `EventPayload['card_read']` discriminated arm includes start/finish/check/clear as HalfDayClock fields. Phase 0's CardReadEvent type is the source of truth (plan 02 schema imports it; plan 06 bridge persists it). If Phase 0 renames a field, both plans break at compile time. reduce.test.ts test 11 + test 12 are runtime gates. |
| T-IDEMPOTENT-BREAK | Tampering | reduce.ts ordering | mitigate | Events sorted by (event_time_ms, local_seq); idempotent.test.ts asserts two runs identical. |
| T-CROSS-COMP-LEAK | Privacy | reduce.ts | mitigate | input.events filtered by competition_id inside the loop; test 7. |
| T-MANUAL-DNF-RACE | Business Logic | manual_dnf vs subsequent card_read | mitigate | manual_dnf_reason gate skips card_read overwrite. Tests 5 + 6. |
| T-WALL-CLOCK-WRAP | Reliability | halfDayClockMath.diffMs across midnight | mitigate | diffMs uses 24h-ring modulo; halfDayClockMath.test.ts test 6 asserts midnight wrap. Phase 1 < 12h course assumption documented. |
</threat_model>

<verification>
1. `pnpm --filter @fartola/edge typecheck` exits 0.
2. `pnpm --filter @fartola/edge test` exits 0 with ~37 new tests passing.
3. The reduce() function is import-only — no IO, no Drizzle imports, no Fastify imports. Verified by grep.
4. Plan 08 can `import { reduce, type CompetitionState } from '../projection/index.ts'`.
5. C-H2 reducer-level regression gates: dnfMp.test.ts test 2b (finish=null → DNF with full punches) + reduce.test.ts test 11 (same scenario through reduce) + reduce.test.ts test 12 (elapsed from HalfDayClock pair) + reduce.test.ts test 13 (history preserves clocks).
</verification>

<success_criteria>
- Pure reducer landed and exhaustively tested.
- **Codex C-H2 chain fully closed at the reducer layer:** reducer reads payload.start + payload.finish from CardReadEvent top-level fields; punches[] is used ONLY for MP order-match (not for start/finish guessing); finish=null → DNF; elapsed = diffMs(start, finish).**
- halfDayClockMath helper handles wrap-around (midnight + AM/PM half-day boundary).
- REQ-EVT-003 + REQ-EVT-004 satisfied with automated tests.
- Walk-up trigger path (pending_unknown_cards) wired for plan 14.
- Wave 2 plan 08 (WS results channel) unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/01-single-laptop-training-mvp/01-07-SUMMARY.md` using the GSD summary template.
Record:
- Performance: idempotent.test.ts test 3 actual runtime for 1000 events.
- Any reduce.ts refactor (executor may have collapsed maps).
- Confirmation that the punch-code constants (START/FINISH/CHECK) from revision 1 are NOT present anywhere in the codebase (grep `START_CODES\|FINISH_CODES\|CHECK_CODES` apps/edge/src/projection/ → zero matches). The C-H2 removal is mechanically clean.
- Confirmation that the SI10 Jonas fixture replayed through plan 06's bridge produces a card_read payload with the C-H2 fields populated, and the reduce.ts handles it without referencing punch codes for start/finish.
</output>
