---
phase: 01-single-laptop-training-mvp
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - apps/edge/package.json
  - apps/edge/tsconfig.json
  - apps/edge/tsup.config.ts
  - apps/edge/src/server.ts
  - apps/edge/src/bin/fartol.ts
  - apps/edge/src/routes/health.ts
  - apps/edge/src/server.test.ts
  - apps/web/package.json
  - apps/web/svelte.config.js
  - apps/web/vite.config.ts
  - apps/web/vitest.config.ts
  - apps/web/tsconfig.json
  - apps/web/src/app.html
  - apps/web/src/routes/+layout.ts
  - apps/web/src/routes/+layout.svelte
  - apps/web/src/routes/+page.svelte
  - apps/web/static/manifest.webmanifest
  - packages/shared-types/package.json
  - packages/shared-types/tsconfig.json
  - packages/shared-types/src/index.ts
  - packages/shared-types/src/events.ts
  - packages/shared-types/src/dtos.ts
  - packages/shared-types/src/ws.ts
  - packages/shared-types/src/index.test.ts
  - playwright.config.ts
autonomous: true
tags: [scaffold, monorepo, fastify, sveltekit, shared-types]
requirements:
  - REQ-OPS-001
  - REQ-UI-001
must_haves:
  truths:
    - "pnpm install --frozen-lockfile completes from clean workspace"
    - "pnpm -r typecheck exits 0 across @fartol/edge, @fartol/web, @fartol/shared-types"
    - "pnpm -r test exits 0 (placeholder tests in all three packages)"
    - "apps/web/src/routes/+page.svelte renders the AGPL string 'FartOL' on cold dev boot"
    - "apps/edge/src/server.ts boots Fastify on 127.0.0.1:3000 (no LAN exposure)"
    - "GET /api/health returns 200 { status: 'ok' } against the booted server"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace includes apps/* and packages/*"
      contains: "apps/*"
    - path: "apps/edge/package.json"
      provides: "@fartol/edge manifest with bin: fartol, type: module, node:test script"
      contains: "@fartol/edge"
    - path: "apps/edge/src/server.ts"
      provides: "Fastify app factory + register sensible/cors/static plugins + setNotFoundHandler"
      min_lines: 40
    - path: "apps/edge/src/bin/fartol.ts"
      provides: "Binary entrypoint with isEntrypoint guard, parses --port/--db-path/--bind-host, boots server"
      min_lines: 40
    - path: "apps/web/svelte.config.js"
      provides: "@sveltejs/adapter-static with fallback: '200.html', strict: false"
      contains: "200.html"
    - path: "apps/web/vite.config.ts"
      provides: "Dev proxy block for /api → :3000 and /ws → ws://:3000"
      contains: "proxy"
    - path: "packages/shared-types/src/index.ts"
      provides: "Barrel re-exports of events + dtos + ws"
      contains: "export"
    - path: "playwright.config.ts"
      provides: "Repo-root Playwright config pointing testDir to tests/e2e/"
      contains: "testDir"
  key_links:
    - from: "apps/edge/src/bin/fartol.ts"
      to: "apps/edge/src/server.ts"
      via: "buildServer() factory import"
      pattern: "buildServer|createServer"
    - from: "apps/edge/src/server.ts"
      to: "127.0.0.1"
      via: "app.listen({ host: '127.0.0.1' })"
      pattern: "127\\.0\\.0\\.1"
    - from: "apps/web/vite.config.ts"
      to: "http://localhost:3000"
      via: "server.proxy['/api'] target"
      pattern: "localhost:3000"
---

<objective>
Land the Phase 1 monorepo skeleton: three new workspace packages (`apps/edge/`, `apps/web/`, `packages/shared-types/`), the workspace manifest update, repo-root e2e config, and a minimal but *real* Fastify + SvelteKit + shared-types triangle that boots end-to-end on `pnpm dev` (Vite :5173 with proxy to Fastify :3000).

This plan is the foundation for everything else in Phase 1. After this plan, the workspace installs, typechecks, runs unit tests, and a Fastify server actually answers GET /api/health from the SvelteKit dev page. No DB, no SI bridge, no WS yet — those land in plans 02/03.

Purpose: Unblock all downstream plans. Per CONTEXT.md D-05 (workspace shape), D-06 (single fartol binary), D-07 (adapter-static SPA), D-08 (shared-types pure-TS).

Output:
- Modified `pnpm-workspace.yaml` (one-line `apps/*` addition).
- `apps/edge/`, `apps/web/`, `packages/shared-types/` package skeletons (manifests, tsconfig, build configs, smallest-possible source + test files).
- Repo-root `playwright.config.ts`.
- `package.json` root scripts: `test:quick` (filters edge + shared-types unit only) and `e2e` (Playwright).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-single-laptop-training-mvp/01-CONTEXT.md
@.planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md
@.planning/phases/01-single-laptop-training-mvp/01-PATTERNS.md
@.planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md
@pnpm-workspace.yaml
@package.json
@tsconfig.json
@packages/sportident/package.json
@packages/sportident/tsconfig.json
@packages/sportident/tsup.config.ts
@packages/sportident/src/index.ts

<interfaces>
Phase 0 surface to consume (from packages/sportident/src/index.ts):
- `SerialTransport`, `SiMainStation`
- `NdjsonEmitter`, `emitDiagnostic`
- Types: `NdjsonEvent`, `NdjsonBase`, `CardType`, `NdjsonPunch`, `ConnectionChangedEvent`, `CardInsertedEvent`, `CardReadEvent`, `CardRemovedEvent`, `FrameErrorEvent`

These are NOT imported in this plan (no SI bridge yet) — but `packages/shared-types/src/events.ts` re-exports the Ndjson* types so downstream consumers have a single import path.

Workspace dependency contract (locked here):
- `apps/edge/` depends on `@fartol/sportident@workspace:*` and `@fartol/shared-types@workspace:*`
- `apps/web/` depends on `@fartol/shared-types@workspace:*`
- `packages/shared-types/` depends on `@fartol/sportident@workspace:*` (for re-export of Ndjson types)
- `packages/sportident/` has no new deps (frozen from Phase 0)

Root tsconfig (frozen — DO NOT MODIFY):
- strict, noUncheckedIndexedAccess, exactOptionalPropertyTypes, erasableSyntaxOnly, verbatimModuleSyntax, allowImportingTsExtensions, noEmit
- Every new tsconfig MUST `extends: "../../tsconfig.json"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update workspace manifest + create packages/shared-types/</name>
  <files>
    pnpm-workspace.yaml,
    packages/shared-types/package.json,
    packages/shared-types/tsconfig.json,
    packages/shared-types/src/index.ts,
    packages/shared-types/src/events.ts,
    packages/shared-types/src/dtos.ts,
    packages/shared-types/src/ws.ts,
    packages/shared-types/src/index.test.ts
  </files>
  <read_first>
    pnpm-workspace.yaml (preserve the 5-line provenance header verbatim — codex review #10),
    packages/sportident/package.json (analog),
    packages/sportident/tsconfig.json (analog),
    packages/sportident/src/index.ts (barrel pattern),
    packages/sportident/src/output/ndjson.ts (re-export source — read lines 1-150 only),
    .planning/phases/01-single-laptop-training-mvp/01-PATTERNS.md §"packages/shared-types/" (exact analog),
    .planning/phases/01-single-laptop-training-mvp/01-PATTERNS.md §"Pattern S-1: File-header comment block",
    .planning/phases/01-single-laptop-training-mvp/01-PATTERNS.md §"Pattern S-6: Snake_case at the I/O boundary",
    .planning/phases/01-single-laptop-training-mvp/01-PATTERNS.md §"Pattern S-8: AGPL vs MIT licence boundary"
  </read_first>
  <action>
    Edit pnpm-workspace.yaml: append one line `  - 'apps/*'` after the existing `  - 'packages/*'` line. Preserve the existing 5-line comment header verbatim.

    Create packages/shared-types/ as a pure-TS workspace package (no build step). Per CONTEXT D-08 + PATTERNS S-8, license is MIT (re-exports MIT Phase 0 types).

    packages/shared-types/package.json — copy shape from packages/sportident/package.json. Required fields:
    - name: "@fartol/shared-types"
    - version: "0.0.0"
    - private: true
    - type: "module"
    - license: "MIT" (PATTERNS S-8)
    - engines.node: ">=22.18.0"
    - exports: { ".": "./src/index.ts" }  (pure-TS — no dist; consumers rely on root tsconfig allowImportingTsExtensions)
    - scripts: { test: "node --test --test-reporter=spec 'src/**/*.test.ts'", typecheck: "tsc --noEmit" }
    - dependencies: { "@fartol/sportident": "workspace:*" }

    packages/shared-types/tsconfig.json — verbatim copy from packages/sportident/tsconfig.json (extends "../../tsconfig.json", rootDir ".", include ["src/**/*.ts"]).

    packages/shared-types/src/index.ts — barrel (PATTERNS S-1 header + analog packages/sportident/src/index.ts):
    - Section "// --- NDJSON events ---" re-exports from './events.ts'
    - Section "// --- REST DTOs ---" re-exports from './dtos.ts'
    - Section "// --- WebSocket envelopes ---" re-exports from './ws.ts'

    packages/shared-types/src/events.ts — re-export Phase 0 NDJSON types. Use `export type { NdjsonEvent, NdjsonBase, CardType, HalfDayClock, NdjsonPunch, ConnectionChangedEvent, CardInsertedEvent, CardReadEvent, CardRemovedEvent, FrameErrorEvent } from '@fartol/sportident';`. Add `export const EVENT_SCHEMA_VERSION = 1 as const;` for downstream version pinning. Snake_case at the boundary (PATTERNS S-6) — these types already are.

    packages/shared-types/src/dtos.ts — minimal placeholder DTO union. For Phase 1 plan 01 just export the type stubs the downstream plans will flesh out:
    ```
    export interface CompetitionDTO { id: string; name: string; date: string; receipt_template: string; auto_print: boolean; created_at_ms: number; }
    export interface CompetitorDTO { id: string; competition_id: string; name: string; club: string | null; class_id: string; card_number: number | null; consent_at_ms: number | null; consent_status: 'explicit' | 'pending_first_read' | 'confirmed_on_read'; }
    export interface HealthDTO { status: 'ok'; node_id: string; uptime_ms: number; }
    ```
    Note: snake_case at the wire boundary (PATTERNS S-6, CONTEXT D-15). Each later plan adds fields as needed.

    packages/shared-types/src/ws.ts — channel name builders + envelope discriminator. Stubs:
    ```
    export type ChannelName = `readout:${string}` | `results:${string}`;
    export function readoutChannel(competitionId: string): ChannelName { return `readout:${competitionId}`; }
    export function resultsChannel(competitionId: string): ChannelName { return `results:${competitionId}`; }
    export interface WsEnvelope<T = unknown> { type: string; channel?: ChannelName; payload: T; seq?: number; }
    export interface WsHelloMessage { type: 'hello'; channels: ChannelName[]; last_seen_seq: number; }
    export interface WsSubscribeMessage { type: 'subscribe'; channel: ChannelName; }
    ```

    packages/shared-types/src/index.test.ts — single node:test asserting `EVENT_SCHEMA_VERSION === 1` and `readoutChannel('abc') === 'readout:abc'`. Pattern from packages/sportident/src/output/ndjson.test.ts header.

    Every .ts file MUST start with the PATTERNS S-1 file-header preamble (3-25 lines: what + provenance "Authored for fartol. Not ported from upstream." + which planning doc locks behavior). The packages/shared-types/src/events.ts header MUST note that the re-export from @fartol/sportident is intentional drift-prevention per PATTERNS §"shared-types/src/events.ts".
  </action>
  <verify>
    <automated>cd /home/jonas/src/FartOL-phase-1 &amp;&amp; pnpm install --frozen-lockfile=false &amp;&amp; pnpm --filter @fartol/shared-types typecheck &amp;&amp; pnpm --filter @fartol/shared-types test</automated>
  </verify>
  <done>
    pnpm-workspace.yaml lists both 'packages/*' and 'apps/*'. packages/shared-types/ installs, typechecks under root strict + allowImportingTsExtensions, and the placeholder test passes. `import { EVENT_SCHEMA_VERSION, readoutChannel } from '@fartol/shared-types'` resolves from a sibling package. No build step required.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create apps/edge/ skeleton with Fastify boot + /api/health</name>
  <files>
    apps/edge/package.json,
    apps/edge/tsconfig.json,
    apps/edge/tsup.config.ts,
    apps/edge/src/server.ts,
    apps/edge/src/bin/fartol.ts,
    apps/edge/src/routes/health.ts,
    apps/edge/src/server.test.ts
  </files>
  <read_first>
    packages/sportident/package.json (analog for manifest shape),
    packages/sportident/tsup.config.ts (analog for build config),
    packages/sportident/src/bin/fartol-readout.ts (analog for bin entrypoint structure — lines 1-50 header, lines 86-137 argv parsing, lines 257-325 shutdown + isEntrypoint guard),
    packages/sportident/src/output/ndjson.test.ts (node:test header pattern — lines 17-32),
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Pattern 3: Fastify SPA fallback" (lines 364-402 — verbatim usable as src/server.ts skeleton minus the import routes call),
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Security Domain V4 + V14" (bind 127.0.0.1 only — T-WS-FAN-OUT mitigation),
    .planning/phases/01-single-laptop-training-mvp/01-PATTERNS.md §"apps/edge/src/bin/fartol.ts" + §"apps/edge/package.json" + §"apps/edge/tsconfig.json" + §"apps/edge/tsup.config.ts",
    .planning/phases/01-single-laptop-training-mvp/01-PATTERNS.md §"Pattern S-1" + §"Pattern S-2 sink injection" + §"Pattern S-7 isEntrypoint guard"
  </read_first>
  <action>
    Create apps/edge/ as a workspace package — the Fastify bridge entry. License: AGPL-3.0-or-later (PATTERNS S-8).

    apps/edge/package.json — copy shape from packages/sportident/package.json with overrides:
    - name: "@fartol/edge"
    - version: "0.0.0"
    - private: true
    - type: "module"
    - license: "AGPL-3.0-or-later"
    - engines.node: ">=22.18.0"
    - bin: { "fartol": "./dist/bin/fartol.cjs" }
    - exports: { ".": { "types": "./dist/server.d.ts", "import": "./dist/server.mjs" } }
    - scripts:
      - build: "tsup"
      - dev: "tsx watch src/bin/fartol.ts"
      - test: "node --test --test-reporter=spec --import tsx 'src/**/*.test.ts'"
      - test:watch: "node --test --watch --test-reporter=spec --import tsx 'src/**/*.test.ts'"
      - typecheck: "tsc --noEmit"
      - lint: "eslint src"
    - dependencies: fastify@^5.8.5, @fastify/static@^9.1.3, @fastify/cors@^11.2.0, @fastify/sensible@^6.0.4, "@fartol/shared-types": "workspace:*", "@fartol/sportident": "workspace:*"
    - devDependencies: @types/node@^22.10.0, tsx@^4.20.0, tsup@^8.3.5, typescript@^5.6.3

    Note: @fastify/websocket, better-sqlite3, drizzle-orm/kit, fast-xml-parser, libxmljs2-xsd, node-thermal-printer, zod, i18next are added by later plans — NOT here. Keeping plan 01 dependency surface tight reduces install failure modes.

    apps/edge/tsconfig.json — verbatim from packages/sportident/tsconfig.json (extends "../../tsconfig.json", rootDir ".", include ["src/**/*.ts", "test/**/*.ts"]).

    apps/edge/tsup.config.ts — verbatim from packages/sportident/tsup.config.ts with entry change:
    - entry: ['src/server.ts', 'src/bin/fartol.ts']
    - format: ['esm', 'cjs']
    - dts: true
    - sourcemap: true, clean: true
    - target: 'node22'
    - splitting: false
    - outDir: 'dist'
    - outExtension({ format }): { js: format === 'esm' ? '.mjs' : '.cjs' }
    - esbuildOptions: silence 'empty-import-meta'

    apps/edge/src/server.ts — Fastify app factory (RESEARCH Pattern 3, modified — no static yet because apps/web/build/ doesn't exist at install time; that wires in plan 11). Export `buildServer(opts: { logger?: boolean }): FastifyInstance` that:
    - Constructs Fastify with logger from opts (default true in production, false in tests)
    - Registers @fastify/sensible
    - Registers @fastify/cors with origin: 127.0.0.1 only (T-CORS mitigation)
    - Mounts apps/edge/src/routes/health.ts at /api/health
    - Sets setNotFoundHandler returning { error: 'Not found' } for /api/* and /ws (full SPA fallback added in plan 11)
    - Returns the app instance (does NOT call .listen — listen is the bin's job, PATTERNS S-7)

    apps/edge/src/routes/health.ts — register a single GET /api/health route returning HealthDTO from @fartol/shared-types. node_id sourced from env FARTOL_NODE_ID (default 'local-dev'), uptime_ms = process.uptime() * 1000.

    apps/edge/src/bin/fartol.ts — binary entrypoint mirroring packages/sportident/src/bin/fartol-readout.ts structure:
    - #!/usr/bin/env node shebang
    - PATTERNS S-1 header (provenance: "Authored for fartol. Not ported from upstream.")
    - Hand-rolled argv parser (no commander/yargs) accepting:
      - --port=<int> (default 3000)
      - --bind-host=<string> (default '127.0.0.1' — REJECT '0.0.0.0' unless `--allow-lan` flag is also present; T-WS-FAN-OUT mitigation)
      - --db-path=<string> (default './fartol.db' — consumed later by plan 02)
      - --help
    - Async main() that calls buildServer({ logger: true }) and await app.listen({ port, host: bindHost })
    - SIGINT handler: try { await app.close(); } catch {} ; process.exit(0)
    - process.on('uncaughtException') + process.on('unhandledRejection') logging via stderr + process.exit(1) (RESEARCH Pitfall 9)
    - isEntrypoint guard at bottom via realpathSync(fileURLToPath(import.meta.url)) === realpathSync(process.argv[1]) (PATTERNS S-7) — only calls main() if true

    apps/edge/src/server.test.ts — node:test integration:
    - test 1: `buildServer({ logger: false })` returns an instance; `await app.inject({ method: 'GET', url: '/api/health' })` returns statusCode 200 with body { status: 'ok' }
    - test 2: GET /api/unknown returns 404 with { error: 'Not found' }
    - test 3 (anti-T-WS-FAN-OUT): construct via the bin's argv parser — `parseArgs(['--bind-host', '0.0.0.0'])` returns an Error/throw; `parseArgs(['--bind-host', '0.0.0.0', '--allow-lan'])` succeeds. Extract argv parsing to an exported `parseArgs(argv: string[]): Options` function on the bin module so the test can hit it directly without booting the server. Lifecycle (main) stays guarded by isEntrypoint.

    Apply PATTERNS S-1 header to every new .ts file.
  </action>
  <verify>
    <automated>cd /home/jonas/src/FartOL-phase-1 &amp;&amp; pnpm install --frozen-lockfile=false &amp;&amp; pnpm --filter @fartol/edge typecheck &amp;&amp; pnpm --filter @fartol/edge test 2>&amp;1 | grep -v '^#' | grep -E '(pass|fail) [0-9]+'</automated>
  </verify>
  <done>
    `pnpm --filter @fartol/edge typecheck` exits 0. Three node:test cases pass: GET /api/health → 200, unknown route → 404, bind-host=0.0.0.0 rejected unless --allow-lan. `node --import tsx apps/edge/src/bin/fartol.ts --help` prints usage and exits 0 (test 4 — manual smoke after task completes; not asserted automatically).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create apps/web/ SvelteKit SPA skeleton + Playwright config + root scripts</name>
  <files>
    apps/web/package.json,
    apps/web/svelte.config.js,
    apps/web/vite.config.ts,
    apps/web/vitest.config.ts,
    apps/web/tsconfig.json,
    apps/web/src/app.html,
    apps/web/src/routes/+layout.ts,
    apps/web/src/routes/+layout.svelte,
    apps/web/src/routes/+page.svelte,
    apps/web/static/manifest.webmanifest,
    playwright.config.ts,
    package.json
  </files>
  <read_first>
    package.json (root — preserve existing scripts; only add test:quick + e2e),
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"svelte.config.js — adapter-static SPA mode" (verbatim usable),
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Pitfall 2: Dev proxy missing for /ws and /api/*" (vite.config.ts proxy block — verbatim),
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Pitfall 1: adapter-static ssr/prerender misconfiguration" (ssr=false + prerender=false in +layout.ts),
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Pitfall 10: i18next locale loading timing" (sync init — only stub locale handling here, full i18n in plan 11),
    .planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md §"Library policy" (NO Skeleton/shadcn-svelte; hand-rolled),
    .planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md §"Visual Anchors" (lang="sv" on HTML root),
    .planning/phases/01-single-laptop-training-mvp/01-CONTEXT.md D-14 (PWA manifest + icons, no SW),
    .planning/phases/01-single-laptop-training-mvp/01-PATTERNS.md §"Web tier" table + §"No Analog Found" rows for svelte.config.js / vite.config.ts
  </read_first>
  <action>
    Create apps/web/ as a SvelteKit SPA. License: AGPL-3.0-or-later (PATTERNS S-8).

    apps/web/package.json:
    - name: "@fartol/web"
    - version: "0.0.0"
    - private: true
    - type: "module"
    - license: "AGPL-3.0-or-later"
    - engines.node: ">=22.18.0"
    - scripts:
      - dev: "vite dev --port 5173"
      - build: "vite build"
      - preview: "vite preview"
      - test: "vitest run"
      - test:watch: "vitest"
      - typecheck: "svelte-kit sync && tsc --noEmit"
      - lint: "eslint src"
    - dependencies: { "@fartol/shared-types": "workspace:*" }
    - devDependencies:
      - svelte@^5.55.5
      - @sveltejs/kit@^2.59.1
      - @sveltejs/adapter-static@^3.0.10
      - @sveltejs/vite-plugin-svelte@^4.0.0
      - vite@^8.0.12
      - vitest@^4.1.6
      - typescript@^5.6.3

    Note: i18next, fonts, design tokens land in plan 11. Plan 01 ships the bare scaffold only.

    apps/web/svelte.config.js — RESEARCH §"svelte.config.js" verbatim:
    ```
    import adapter from '@sveltejs/adapter-static';
    import { vitePreprocess } from '@sveltejs/vite-plugin-svelte';
    export default {
      preprocess: vitePreprocess(),
      kit: {
        adapter: adapter({ fallback: '200.html', strict: false }),
        prerender: { entries: [] },
      },
    };
    ```

    apps/web/vite.config.ts — RESEARCH §Pitfall 2 proxy block + vitest config (Vitest reads vite.config.ts by default; add `test:` block with environment 'jsdom' and globals false):
    ```
    server: { proxy: { '/api': { target: 'http://localhost:3000', changeOrigin: true }, '/ws': { target: 'ws://localhost:3000', ws: true } } },
    test: { environment: 'jsdom', include: ['src/**/*.test.ts'] }
    ```
    Include sveltekit() plugin.

    apps/web/vitest.config.ts — Either omit (use vite.config.ts) or add a thin re-export. Choice: omit; add `test` block inside vite.config.ts (RESEARCH validation architecture says vitest config can live inside vite.config.ts).

    Decision: keep vitest.config.ts as a single-line re-export of vite.config.ts so the planner's intent (separate file existed) shows in the file list, but the actual config lives inside vite.config.ts. Content: `export { default } from './vite.config.ts';`.

    apps/web/tsconfig.json — extends "../../tsconfig.json", adds SvelteKit-generated `.svelte-kit/tsconfig.json` extension chain via `"extends": ["./.svelte-kit/tsconfig.json", "../../tsconfig.json"]` (root tsconfig provides strict + allowImportingTsExtensions; SvelteKit's generated tsconfig provides path aliases like $lib).

    apps/web/src/app.html — minimal SvelteKit template. `<html lang="sv">` (UI-SPEC Visual Anchors). Link `<link rel="manifest" href="%sveltekit.assets%/manifest.webmanifest">` (D-14).

    apps/web/src/routes/+layout.ts — `export const ssr = false; export const prerender = false;` (RESEARCH Pitfall 1).

    apps/web/src/routes/+layout.svelte — minimal `<slot />` wrapper. PATTERNS S-1 header noting full AppShell + Sidebar + TopBar land in plan 11.

    apps/web/src/routes/+page.svelte — placeholder home page rendering literal "FartOL" + a paragraph "Phase 1 walking skeleton — full UI lands in plan 11." PATTERNS S-1 header.

    apps/web/static/manifest.webmanifest — D-14 PWA manifest:
    ```
    { "name": "FartOL", "short_name": "FartOL", "start_url": "/", "display": "standalone", "background_color": "#fafaf7", "theme_color": "#2f7c4f", "icons": [{ "src": "/icon-192.png", "sizes": "192x192", "type": "image/png" }, { "src": "/icon-512.png", "sizes": "512x512", "type": "image/png" }] }
    ```
    Icons themselves are not generated in plan 01 — they're a plan 11 deliverable. The manifest references them so the file is structurally complete; missing icons just yield 404 on those paths until plan 11.

    playwright.config.ts (repo root) — Playwright config pointing testDir at `./tests/e2e`, baseURL at `http://localhost:5173`, webServer entry that runs `pnpm --filter @fartol/edge dev` AND `pnpm --filter @fartol/web dev` in parallel (use webServer array with reuseExistingServer:true). The actual walking-skeleton e2e spec lands in plan 03; this plan just creates the config so Playwright `npx playwright test --list` works.

    package.json (root) — add two scripts only. PRESERVE all existing scripts verbatim:
    - "test:quick": "pnpm --filter @fartol/edge test &amp;&amp; pnpm --filter @fartol/shared-types test &amp;&amp; pnpm --filter @fartol/web test"
    - "e2e": "playwright test"
    - "dev": "pnpm --parallel --filter ./apps/* dev"
    Also add devDependency: @playwright/test@^1.60.0

    Apply PATTERNS S-1 header to every new .ts/.svelte/.js file. The .svelte files use HTML comment headers `<!-- ... -->` rather than `//`.
  </action>
  <verify>
    <automated>cd /home/jonas/src/FartOL-phase-1 &amp;&amp; pnpm install --frozen-lockfile=false &amp;&amp; pnpm --filter @fartol/web exec svelte-kit sync &amp;&amp; pnpm --filter @fartol/web typecheck &amp;&amp; pnpm --filter @fartol/web build &amp;&amp; test -f apps/web/build/200.html &amp;&amp; npx playwright test --list 2>&amp;1 | grep -v '^#' | head -5</automated>
  </verify>
  <done>
    `pnpm --filter @fartol/web build` produces `apps/web/build/200.html` (adapter-static SPA fallback). `pnpm --filter @fartol/web typecheck` exits 0. `npx playwright test --list` lists zero specs but does not error (config is structurally valid). Root `pnpm test:quick` runs and exits 0 across all three packages.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localhost loopback only | Phase 1 bridge binds 127.0.0.1 only by default; no LAN exposure |
| browser→Fastify (loopback) | UI client sends REST + WS via vite dev proxy; same origin in prod |
| Drizzle / SQLite | not yet present — lands in plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-WS-FAN-OUT | Spoofing / Info Disclosure | apps/edge bin --bind-host | mitigate | --bind-host defaults to 127.0.0.1; --bind-host 0.0.0.0 only allowed when --allow-lan is ALSO present. Test 3 in server.test.ts asserts parseArgs rejects bare 0.0.0.0. |
| T-CORS | Tampering | apps/edge server.ts @fastify/cors | mitigate | origin: 127.0.0.1 only in plan 01 (no LAN clients yet). Expanded in plan 03 when --allow-lan is wired. |
| T-DOS-WS | DoS | future @fastify/websocket | accept (deferred) | WS plugin lands in plan 03; maxPayload + per-connection rate limit added there. Low severity in P1 (single-laptop) per RESEARCH §Security Domain. |
</threat_model>

<verification>
1. `pnpm install --frozen-lockfile=false` from clean state succeeds (lockfile regenerates with new workspace packages).
2. `pnpm -r typecheck` exits 0 across @fartol/edge, @fartol/web, @fartol/shared-types, @fartol/sportident.
3. `pnpm -r test` exits 0 — all three new packages have at least one passing placeholder test; @fartol/sportident's Phase 0 suite (76+ tests) still passes.
4. `pnpm --filter @fartol/web build` produces `apps/web/build/200.html`.
5. `pnpm --filter @fartol/edge build` produces `apps/edge/dist/server.mjs`, `apps/edge/dist/server.cjs`, `apps/edge/dist/bin/fartol.mjs`, `apps/edge/dist/bin/fartol.cjs`, `apps/edge/dist/server.d.ts`.
6. `npx playwright test --list` exits 0 (zero specs but config valid).
7. Manual smoke (developer only — not automated): `pnpm dev` opens Vite at :5173 and Fastify at :3000; visiting http://localhost:5173/ shows the placeholder home page; visiting http://localhost:5173/api/health proxies to Fastify and returns `{ status: 'ok', node_id: 'local-dev', uptime_ms: <number> }`.
</verification>

<success_criteria>
- Workspace installs from clean (`rm -rf node_modules **/node_modules pnpm-lock.yaml && pnpm install` exits 0).
- Three new packages exist with manifest, tsconfig, README-less but PATTERNS S-1 headered source.
- `pnpm test:quick` exits 0.
- `pnpm --filter @fartol/web build` produces an adapter-static SPA output.
- `pnpm --filter @fartol/edge build` produces dual ESM + CJS dist with bin/fartol.cjs.
- Phase 0 @fartol/sportident test suite remains green.
- No new tooling files at repo root beyond `playwright.config.ts` and 2 lines of `package.json` script additions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-single-laptop-training-mvp/01-01-SUMMARY.md` using $HOME/.claude/get-shit-done/templates/summary.md. Record:
- Exact package versions installed (pnpm-lock.yaml diff summary).
- Decision: did `tsx` get added as @fartol/edge devDep, or did the bin run under `node --import tsx --experimental-strip-types`? Document the actual choice.
- Any auto-fixes during execution (per Rule 1/2/3).
- Confirm pnpm-lock.yaml was committed.
</output>
