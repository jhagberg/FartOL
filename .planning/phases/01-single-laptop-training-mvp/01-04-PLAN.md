---
phase: 01-single-laptop-training-mvp
plan: 04
type: execute
wave: 1
depends_on: [02, 03]
files_modified:
  - apps/edge/package.json
  - apps/edge/src/routes/competitions.ts
  - apps/edge/src/routes/classes.ts
  - apps/edge/src/routes/courses.ts
  - apps/edge/src/routes/competitors.ts
  - apps/edge/src/routes/clubs.ts
  - apps/edge/src/server.ts
  - apps/edge/src/routes/competitions.test.ts
  - apps/edge/src/routes/competitors.test.ts
  - apps/edge/src/routes/courses.test.ts
  - packages/shared-types/src/dtos.ts
  - packages/shared-types/src/index.ts
autonomous: true
tags: [crud, rest, zod, validation, competition]
requirements:
  - REQ-EVT-CMP-001
  - REQ-EVT-CMP-004
  - REQ-PRIV-001
must_haves:
  truths:
    - "POST /api/competitions creates a competition row and returns 201 with the created CompetitionDTO"
    - "GET /api/competitions lists all competitions, ordered by created_at_ms DESC"
    - "GET /api/competitions/:id returns 200 with the competition + its classes + its courses; 404 if not found"
    - "PATCH /api/competitions/:id can update receipt_template and auto_print (UI-SPEC §Auto-print toggle is event-level)"
    - "POST /api/competitors creates a competitor row + emits a card_bound event in a single transaction (REQ-PRIV-001 consent timestamp recorded)"
    - "GET /api/clubs returns top-50 club names ordered by last_seen_at_ms DESC (walk-up autocomplete source)"
    - "Every REST route validates body+params via Zod schemas living in packages/shared-types/"
    - "All requests rejected by Zod return 400 with structured { errors: [...] } body"
  artifacts:
    - path: "apps/edge/src/routes/competitions.ts"
      provides: "REST handlers: list/get/create/patch competitions; nested classes + courses include"
      min_lines: 100
    - path: "apps/edge/src/routes/competitors.ts"
      provides: "REST handlers: walk-up create competitor + card_bound event in single transaction"
      min_lines: 80
    - path: "packages/shared-types/src/dtos.ts"
      provides: "Zod schemas + inferred TS types for every REST DTO in Phase 1 CRUD"
      contains: "z.object"
  key_links:
    - from: "apps/edge/src/routes/competitions.ts"
      to: "apps/edge/src/db/schema.ts"
      via: "drizzle insert + select against competitions/classes/courses tables"
      pattern: "competitions"
    - from: "apps/edge/src/routes/competitors.ts"
      to: "apps/edge/src/ws/index.ts"
      via: "app.wsBroadcast(readoutChannel(competitionId), { type: 'card_bound', ... })"
      pattern: "wsBroadcast"
    - from: "apps/edge/src/routes/competitors.ts"
      to: "apps/edge/src/db/schema.ts events table"
      via: "single sqlite.transaction inserts competitor row + card_bound event"
      pattern: "transaction"
---

<objective>
Competition CRUD + competitor walk-up + clubs autocomplete. These are the mutable-config-tables (CONTEXT D-09) REST surface that the SvelteKit wizard (plan 12) and walk-up modal (plan 14) consume.

Honors:
- D-15 three-click wizard: POST /api/competitions returns the row used by the wizard to route to /competition/[id]/readout.
- D-11 hybrid card-to-competitor matching: POST /api/competitors with cardNumber sets up the auto-attach for next read.
- D-04 walk-up registration first-class: dedicated walk-up code path on POST /api/competitors.
- REQ-PRIV-001: consent timestamp captured at competitor creation (NOT NULL on competitors.consent_at_ms).
- UI-SPEC §Wizard: classes can be created later (from XML import, plan 05) — POST /api/competitions does NOT require classes in the body.
- UI-SPEC §"Auto-print toggle": event-level, persisted on competitions row.

Out of scope for plan 04:
- XML import (plan 05).
- Card → competitor auto-attach reducer (plan 07) — plan 04 stores the binding row; the reducer reads it.
- DELETE competition (UI-SPEC §"Destructive actions" — NOT in Phase 1).

Output: 5 route modules, 1 Zod schema bundle, 3 test files. After this plan, an operator can POST a competition, PATCH its auto_print toggle, and POST walk-up competitors. The XML importer (plan 05) reuses the competitions route's auto-create class semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-single-laptop-training-mvp/01-CONTEXT.md
@.planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md
@.planning/phases/01-single-laptop-training-mvp/01-PATTERNS.md
@.planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md
@.planning/phases/01-single-laptop-training-mvp/01-01-PLAN.md
@.planning/phases/01-single-laptop-training-mvp/01-02-PLAN.md
@.planning/phases/01-single-laptop-training-mvp/01-03-PLAN.md

<interfaces>
From @fartola/shared-types (plans 01 + 02 + 04):
- Zod schemas + inferred types: CompetitionDTO, CompetitionCreateInput, CompetitionPatchInput, ClassDTO, CourseDTO, CompetitorDTO, CompetitorCreateInput, ClubDTO.
- Row types (Event, Competition, Class, Course, Competitor, Club).
- WsEnvelope, readoutChannel.

From plan 02:
- Drizzle schema imports (competitions, classes, courses, course_controls, controls, competitors, clubs, events).
- openDatabase / DbHandle / ensureNodeId / nextLocalSeq.

From plan 03:
- app.fartolaDb (DbHandle), app.fartolaNodeId, app.wsBroadcast.

Wire envelope shapes (locked here):
- card_bound: `{ type: 'card_bound'; payload: { competitor_id: string; card_number: number; competition_id: string; class_id: string; name: string; club: string | null }; channel: 'readout:<id>'; seq: number }`.

Zod boundary contract (REQ-PRIV-001):
- CompetitorCreateInput requires `consent: true` literal. The route uses Date.now() as consent_at_ms (server-attested timestamp). If consent is missing/false, return 400 with errors: [{ path: 'consent', message: 'Consent required' }].

D-15 dates: 'YYYY-MM-DD' string. Zod regex `/^\d{4}-\d{2}-\d{2}$/`. UI-SPEC Visual Anchors: NO locale-dependent date input.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zod schemas in shared-types + competitions/classes/courses GET+POST+PATCH routes</name>
  <files>
    apps/edge/package.json,
    packages/shared-types/package.json,
    packages/shared-types/src/dtos.ts,
    packages/shared-types/src/index.ts,
    apps/edge/src/routes/competitions.ts,
    apps/edge/src/routes/classes.ts,
    apps/edge/src/routes/courses.ts,
    apps/edge/src/server.ts,
    apps/edge/src/routes/competitions.test.ts,
    apps/edge/src/routes/courses.test.ts
  </files>
  <read_first>
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Standard Stack" zod entry + §"Don't Hand-Roll" zod row,
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Security Domain V5 Input Validation",
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Pitfall 5: IOF XSD passing locally" (informs the export-validation contract, not plan 04 directly — but the patterns of strict input validation apply),
    .planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md §Wizard,
    .planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md §"Auto-print toggle",
    .planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md §"Receipt template DEFAULT",
    .planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md §Copywriting (error states for import + reader handshake — informs DTO error.code values),
    .planning/phases/01-single-laptop-training-mvp/01-CONTEXT.md D-09 D-15,
    apps/edge/src/db/schema.ts (plan 02)
  </read_first>
  <action>
    Add `zod@^4.4.3` to packages/shared-types/package.json dependencies AND apps/edge/package.json dependencies (shared-types re-exports the Zod object; @fartola/edge uses it directly for body validation).

    Update packages/shared-types/src/dtos.ts to define real Zod schemas + inferred types. PATTERNS S-6 snake_case at the wire boundary. PATTERNS S-1 file header.

    Required schemas:

    ```
    import { z } from 'zod';

    const ISO_DATE = z.string().regex(/^\d{4}-\d{2}-\d{2}$/, 'expected YYYY-MM-DD');
    const RECEIPT_TEMPLATE = z.enum(['classic', 'standing', 'detailed', 'top4', 'minimal', 'kids']);
    const UUID = z.string().uuid();
    const POSITIVE_INT = z.number().int().positive();

    // --- Competition ---
    export const CompetitionDTO = z.object({
      id: UUID,
      name: z.string().min(1),
      date: ISO_DATE,
      receipt_template: RECEIPT_TEMPLATE,
      auto_print: z.boolean(),
      created_at_ms: z.number().int().nonnegative(),
    });
    export type CompetitionDTO = z.infer<typeof CompetitionDTO>;

    export const CompetitionCreateInput = z.object({
      name: z.string().min(1).max(200),
      date: ISO_DATE,
      receipt_template: RECEIPT_TEMPLATE.optional(),  // server default 'classic'
      auto_print: z.boolean().optional(),              // server default false
    });
    export type CompetitionCreateInput = z.infer<typeof CompetitionCreateInput>;

    export const CompetitionPatchInput = z.object({
      name: z.string().min(1).max(200).optional(),
      date: ISO_DATE.optional(),
      receipt_template: RECEIPT_TEMPLATE.optional(),
      auto_print: z.boolean().optional(),
    });
    export type CompetitionPatchInput = z.infer<typeof CompetitionPatchInput>;

    // --- Class ---
    export const ClassDTO = z.object({
      id: UUID,
      competition_id: UUID,
      name: z.string().min(1),
      short_name: z.string().nullable(),
    });
    export type ClassDTO = z.infer<typeof ClassDTO>;
    export const ClassCreateInput = ClassDTO.omit({ id: true });

    // --- Course ---
    export const CourseDTO = z.object({
      id: UUID,
      competition_id: UUID,
      name: z.string().min(1),
      class_id: UUID.nullable(),
      length_m: z.number().int().nullable(),
      climb_m: z.number().int().nullable(),
      controls: z.array(z.object({ control_code: POSITIVE_INT, order_idx: z.number().int().nonnegative() })),
    });
    export type CourseDTO = z.infer<typeof CourseDTO>;

    // --- Competitor ---
    export const CompetitorDTO = z.object({
      id: UUID,
      competition_id: UUID,
      name: z.string(),
      club: z.string().nullable(),
      class_id: UUID,
      card_number: POSITIVE_INT.nullable(),
      consent_at_ms: z.number().int().nonnegative().nullable(),
      consent_status: z.enum(['explicit', 'pending_first_read', 'confirmed_on_read']),
      scrubbed_at_ms: z.number().int().nullable(),
    });
    export type CompetitorDTO = z.infer<typeof CompetitorDTO>;

    export const CompetitorCreateInput = z.object({
      competition_id: UUID,
      name: z.string().trim().min(2).max(200),     // UI-SPEC §Walk-up modal: minLength 2
      club: z.string().trim().max(120).nullable().optional().transform(v => v ?? null),
      class_id: UUID,
      card_number: POSITIVE_INT.nullable().optional().transform(v => v ?? null),
      consent: z.literal(true),                     // REQ-PRIV-001 explicit consent
      consent_status: z.literal('explicit').optional(),  // C-M4: walk-up POST is always 'explicit'; omitted = server default 'explicit' (mirrors DB column DEFAULT from plan 02)
    });
    export type CompetitorCreateInput = z.infer<typeof CompetitorCreateInput>;

    // --- Club ---
    export const ClubDTO = z.object({ name: z.string(), last_seen_at_ms: z.number().int().nonnegative() });
    export type ClubDTO = z.infer<typeof ClubDTO>;

    // --- Health (from plan 01 — keep) ---
    export const HealthDTO = z.object({ status: z.literal('ok'), node_id: z.string(), uptime_ms: z.number() });
    export type HealthDTO = z.infer<typeof HealthDTO>;
    ```

    Update packages/shared-types/src/index.ts to barrel-export the new schemas (both schema objects AND inferred types).

    apps/edge/src/routes/competitions.ts — Fastify plugin registering:
    - GET /api/competitions → list, ordered by created_at_ms DESC. Returns `{ competitions: CompetitionDTO[] }`.
    - POST /api/competitions → body validated against CompetitionCreateInput. Server generates UUID id + created_at_ms = Date.now(). Defaults: receipt_template='classic', auto_print=false. Returns 201 + the created CompetitionDTO.
    - GET /api/competitions/:id → returns `{ competition: CompetitionDTO, classes: ClassDTO[], courses: CourseDTO[] }`. 404 if not found.
    - PATCH /api/competitions/:id → body validated against CompetitionPatchInput. Updates only the provided fields. Returns 200 + updated CompetitionDTO. 404 if not found.

    Body validation pattern (used everywhere in plan 04):
    ```
    const parsed = CompetitionCreateInput.safeParse(req.body);
    if (!parsed.success) return reply.code(400).send({ errors: parsed.error.issues.map(i => ({ path: i.path.join('.'), code: i.code, message: i.message })) });
    ```

    apps/edge/src/routes/classes.ts:
    - GET /api/competitions/:id/classes → list classes for the competition.
    - POST /api/competitions/:id/classes → create class (used by plan 05 XML auto-create class flow).
    - (Decision: NO standalone /api/classes route; classes are always nested under competition — RESTful and matches UI-SPEC §Wizard.)

    apps/edge/src/routes/courses.ts:
    - GET /api/competitions/:id/courses → list courses with embedded controls (joined from course_controls + controls). Order controls by order_idx ASC. Returns `{ courses: CourseDTO[] }`.
    - POST /api/competitions/:id/courses → create a course (used by plan 05 XML import). Body validated against `CourseDTO.omit({ id: true })`. Inserts course row + control rows + course_control rows in a single transaction.

    Update apps/edge/src/server.ts to register the new route modules (after the existing health route, before notFoundHandler):
    - registerCompetitions, registerClasses, registerCourses.

    apps/edge/src/routes/competitions.test.ts — node:test:
    - test 1: POST /api/competitions with valid body → 201; row exists in DB.
    - test 2: POST without required fields → 400 with errors array.
    - test 3: GET /api/competitions/:id → 200 with nested classes + courses arrays (both empty initially).
    - test 4: GET /api/competitions/:id with unknown id → 404.
    - test 5: PATCH /api/competitions/:id setting auto_print: true → row updated; subsequent GET reflects it.
    - test 6 (D-15 date format): POST with date='2026-13-99' → 400 (Zod regex rejects invalid month/day, but specifically the regex `^\d{4}-\d{2}-\d{2}$` only catches structural failures — UI-SPEC explicitly accepts the regex as the wire contract; database doesn't validate semantic date validity. Document this as expected behavior).
    - test 7: PATCH with empty body → 200 (no fields changed; idempotent).

    apps/edge/src/routes/courses.test.ts — node:test:
    - test 1: POST /api/competitions/:id/courses with controls=[{control_code:31, order_idx:0},{control_code:32, order_idx:1}] → 201; GET returns the course with both controls in order.
    - test 2: POST course with non-existent competition_id → 404 (FK ON DELETE CASCADE means inserting against missing FK fails — translate to 404 explicitly).
    - test 3: GET /api/competitions/:id/courses returns courses sorted by name ASC, controls per-course sorted by order_idx.

    Apply PATTERNS S-1 file-header to every new .ts file.
  </action>
  <verify>
    <automated>cd /home/jonas/src/fartOLa-phase-1 &amp;&amp; pnpm install --frozen-lockfile=false &amp;&amp; pnpm --filter @fartola/edge typecheck &amp;&amp; pnpm --filter @fartola/shared-types typecheck &amp;&amp; pnpm --filter @fartola/edge test 2>&amp;1 | grep -E '(pass|fail) [0-9]+'</automated>
  </verify>
  <done>
    All competition + class + course CRUD routes registered. Zod validation produces structured 400 errors. All Task-1 node:tests pass (target: ~10 new tests). Receipt template default 'classic' and auto_print default false are enforced server-side.
  </done>
</task>

<task type="auto">
  <name>Task 2: POST /api/competitors (walk-up + card_bound event) + GET /api/clubs autocomplete</name>
  <files>
    apps/edge/src/routes/competitors.ts,
    apps/edge/src/routes/clubs.ts,
    apps/edge/src/server.ts,
    apps/edge/src/routes/competitors.test.ts
  </files>
  <read_first>
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Walk-up registration: bind unknown card" code excerpt (lines 766-792 — usable verbatim),
    .planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md §"Walk-up modal" (LOCKED form contract: name min 2, klubb optional autocomplete, klass required select, bricka editable),
    .planning/phases/01-single-laptop-training-mvp/01-CONTEXT.md D-04 D-11,
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Security Domain V8 + V11" (REQ-PRIV-001 + business logic — consent flag),
    apps/edge/src/db/schema.ts competitors + clubs + events tables,
    apps/edge/src/routes/dev.ts (plan 03 transaction pattern — single sqlite.transaction for both inserts)
  </read_first>
  <action>
    apps/edge/src/routes/competitors.ts — single Fastify plugin registering:

    - POST /api/competitors → walk-up registration (D-04 first-class). Body validated against CompetitorCreateInput. Workflow:
      1. Verify competition_id exists; 404 if not.
      2. Verify class_id exists AND belongs to competition_id; 422 if not (semantic vs syntactic failure).
      3. If card_number provided, check the partial-unique-index — if another competitor in this competition already has this card, return 409 with `{ error: 'card_taken', existing_competitor_id: '...' }`. Operator-visible UI in plan 14 surfaces this.
      4. In a single sqlite.transaction:
         - Insert competitor row with consent_at_ms = Date.now(), scrubbed_at_ms = null.
         - If club non-null AND non-empty, upsert clubs row (INSERT ... ON CONFLICT(name) DO UPDATE SET last_seen_at_ms = excluded.last_seen_at_ms).
         - If card_number provided, INSERT into events table with eventType='card_bound', payload={event_type:'card_bound', competitor_id, card_number, walkup:true, consent_at_ms}. local_seq via nextLocalSeq.
      5. After commit, if card_number provided, call app.wsBroadcast(readoutChannel(competition_id), { type: 'card_bound', payload: {...} }).
      6. Return 201 + CompetitorDTO.

    - GET /api/competitions/:id/competitors → list competitors for a competition (used by results page + plan 11 UI).
    - GET /api/competitions/:id/competitors/:competitorId → single competitor (used for walk-up modal pre-fill).

    apps/edge/src/routes/clubs.ts — single Fastify plugin:

    - GET /api/clubs?prefix=<string>&amp;limit=<int> → list clubs, ordered by last_seen_at_ms DESC, optionally filtered by name LIKE prefix%. Default limit 50. Returns `{ clubs: ClubDTO[] }`. Used by walk-up modal autocomplete (UI-SPEC §"Walk-up modal" — Klubb field autocomplete from `competitor_clubs` aggregate in production).

    Both routes use the body-validation pattern from Task 1 (Zod safeParse + structured 400 response).

    Update apps/edge/src/server.ts to register both new plugins.

    apps/edge/src/routes/competitors.test.ts — node:test:
    - test 1: POST /api/competitors with valid body including card_number + club → 201; competitor row + clubs upsert + events row (eventType='card_bound') all present in DB.
    - test 2: POST with same card_number for same competition (twice) → second request 409 'card_taken'.
    - test 3 (REQ-PRIV-001): POST without `consent: true` → 400 with errors: [{ path: 'consent', ... }].
    - test 4: POST with consent: false → 400.
    - test 5: POST with class_id from a DIFFERENT competition → 422 (semantic mismatch).
    - test 6: POST without card_number (pre-entry, walk-up scenario A) → 201; no card_bound event emitted (card binding happens later); competitor row has card_number IS NULL.
    - test 7: After successful POST with card_number, app.wsBroadcast was invoked with channel='readout:<comp_id>' and type='card_bound' (use a wsBroadcast spy via PATTERNS S-2 — buildServer accepts an optional opts.broadcastSink for tests, default falls through to the real wsBroadcast).
    - test 8 (GET /api/clubs): three clubs inserted with different last_seen_at_ms; GET returns them sorted DESC.
    - test 9: Transaction atomicity — force the events insert to fail (e.g., by mocking nextLocalSeq to throw); assert competitor row is NOT created (rollback).

    Apply PATTERNS S-1 header to every new .ts file.
  </action>
  <verify>
    <automated>cd /home/jonas/src/fartOLa-phase-1 &amp;&amp; pnpm --filter @fartola/edge typecheck &amp;&amp; pnpm --filter @fartola/edge test 2>&amp;1 | grep -E '(pass|fail) [0-9]+|competitor|consent'</automated>
  </verify>
  <done>
    POST /api/competitors creates a competitor + card_bound event atomically (REQ-EVT-CMP-004 + REQ-PRIV-001). Consent flag is enforced. Card_taken collisions return 409. GET /api/clubs returns ordered autocomplete data. All Task-2 node:tests pass (~9 new tests).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→/api/* | All inputs untrusted; Zod validates every body and params |
| /api/competitors→competitors + events + clubs tables | Single sqlite.transaction guards atomicity |
| consent flag→storage | Server-attested timestamp; client cannot backdate consent_at_ms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SQL-INJECT | Tampering | All routes | mitigate | Drizzle query builder parameterizes; never compose SQL via template strings. Code review enforces. |
| T-PII-AT-REST | Information Disclosure | competitors.name + .club | accept (P1 baseline) | Localhost-only; full-disk crypto is operator responsibility. REQ-PRIV-002 scrub lands in plan 17. |
| T-CONSENT-BYPASS | Privacy | POST /api/competitors | mitigate | Zod requires consent: literal true; server attests consent_at_ms via Date.now(). Tests 3 + 4 in competitors.test.ts. |
| T-DUPLICATE-CARD-BINDING | Tampering | competitors partial unique index | mitigate | DB partial unique index (plan 02) + 409 response. Test 2 in competitors.test.ts. |
| T-CLASS-COMP-MISMATCH | Tampering | POST /api/competitors | mitigate | Verify class_id belongs to body.competition_id; 422 otherwise. Test 5. |
| T-INPUT-SIZE-DOS | DoS | All POST routes | mitigate | Fastify bodyLimit default (1MB) + Zod max() limits on string fields (name max 200, club max 120). Sufficient for P1. |
</threat_model>

<verification>
1. `pnpm --filter @fartola/shared-types typecheck` exits 0 (Zod schemas + inferred types compile).
2. `pnpm --filter @fartola/edge typecheck` exits 0 across all new routes.
3. `pnpm --filter @fartola/edge test` exits 0 with ~19 new tests passing (10 + 9).
4. Manual smoke: `FARTOLA_DEV=1 pnpm dev`, then `curl -X POST http://localhost:3000/api/competitions -H 'content-type:application/json' -d '{"name":"StorTuna Tuesday","date":"2026-05-19"}'` returns 201 + CompetitionDTO.
5. The wave-1 plan 05 (XML import) can rely on POST /api/competitions/:id/courses to create courses from parsed XML.
</verification>

<success_criteria>
- All Phase 1 mutable-config CRUD routes registered: competitions, classes, courses, competitors, clubs.
- Zod schemas centralized in @fartola/shared-types so SvelteKit forms (plan 12 + 14) consume the same shapes.
- REQ-PRIV-001 consent literal enforced server-side.
- D-04 walk-up: competitor + card_bound event committed atomically; WS broadcast happens after commit.
- D-11 hybrid matching: cardNumber persisted on competitor; the plan-07 reducer reads competitors.card_number to auto-attach.
- Wave 1 plan 05 unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/01-single-laptop-training-mvp/01-04-SUMMARY.md` using the GSD summary template.
Record: exact zod version installed, decision on how empty-body PATCH was handled (200 with unchanged row vs 304), any Drizzle ON CONFLICT pattern quirks for the clubs upsert.
</output>
