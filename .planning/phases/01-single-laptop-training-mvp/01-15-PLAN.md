---
phase: 01-single-laptop-training-mvp
plan: 15
type: execute
wave: 4
depends_on: [04, 08, 11, 13]
files_modified:
  - apps/edge/package.json
  - apps/edge/src/print/escposDriver.ts
  - apps/edge/src/print/escposDriver.test.ts
  - apps/edge/src/print/templates.ts
  - apps/edge/src/print/templates/classic.ts
  - apps/edge/src/print/templates/standing.ts
  - apps/edge/src/print/templates/detailed.ts
  - apps/edge/src/print/templates/top4.ts
  - apps/edge/src/print/templates/minimal.ts
  - apps/edge/src/print/templates/kids.ts
  - apps/edge/src/print/kids-svg-to-bitmap.ts
  - apps/edge/src/print/kids-svg-to-bitmap.test.ts
  - apps/edge/src/routes/print.ts
  - apps/edge/src/routes/print.test.ts
  - apps/edge/src/server.ts
  - apps/edge/src/bin/fartol.ts
  - apps/web/src/lib/screens/ReadoutView.svelte
autonomous: true
tags: [escpos, thermal-print, receipt, hardware, sharp]
requirements:
  - REQ-UI-004
must_haves:
  truths:
    - "POST /api/competitions/:id/print-receipt { competitor_id, template } renders the chosen template to ESC/POS bytes and sends to /dev/usb/lp* (or stdout-sink in dev)"
    - "All 6 receipt templates supported server-side; output matches the UI ReceiptPaper mirror visually within the limits of an 80mm thermal column"
    - "node-thermal-printer is the default driver; printer-type chosen from competition.receipt_template? — NO, printer-type chosen from new optional column competitions.printer_type (default 'star'); operator can switch per competition"
    - "Kids template renders Skogis as raster bitmap: SVG -> PNG (via sharp) -> printer.printImage()"
    - "Auto-print toggle (plan 13 UI + competition.auto_print column) wires: bridge card_read event handler enqueues a print request when auto_print=true; the print queue is single-flight (no concurrent print to /dev/usb/lp*)"
    - "Single-flight print queue: in-process FIFO ensures only one print at a time; queued items wait their turn"
    - "On print fail (printer offline, paper out, EBUSY): single-flight returns 503 + UI-SPEC error toast 'Utskrift misslyckades'; reads continue uninterrupted"
  artifacts:
    - path: "apps/edge/src/print/escposDriver.ts"
      provides: "createNodeThermalPrinterSink(opts) -> PrinterSink (the plan 03 PrinterSink interface) wrapping node-thermal-printer; PATTERNS S-3 lazy native require"
      min_lines: 60
    - path: "apps/edge/src/print/templates.ts"
      provides: "renderTemplate(template, data) -> ESC/POS command sequence (newline, justify, bold, image) using node-thermal-printer's builder API"
      min_lines: 30
    - path: "apps/edge/src/routes/print.ts"
      provides: "POST /api/competitions/:id/print-receipt — gathers competitor projection + course + place context, dispatches to template renderer + sink"
      min_lines: 50
  key_links:
    - from: "apps/edge/src/routes/print.ts"
      to: "apps/edge/src/print/escposDriver.ts"
      via: "app.printerSink.print(envelope) — single-flight queue inside sink"
      pattern: "printerSink"
    - from: "apps/edge/src/print/templates/kids.ts"
      to: "apps/edge/src/print/kids-svg-to-bitmap.ts"
      via: "Reuse skogis generator output, rasterize SVG to PNG buffer, printer.printImage(buffer)"
      pattern: "printImage"
---

<objective>
Thermal print path — the final REQ-UI-004 binding contract for Phase 1 SC#5. After this plan:

- POST /api/competitions/:id/print-receipt drives a real ESC/POS print on Star TSP143 / Epson TM-T20 / Brother PJ-7 via /dev/usb/lp0.
- All 6 receipt templates render to the printer; Skogis renders as a raster bitmap derived from the same procedural generator that drives the UI mirror.
- Auto-print toggle (plan 13 UI + plan 04 PATCH endpoint + competitions.auto_print column) wires through the bridge: card_read with active competition + auto_print=true → enqueue print.
- Single-flight queue ensures only one print at a time; failures surface as toasts to the operator.

Honors:
- REQ-UI-004: thermal receipt with splits via ESC/POS; Star TSP143 / Epson TM-T20 / Brother PJ-7 supported via node-thermal-printer.
- UI-SPEC §"Receipt templates" (LOCKED 6 + monochrome Skogis).
- UI-SPEC §"Auto-print toggle" (~400ms after card_read; event-level setting).
- PATTERNS S-3 lazy native binding (node-thermal-printer wraps native USB write).
- RESEARCH §"Pitfall 6: USB device path varies" — probe /dev/usb/lp* at print time.

Output: ESC/POS driver, 6 templates, kids SVG → bitmap, print REST endpoint, auto-print bridge wiring, integration tests via PrinterSink (PATTERNS S-2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md
@.planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md
@.planning/phases/01-single-laptop-training-mvp/01-PATTERNS.md
@.planning/phases/01-single-laptop-training-mvp/01-SKETCHES/claude-design-bundle/project/print-app.jsx
@.planning/phases/01-single-laptop-training-mvp/01-04-PLAN.md
@.planning/phases/01-single-laptop-training-mvp/01-06-PLAN.md
@.planning/phases/01-single-laptop-training-mvp/01-08-PLAN.md
@.planning/phases/01-single-laptop-training-mvp/01-13-PLAN.md
@apps/web/src/lib/skogis/skogis.ts

<interfaces>
PrinterSink (defined in plan 03):
```
export interface PrinterSink {
  isPrinterConnected(): Promise<boolean>;
  print(envelope: PrintEnvelope): Promise<void>;
}
export interface PrintEnvelope { template: 'classic' | 'standing' | 'detailed' | 'top4' | 'minimal' | 'kids'; competition_id: string; card_number: number; data: unknown; }
```

Plan 15 lands the production implementation `createNodeThermalPrinterSink(opts: { printerType, devicePath })` that satisfies PrinterSink. The walking-skeleton's stdout-sink remains for tests + --no-printer dev mode.

Single-flight queue inside createNodeThermalPrinterSink — FIFO. `print()` returns a promise that resolves after the underlying printer.execute() completes. Concurrent callers serialize.

Skogis bitmap pipeline (plan 13's Svelte component renders SVG; plan 15 needs server-side rasterization):
- Reuse the skogis generator from apps/web/src/lib/skogis/skogis.ts: import it server-side via a small adapter at apps/edge/src/print/kids-svg-to-bitmap.ts that serializes SkogisDescriptor → SVG string → PNG buffer via sharp.
- PROBLEM: apps/web/src/lib/skogis/skogis.ts lives in @fartol/web's source. Phase 1 doesn't publish a shared package for it. RESOLUTION: move skogis.ts to packages/shared-types/src/skogis.ts (pure-TS, no DOM dep — the generator returns an abstract SkogisDescriptor, no actual SVG strings). apps/web continues to import it; apps/edge imports it for the bitmap pipeline. Plan 15 includes this move + import path updates in apps/web/src/lib/components/receipt-templates/Kids.svelte (the descriptor → SVG render in Svelte stays in apps/web; the descriptor → SVG-string serialization for sharp lives in apps/edge/src/print/kids-svg-to-bitmap.ts).

Alternative if move feels heavy: duplicate the generator in apps/edge/src/print/skogis-server.ts with a NOTE that it must stay byte-identical to apps/web/src/lib/skogis/skogis.ts. Choose the move approach — DRY wins for Phase 1.

competition.printer_type column — Phase 1 deferral: skip this column; default to STAR everywhere. Operators bench-verify on Star TSP143 first; Epson + Brother land via env var (FARTOL_PRINTER_TYPE) in plan 18 packaging. Documented as known limitation; UI-SPEC §"Tweaks panel" doesn't expose printer type today.

Auto-print wiring:
- The bridge (plan 06's bridge.ts) receives card_read events. On each card_read:
  1. Insert event (already done).
  2. Broadcast WS (already done).
  3. markDirty projection (plan 08, already done).
  4. NEW (plan 15): check active competition's auto_print; if true, ~400ms later (matches UI-SPEC §"Readout view live behavior") enqueue a print of the competitor's current projection via printerSink.
- The 400ms delay matches the UI receipt-toast timing (UI-SPEC). Implemented as a setTimeout inside the bridge's cardRead handler; cleared on detach.

T-USB-LP (USB lp device path): node-thermal-printer expects `printer:/dev/usb/lp0`. RESEARCH Pitfall 6 says probe /dev/usb/lp* at print time. createNodeThermalPrinterSink scans /dev/usb/lp{0..3} via fs.existsSync; first match wins.

T-DOS-PRINT: single-flight queue cap. If queue length exceeds 50, reject new prints with 429 (operator likely has a runaway loop). RESEARCH §"Security Domain V11" + plan 03 threat model T-DOS-WS analog.

Print REST endpoint shape:
```
POST /api/competitions/:id/print-receipt
Body: { competitor_id: string, template?: 'classic'|'standing'|'detailed'|'top4'|'minimal'|'kids' }
Response 201: { queued: true, queue_position: number }
Response 503: { error: 'printer_offline' | 'paper_out' | 'ebusy', detail: string }
Response 429: { error: 'queue_full' }
Response 404: { error: 'competitor_not_found' }
```
template defaults to competition.receipt_template. The endpoint resolves the competitor + course + projection (via ProjectionStore.get) + place context, builds a TemplateData object, and calls printerSink.print().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: node-thermal-printer driver + single-flight queue + templates (5 text + 1 stub for kids) + print route</name>
  <files>
    apps/edge/package.json,
    apps/edge/src/print/escposDriver.ts,
    apps/edge/src/print/escposDriver.test.ts,
    apps/edge/src/print/templates.ts,
    apps/edge/src/print/templates/classic.ts,
    apps/edge/src/print/templates/standing.ts,
    apps/edge/src/print/templates/detailed.ts,
    apps/edge/src/print/templates/top4.ts,
    apps/edge/src/print/templates/minimal.ts,
    apps/edge/src/print/templates/kids.ts,
    apps/edge/src/routes/print.ts,
    apps/edge/src/routes/print.test.ts,
    apps/edge/src/server.ts
  </files>
  <read_first>
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Pattern 6: ESC/POS thermal print via node-thermal-printer" (lines 495-522 — usable verbatim),
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Don't Hand-Roll" node-thermal-printer row,
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §"Pitfall 6: USB device path varies" (probe lp*),
    .planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md §"Receipt templates" + §"Receipt-specific typography",
    .planning/phases/01-single-laptop-training-mvp/01-SKETCHES/claude-design-bundle/project/print-app.jsx (print-side template structure),
    .planning/phases/01-single-laptop-training-mvp/01-PATTERNS.md §"Pattern S-2 sink injection" + §"Pattern S-3 lazy native require",
    apps/edge/src/print/sink.ts (plan 03 — PrinterSink interface)
  </read_first>
  <action>
    Add to apps/edge/package.json dependencies: `node-thermal-printer@^4.4.3`.

    apps/edge/src/print/escposDriver.ts — implements PrinterSink with PATTERNS S-3 lazy native require + single-flight:
    ```
    import { existsSync } from 'node:fs';
    import { createRequire } from 'node:module';
    import type { PrinterSink, PrintEnvelope } from './sink.ts';
    import { renderTemplate } from './templates.ts';

    export interface DriverOpts {
      printerType?: 'star' | 'epson' | 'brother';        // default 'star'
      devicePath?: string;                               // default: probe /dev/usb/lp0..3
      characterSet?: string;                             // default 'PC852_LATIN2' (covers sv å/ä/ö)
      queueCap?: number;                                 // default 50
      // Test injection (PATTERNS S-2):
      printerFactory?: (opts: { type: string; interface: string; characterSet: string }) => InternalThermalPrinter;
    }
    interface InternalThermalPrinter {
      isPrinterConnected(): Promise<boolean>;
      println(text: string): void;
      newLine(): void;
      bold(on: boolean): void;
      alignLeft(): void; alignCenter(): void; alignRight(): void;
      drawLine(): void;
      cut(): void;
      printImage(pathOrBuffer: string | Buffer): Promise<void>;
      execute(): Promise<void>;
      clear(): void;
    }

    export function createNodeThermalPrinterSink(opts: DriverOpts = {}): PrinterSink &amp; { dispose: () => void } {
      const queue: Array<() => Promise<void>> = [];
      let processing = false;
      let disposed = false;

      function probePath(): string | null {
        if (opts.devicePath) return opts.devicePath;
        for (let i = 0; i < 4; i++) { const p = `/dev/usb/lp${i}`; if (existsSync(p)) return p; }
        return null;
      }

      function buildPrinter(): InternalThermalPrinter | null {
        const path = probePath();
        if (!path) return null;
        if (opts.printerFactory) return opts.printerFactory({ type: opts.printerType ?? 'star', interface: `printer:${path}`, characterSet: opts.characterSet ?? 'PC852_LATIN2' });
        const require = createRequire(import.meta.url);
        const { printer: ThermalPrinter, types: Types } = require('node-thermal-printer');
        const typeMap = { star: Types.STAR, epson: Types.EPSON, brother: Types.BROTHER };
        return new ThermalPrinter({ type: typeMap[opts.printerType ?? 'star'], interface: `printer:${path}`, characterSet: opts.characterSet ?? 'PC852_LATIN2', removeSpecialCharacters: false, options: { timeout: 5000 } });
      }

      async function pump() {
        if (processing || disposed || queue.length === 0) return;
        processing = true;
        const job = queue.shift()!;
        try { await job(); } catch (e) { /* swallow — already surfaced via reject */ }
        processing = false;
        if (!disposed) pump();
      }

      return {
        async isPrinterConnected() {
          const printer = buildPrinter();
          if (!printer) return false;
          return printer.isPrinterConnected();
        },
        print(envelope: PrintEnvelope): Promise<void> {
          if (disposed) return Promise.reject(new Error('disposed'));
          if (queue.length >= (opts.queueCap ?? 50)) return Promise.reject(new Error('queue_full'));
          return new Promise((resolve, reject) => {
            queue.push(async () => {
              const printer = buildPrinter();
              if (!printer) { reject(new Error('printer_offline')); return; }
              try {
                const connected = await printer.isPrinterConnected();
                if (!connected) { reject(new Error('printer_offline')); return; }
                printer.clear();
                await renderTemplate(printer, envelope.template, envelope.data);
                printer.cut();
                await printer.execute();
                resolve();
              } catch (e) { reject(e); }
            });
            pump();
          });
        },
        dispose() { disposed = true; queue.length = 0; },
      };
    }
    ```

    apps/edge/src/print/templates.ts — single dispatcher:
    ```
    import classic from './templates/classic.ts';
    import standing from './templates/standing.ts';
    import detailed from './templates/detailed.ts';
    import top4 from './templates/top4.ts';
    import minimal from './templates/minimal.ts';
    import kids from './templates/kids.ts';
    export async function renderTemplate(printer: InternalThermalPrinter, name: 'classic'|'standing'|'detailed'|'top4'|'minimal'|'kids', data: unknown): Promise<void> {
      const fn = { classic, standing, detailed, top4, minimal, kids }[name];
      await fn(printer, data as any);
    }
    ```

    Templates (5 text-based, 1 image-based stub). Each accepts (printer, data) where data is shaped per UI-SPEC §"Receipt templates":
    - Each renders header (competition.name + competition.date), runner name, klass + bricka rows, body specific to template, footer (place + leader marker per UI-SPEC §"Visual Anchors" "Leder" not "ledning"), then leaves the cut + execute to escposDriver.

    apps/edge/src/print/templates/classic.ts: header → name → klass/bricka → splits table (code, leg, cum) → total time → place line.
    apps/edge/src/print/templates/standing.ts: header → name → total time → "PLATS X av Y i mål" → "+0:45 efter ledaren" or "★ Leder · —".
    apps/edge/src/print/templates/detailed.ts: header → name → per-leg with rank + time-lost columns.
    apps/edge/src/print/templates/top4.ts: header → "Topp 4" → top-4 leaderboard slice; highlight runner with bold; "Din placering" row below if runner not in top 4.
    apps/edge/src/print/templates/minimal.ts: header → name/class/SI → big total → place + behind-leader.
    apps/edge/src/print/templates/kids.ts: STUB in this task — calls `await printer.println(t('rcpt.kids.placeholder'))`. Task 2 wires the bitmap pipeline.

    Use node-thermal-printer's builder API (println, bold, alignCenter, drawLine, newLine). UI-SPEC §"Receipt-specific typography" suggests sizes for the on-screen MIRROR but the thermal printer renders at its own DPI; the templates use boldness + alignment + double-height for headings (printer.setTextDoubleHeight() if available; otherwise newLine padding).

    apps/edge/src/routes/print.ts:
    - POST /api/competitions/:id/print-receipt {competitor_id, template?}
    - Validate competitor exists; 404 if not.
    - Resolve template: body.template OR competition.receipt_template.
    - Resolve TemplateData: pull competitor row + course (via competitor.classId → course) + projection (app.projectionStore.get(competitionId) — call recomputeNow if cache miss) + placeContext (results_by_class[classId] → find this competitor's row).
    - Build PrintEnvelope = { template, competition_id, card_number: competitor.cardNumber ?? 0, data: { competitor, competition, classObj, course, placeContext } }.
    - Call app.printerSink.print(envelope).
    - Map errors: 'printer_offline' → 503; 'queue_full' → 429; 'paper_out' → 503; otherwise → 503 with generic 'print_failed'.
    - On success → 201 { queued: true, queue_position: <approximation from queue length> }.

    Update apps/edge/src/server.ts: choose the right PrinterSink at construction:
    - if process.env.FARTOL_PRINTER === 'stdout' (test/dev): createStdoutPrinterSink (plan 03).
    - else: createNodeThermalPrinterSink({ printerType: process.env.FARTOL_PRINTER_TYPE as any }).
    - Allow opts override for tests.
    Register print routes.

    apps/edge/src/print/escposDriver.test.ts — node:test using PATTERNS S-2 injected printerFactory:
    - test 1: createNodeThermalPrinterSink with a fake printerFactory; print(envelope) calls the fake's println/cut/execute in expected order.
    - test 2: Single-flight — two concurrent print() calls execute sequentially (assert ordering by spying on the fake's execute calls).
    - test 3: queueCap respected — 51st print() rejects with 'queue_full'.
    - test 4: No device path found (probePath returns null + no devicePath opt) → print rejects with 'printer_offline'.
    - test 5: dispose() clears queue + rejects future prints.

    apps/edge/src/routes/print.test.ts — node:test:
    - test 1: POST print-receipt with valid competitor + 'classic' template → 201; the test's injected printerSink (PATTERNS S-2 — buildServer accepts opts.printerSink for tests) received the envelope.
    - test 2: Unknown competitor → 404.
    - test 3: Template omitted → defaults to competition.receipt_template ('classic' for new competitions per plan 04).
    - test 4: printerSink rejects 'printer_offline' → 503.
    - test 5: printerSink rejects 'queue_full' → 429.

    Apply PATTERNS S-1 header to all new files.
  </action>
  <verify>
    <automated>cd /home/jonas/src/FartOL-phase-1 &amp;&amp; pnpm install --frozen-lockfile=false &amp;&amp; pnpm --filter @fartol/edge typecheck &amp;&amp; pnpm --filter @fartol/edge test 2>&amp;1 | grep -E '(pass|fail) [0-9]+|escpos|template|print'</automated>
  </verify>
  <done>
    Driver wraps node-thermal-printer with single-flight queue + queue cap + lazy native require. All 5 text templates render via the builder API. Print route maps errors correctly. Kids template stubbed for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Kids template via SVG-to-bitmap (sharp) + auto-print bridge wiring + ReadoutView print button wiring</name>
  <files>
    apps/edge/package.json,
    apps/edge/src/print/kids-svg-to-bitmap.ts,
    apps/edge/src/print/kids-svg-to-bitmap.test.ts,
    apps/edge/src/print/templates/kids.ts,
    apps/edge/src/si/bridge.ts,
    apps/edge/src/bin/fartol.ts,
    apps/web/src/lib/screens/ReadoutView.svelte,
    packages/shared-types/src/skogis.ts,
    packages/shared-types/src/index.ts,
    apps/web/src/lib/skogis/skogis.ts
  </files>
  <read_first>
    apps/web/src/lib/skogis/skogis.ts (plan 13 — the generator we are moving),
    .planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md §"Receipt templates" Kids row + monochrome rules,
    .planning/phases/01-single-laptop-training-mvp/01-UI-SPEC.md §"Auto-print toggle",
    .planning/phases/01-single-laptop-training-mvp/01-RESEARCH.md §A3 assumption (sharp/canvas SVG-to-PNG),
    apps/edge/src/si/bridge.ts (plan 06 + plan 08 — add auto-print enqueue),
    apps/edge/src/print/escposDriver.ts (just landed)
  </read_first>
  <action>
    Move generator into shared-types:
    - Create packages/shared-types/src/skogis.ts as a 1:1 copy of apps/web/src/lib/skogis/skogis.ts (with PATTERNS S-1 header noting the move + the apps/edge consumer).
    - Update packages/shared-types/src/index.ts to barrel-export `// --- Skogis (kids template generator) ---` from './skogis.ts'.
    - Update apps/web/src/lib/skogis/skogis.ts to re-export from '@fartol/shared-types' (one line: `export { generateSkogis } from '@fartol/shared-types'; export type { SkogisDescriptor, SkogisStats } from '@fartol/shared-types';`). Plan 13's Kids.svelte continues to compile against this path.

    Add `sharp@^0.34.0` to apps/edge/package.json dependencies (sharp is the most reliable Node SVG-to-PNG path; alternative @resvg/resvg-js is also fine — executor's choice, documented in summary).

    apps/edge/src/print/kids-svg-to-bitmap.ts:
    ```
    import { generateSkogis, type SkogisDescriptor } from '@fartol/shared-types';
    import { createRequire } from 'node:module';

    export interface KidsInput { card_number: number; name: string; club: string | null; class_id: string; }
    export interface BitmapOutput { png: Buffer; width: number; height: number; }

    export function descriptorToSvgString(d: SkogisDescriptor): string {
      const [x, y, w, h] = d.viewBox;
      const elements = d.elements.map(el => {
        const attrs = Object.entries(el.attrs).map(([k, v]) => `${k}="${v}"`).join(' ');
        return `<${el.type} ${attrs}/>`;
      }).join('');
      // Mono-printable: force black strokes, paper fills.
      return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="${x} ${y} ${w} ${h}" stroke="#000" stroke-width="1.5" fill="#fff">${elements}</svg>`;
    }

    export async function generateKidsBitmap(input: KidsInput): Promise<BitmapOutput> {
      const descriptor = generateSkogis(input);
      const svg = descriptorToSvgString(descriptor);
      const require = createRequire(import.meta.url);
      const sharp = require('sharp');
      const png = await sharp(Buffer.from(svg)).resize({ width: 384, fit: 'inside' }).png({ palette: true }).toBuffer();
      // 384 px = standard 80mm thermal column at 8 dots/mm.
      return { png, width: 384, height: 384 };
    }
    ```

    apps/edge/src/print/kids-svg-to-bitmap.test.ts — node:test:
    - test 1: generateKidsBitmap returns a PNG buffer > 100 bytes for a deterministic input.
    - test 2: Two calls with same input produce byte-identical buffers (determinism — relies on sharp + skogis both being deterministic).
    - test 3: descriptorToSvgString output contains 'stroke="#000"' and is valid XML (parse with a quick regex check).

    apps/edge/src/print/templates/kids.ts:
    ```
    import { generateKidsBitmap } from '../kids-svg-to-bitmap.ts';
    export default async function kids(printer: any, data: { competitor: any; competition: any; classObj: any; course: any; placeContext?: any }) {
      printer.alignCenter();
      printer.bold(true); printer.println(data.competition.name); printer.bold(false);
      printer.println(data.competition.date);
      printer.drawLine();
      printer.bold(true); printer.println(data.competitor.name); printer.bold(false);
      const bitmap = await generateKidsBitmap({ card_number: data.competitor.card_number ?? 0, name: data.competitor.name, club: data.competitor.club, class_id: data.competitor.class_id });
      await printer.printImage(bitmap.png);
      printer.println(`FART ${data._skogisStats?.fart ?? '—'}  STIG ${data._skogisStats?.stig ?? '—'}`);
      printer.println(`KART ${data._skogisStats?.kart ?? '—'}  TUR  ${data._skogisStats?.tur ?? '—'}`);
      printer.drawLine();
      printer.println('Spara kvittot — skogisen är din!');
    }
    ```
    Note on stats: the generator produces stats inside SkogisDescriptor; the template fetches them via a second call (or via a single call shared between bitmap + stats). Resolve in execution by either (a) caching the descriptor when generating the bitmap and reading stats from it, or (b) calling generateSkogis twice (fast deterministic). Document choice in summary.

    Update apps/edge/src/si/bridge.ts — auto-print path:
    - BridgeOpts gets two new fields:
      - `printerSink: PrinterSink`
      - `getCompetition: (id: string) => CompetitionDTO | null` (so the bridge can check auto_print + receipt_template per competition without crossing a layer boundary; the helper is built from app.fartolDb in the bin).
    - Inside the cardRead handler (after insertEvent + broadcast + markDirty), if activeCompetitionId !== null:
      - const comp = opts.getCompetition(activeCompetitionId);
      - if (comp?.auto_print) setTimeout(() => { void enqueuePrint(); }, 400);
      - enqueuePrint() resolves the competitor (via DB lookup card_number → competitor) + projection (opts.projectionStore.get) + builds the PrintEnvelope and calls printerSink.print(). Errors logged via stderr; no UI surface (the WS readout: channel can broadcast a 'print_failed' envelope — RESEARCH §Security Domain).
    - Document the 400ms delay matches UI-SPEC §"Readout view live behavior".

    Update apps/edge/src/bin/fartol.ts to pass printerSink + getCompetition to attachBridge.

    apps/web/src/lib/screens/ReadoutView.svelte:
    - The "🖨 Skriv ut kvitto" button (per UI-SPEC §"Component Inventory") POSTs to api.printReceipt(competitionId, competitorId, selectedTemplate) on click.
    - On 201 success: show toast "Skriver ut…" (UI-SPEC; toast auto-dismisses 2s).
    - On 503/429: show error toast "Utskrift misslyckades — Kontrollera papper i Star TSP143." (UI-SPEC §"Error states"; toast 5s).
    - When competition.auto_print === true: button disabled + AUTO badge per plan 13 (already wired); the actual auto-print happens server-side via the bridge.
    - Keyboard 'P' triggers the same button click (UI-SPEC §"Keyboard shortcuts").

    Apply PATTERNS S-1 header to all modified/new files.
  </action>
  <verify>
    <automated>cd /home/jonas/src/FartOL-phase-1 &amp;&amp; pnpm install --frozen-lockfile=false &amp;&amp; pnpm --filter @fartol/edge typecheck &amp;&amp; pnpm --filter @fartol/shared-types typecheck &amp;&amp; pnpm --filter @fartol/web typecheck &amp;&amp; pnpm --filter @fartol/edge test 2>&amp;1 | grep -E '(pass|fail) [0-9]+|kids|bitmap|auto-print'</automated>
  </verify>
  <done>
    Kids template renders Skogis as a printable monochrome PNG. Auto-print bridge wiring fires 400ms after card_read when competition.auto_print=true. ReadoutView's manual print button wires up. Plan 13's skogis usage continues to work via the shared-types re-export.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /dev/usb/lp0 → printer | Kernel-mediated USB write; node-thermal-printer wraps |
| sharp SVG input | Trusted (we generate the SVG from a server-side deterministic function — no user-controlled SVG content) |
| auto-print bridge → printerSink | Single-flight queue protects against double-fire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-USB-LP (lp device access) | Tampering | escposDriver probePath | mitigate | Probe /dev/usb/lp{0..3}; document required udev rule or `lp` group membership for the bench laptop (plan 18 packaging includes the install instructions). |
| T-DOS-PRINT | DoS | print queue | mitigate | queueCap default 50 + 429 response. |
| T-PRINTER-HANG | Reliability | printer.execute timeout | mitigate | node-thermal-printer options.timeout 5000ms; failed print rejects → 503 + toast. |
| T-SHARP-SVG-INPUT | DoS | sharp(svg) | mitigate | SVG is server-generated from deterministic SkogisDescriptor; sharp is given a string buffer, not a user-supplied file. |
| T-AUTOPRINT-LOOP | DoS | bridge auto-print on card_read | mitigate | The bridge only auto-prints on card_read (not card_inserted/removed); single-flight queue caps total prints. setTimeout 400ms is per-event, not retried. |
</threat_model>

<verification>
1. `pnpm --filter @fartol/edge build` produces dist including print/ folder.
2. `pnpm --filter @fartol/edge test` exits 0; print tests pass (~10 new tests).
3. Manual smoke (operator-only, requires hardware):
   - Plug Star TSP143 to /dev/usb/lp0.
   - `FARTOL_PRINTER_TYPE=star pnpm dev`.
   - POST /api/competitions/:id/print-receipt with a real competitor + 'kids' template → physical receipt with Skogis figure.
4. The auto-print path:
   - PATCH /api/competitions/:id with { auto_print: true }.
   - POST /api/__dev/simulate-read.
   - Within ~500ms the printer fires.
5. With no /dev/usb/lp* present, GET /api/__dev/simulate-read still works; the bridge logs auto-print fail to stderr; reads continue.
</verification>

<success_criteria>
- REQ-UI-004 satisfied: thermal receipt via ESC/POS works for all 6 templates.
- Single-flight queue + queue cap protects against overload.
- Kids template renders deterministic Skogis bitmap.
- Auto-print toggle wires through bridge → printer.
- Wave 5 (export + backup + packaging) unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/01-single-laptop-training-mvp/01-15-SUMMARY.md` using the GSD summary template.
Record:
- node-thermal-printer + sharp exact versions.
- Whether the skogis generator was moved to shared-types (per plan) or duplicated.
- Bench-test outcome on real Star TSP143 if Jonas runs it (manual gate — plan completes regardless; success criterion via hardware is a Wave 5 verification task).
- Whether stats came from a cached descriptor or a second generateSkogis call.
</output>
