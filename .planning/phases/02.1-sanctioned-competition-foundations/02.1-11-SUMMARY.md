---
phase: 02.1-sanctioned-competition-foundations
plan: 11
subsystem: eventor-linkage
tags: [eventor, competition, wizard, import, ui]
dependency_graph:
  requires: [02.1-01, 02.1-03, 02.1-10]
  provides: [eventor-event-linkage, wizard-quickstart, linked-import-card]
  affects: [competition-create, import-runners, competition-list]
tech_stack:
  added: []
  patterns: [drizzle-partial-index, svelte5-runes, fastify-proxy-route, i18n-interpolation]
key_files:
  created:
    - apps/edge/drizzle/0009_competitions_eventor_event_id.sql
    - apps/edge/drizzle/meta/0009_snapshot.json
    - apps/edge/src/eventor/fetchEvent.ts
  modified:
    - apps/edge/drizzle/meta/_journal.json
    - apps/edge/src/routes/eventor.ts
    - apps/edge/src/routes/eventor.test.ts
    - apps/edge/src/routes/competitions.ts
    - apps/edge/src/routes/competitions.test.ts
    - apps/edge/src/routes/competitionsFromWizard.ts
    - packages/shared-types/src/dtos.ts
    - apps/web/src/lib/api/client.ts
    - apps/web/src/lib/i18n/sv.json
    - apps/web/src/lib/i18n/en.json
    - apps/web/src/lib/screens/WizardStep1.svelte
    - apps/web/src/lib/screens/NewCompetitionWizard.svelte
    - apps/web/src/lib/screens/WizardStep3.svelte
    - apps/web/src/lib/screens/ImportRunnersView.svelte
    - apps/web/src/lib/components/CompetitionCard.svelte
decisions:
  - "Partial index on eventor_event_id (WHERE NOT NULL) keeps index size minimal since most competitions won't be linked"
  - "fetchEventorEvent in separate module (fetchEvent.ts) keeps eventor route handler thin and the fetch logic independently testable"
  - "Relink flow (startRelink/relinkEvent) reuses existing date-picker search rather than adding a new modal — one less UI surface to maintain"
  - "Eventor chip in CompetitionCard meta row with margin-left:auto pushes it flush-right to avoid visual clutter with starters/finished counts"
  - "Auto-approved Task 3 checkpoint per overnight instruction (non-blocking software verify)"
metrics:
  duration_minutes: 90
  completed: "2026-05-24"
  tasks_completed: 2
  tasks_skipped: 0
  checkpoints_auto_approved: 1
  files_created: 3
  files_modified: 14
---

# Phase 02.1 Plan 11: Eventor Event Linkage Summary

One-liner: Partial index + GET proxy for Eventor events, wizard ID-quickstart that prefills name/date, and linked-event card in ImportRunnersView that collapses the date-picker when `eventor_event_id` is already set.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Migration 0009 + Eventor proxy route + competition CRUD wiring | 9d69db2 | migration SQL, journal, snapshot, fetchEvent.ts, eventor.ts, competitions.ts, competitionsFromWizard.ts, shared-types dtos.ts |
| 2 | Wizard quickstart + linked-event card + competition chip | 325986d | WizardStep1, NewCompetitionWizard, WizardStep3, ImportRunnersView, CompetitionCard, client.ts, i18n sv+en |

## Checkpoint

**Task 3: visual end-to-end verify** (non-blocking) — auto-approved per overnight instruction. Operator should verify manually:
1. Create competition via wizard with Eventor event ID — name/date should prefill
2. Navigate to import runners — linked-event card should show with "Importera startlista" and "Lanka om" buttons
3. Check competition list for Eventor chip on linked competition

## Deviations from Plan

**1. [Rule 1 - Mapping] CompetitionList.svelte does not exist**
- Found during: Task 2 setup
- Issue: Plan references `apps/web/src/lib/screens/CompetitionList.svelte` which doesn't exist. Competition list is rendered via `CompetitionCard.svelte` inside `HomeView.svelte`.
- Fix: Added Eventor chip to `CompetitionCard.svelte` instead — same visual outcome, correct file.
- Files modified: apps/web/src/lib/components/CompetitionCard.svelte

**2. [Rule 2 - Relink UX] Relink flow wires into existing event list rows**
- Found during: Task 2 implementation
- Issue: Plan spec says "relink opens the existing date picker; on selection PATCHes the eventor_event_id" but didn't specify how event rows distinguish relink vs normal import.
- Fix: Event rows in relink mode call `relinkEvent(ev)` instead of `importEvent(ev)`, and button label changes to "Lankom" (Lanka om). Consistent with existing patterns; no new modal needed.

None — svelte-check passes (5 pre-existing errors unrelated to plan 11 changes).

## Known Stubs

None — all data flows wired: event ID from wizard → POST to from-wizard → stored in DB → fetched on ImportRunnersView mount → shown in linked-event card.

## Threat Flags

None — all new surface is within the existing Eventor API trust boundary (operator-supplied integer validated against live Eventor API before persistence; same ApiKey credential as Plan 02.1-08).

## Self-Check: PASSED

- [x] apps/edge/drizzle/0009_competitions_eventor_event_id.sql exists
- [x] apps/edge/src/eventor/fetchEvent.ts exists
- [x] apps/edge/drizzle/meta/0009_snapshot.json exists
- [x] Commit 9d69db2 exists (task 1)
- [x] Commit 325986d exists (task 2)
- [x] svelte-check: 0 new errors in modified files
- [x] typecheck (tsc --noEmit): clean
