# Dependencies
node_modules/

# Build output
dist/
coverage/
.svelte-kit/
apps/web/build/

# Playwright
playwright-report/
test-results/
tests/e2e/.tmp.db
tests/e2e/.tmp.db-wal
tests/e2e/.tmp.db-shm

# Edge bridge runtime
fartola.db
fartola.db-wal
fartola.db-shm

# Daily SQLite backup snapshots (plan 17 — default dir is ./backups/)
backups/

# Logs
*.log

# Local environment
.env
.env.*
!.env.example
.eventor-env
.eventor-env.*
*.secret

# Editor — keep shared extension recommendations, ignore personal settings
.vscode/settings.json
.idea/

# OS
.DS_Store
Thumbs.db

# Scratch space
tmp/

# Third-party reference material (not for public deploy)
docs/guide-meos.pdf

# Local reference material — competition fixtures (Purple Pen / Condes /
# IOF XML exports for specific events), upstream PDFs (Eventor API guide,
# MeOS user manual, IOF spec extracts), and anything else useful as
# context but that we do NOT want to ship in the public repo. Keep it
# local; mention specific files in .planning/* or docs/* by path so
# downstream readers know to ask the maintainer for a copy.
.reference/
