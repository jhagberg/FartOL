# Garbage byte 0x42 prefixed before a valid SET_MS(0x4D) frame.
# parseAll should silently drop the 0x42 and return one message
# (no onFrameError callback for the dropped byte — silent skip is the
# documented contract for stray garbage).
42 02 F0 01 4D 6D 0A 03
